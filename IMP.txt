# BRUTAL QUESTION LIST: FINPLAN PRO — EVERY ANGLE, EVERY SCENARIO

---

## 🔴 SECTION 1: EXISTENCE & PURPOSE QUESTIONS

1. Who is the **actual target customer** — a solo CFO, a Fortune 500 FP&A team, an SMB, or a consulting firm? Because these have radically different requirements and the product seems to be trying to serve ALL of them simultaneously.
2. Why does this need to be **both a desktop app AND a web app**? What specific user need requires Tauri? What data is so sensitive it can't be on a server?
3. If it's **offline-first**, who is paying for it? What's the business model? SaaS subscription doesn't work well with offline-first architecture.
4. Why would any enterprise buy a **React/TypeScript FP&A tool** when they already pay for Anaplan, Adaptive Insights, or Workday Adaptive Planning?
5. What problem does this solve that **Excel doesn't**? Most CFOs still live in Excel. What's the compelling migration story?
6. Is this a **real product being built**, a portfolio project, a startup MVP, or an architecture exercise? The answer dramatically changes what matters.
7. Why **174 engines**? Is this genuinely needed or is this complexity theater? Could 20 engines do 90% of the same work?
8. Why **140+ pages**? Has anyone done user research to confirm users actually need all these pages?
9. Who wrote the **15 sector configurations**? Were any actual banking/healthcare/insurance professionals consulted?
10. What is the **revenue model** — per seat, per entity, per module, flat fee, open source?

---

## 🔴 SECTION 2: ARCHITECTURE DECISIONS — CHALLENGED

11. You have **24 Zustand stores**. At what point does this become harder to manage than a single Redux store with proper selectors? Have you actually benchmarked the re-render counts?
12. Why **Zustand** and not **Jotai**, **Recoil**, **TanStack Query + Zustand**, or **XState** for the workflow engines? What was the actual decision process?
13. **Immer inside Zustand inside persist inside subscribeWithSelector** — four middleware layers. Have you profiled what this does to write performance on large state trees?
14. Why is `masterStorage` routing between **IndexedDB and SQLite** at runtime? What happens when the detection logic is wrong? What's the fallback?
15. The `CalculationGraph` uses **topological sort** — what happens with large, deeply nested dependency graphs? Have you tested with 10,000+ cells? 100,000?
16. You mention **ASC 810 compliant** consolidation. Has a CPA or auditor actually reviewed this engine? "Compliant" is a legal claim.
17. **ASC 842 and IFRS 16** lease accounting — same question. These standards have extremely specific calculation requirements. Who validated these engines?
18. **ASC 606 revenue recognition** — same question. Multi-element arrangements, variable consideration, contract modifications — are these all handled?
19. Why does the **FormulaEngine** reimplement Excel functions? Why not use a battle-tested library like HyperFormula or Formula.js?
20. The **IterativeCalculationEngine** resolves circular references — what's the algorithm? Gauss-Seidel? Fixed-point iteration? What are the convergence guarantees?
21. You have a **SafeMathParser** — what specific injection or overflow attacks is it preventing? Has it been penetration tested?
22. Why is **React 19** being used? It's not yet stable for production. What React 19 features are actually being used that justify this risk?
23. Why **Vite 7.3.2**? Vite 7 is very new. What's the upgrade story when breaking changes land?
24. **React Router 7.15.0** — v7 introduced significant API changes. Are you using the new loader/action API or the legacy `<Route>` API? The document shows legacy API.
25. Why **AG Grid 35** for grids instead of building on top of TanStack Table which is already a dependency?

---

## 🔴 SECTION 3: STATE MANAGEMENT — EXTREME SCRUTINY

26. With **24 stores**, what happens when Store A needs data from Store B which needs data from Store C? How are you preventing circular store dependencies?
27. The document shows `budgetStore` has an `undo/redo` system. `glStore` has undo/redo. Do they share the `UndoRedoEngine`? What happens when you undo a budget change that was triggered by a GL import?
28. `authStore` stores the `token` — you say tokens are stored in memory, not localStorage. But `persist` middleware persists to IndexedDB. Is the token being persisted to IndexedDB? That's a security hole.
29. The `uiStore` has `isOnline` as state. But `navigator.onLine` is notoriously unreliable. What's your actual offline detection strategy?
30. `cubeStore` stores a `CubeEngine` instance as state. **You're storing a class instance in Zustand state.** This breaks serialization, persistence, and devtools. How is this handled?
31. `driverStore` also stores a `DriverCascadeEngine` instance — same problem. Class instances in Zustand state are an anti-pattern. Why?
32. What happens to all 24 stores when a **user switches entities** (`authStore.activeEntityId`)? Does every store flush and reload? Is there a transaction mechanism?
33. `forecastStore` references `budgetStore` (base budget references). How? Does the store directly import and call `useBudgetStore.getState()`? That creates implicit coupling.
34. If `persist` is middleware on every store, and a user has **5 years of financial data**, how large does the IndexedDB database get? Have you tested with realistic data volumes?
35. The `partialize` function in persist — which fields are NOT persisted and why? The document says "certain fields" but doesn't specify. This matters enormously for crash recovery.
36. Store migrations — `src/store/migration/` — what triggers a migration? What happens if a migration fails mid-way? Is there rollback?
37. `subscribeWithSelector` — which stores actually use this and for what selectors? If everything subscribes to everything, you've just reinvented a reactive system with no garbage collection.
38. What is `storeCache.ts`? Is this a separate in-memory cache on top of Zustand? When does it invalidate? Can it get out of sync with store state?
39. `entityStore` has `offline cache` mentioned. Is this separate from `offlineCache.ts`? How many caching layers does this application actually have?
40. When a user logs out, which stores are cleared, which are preserved, and in what order? What happens if the clear fails halfway?

---

## 🔴 SECTION 4: ENGINE ARCHITECTURE — DEEP INTERROGATION

41. **174 engines** — are these all actually implemented, or are some stubs/scaffolding? Be honest.
42. You say engines are "pure TypeScript classes with static methods." But `CubeEngine` is stored as an instance in `cubeStore`. Which is it — static or instantiated?
43. The `EngineRegistry` lazy-loads engines. But **critical engines are loaded at startup**. What's the bundle size of just these 5 critical engines?
44. When `EngineRegistry.load()` fails (network error, chunk load error), what happens? Does the page crash? Is there retry logic?
45. `MonteCarloEngine` runs in a Web Worker. How many iterations are supported before the browser kills the worker? Have you set timeout limits?
46. `MonteCarloEngine` uses "normal, uniform, triangular distributions" — are these seeded? Reproducible? If a user runs Monte Carlo twice, do they get the same results?
47. `FormulaEngine` supports VLOOKUP — over what data structure? If a formula references another budget's data across entities, how does the reference resolve?
48. `FXEngine` — where does it get exchange rates? Hardcoded? API? Cached? What happens if rates are stale? What's the cache invalidation strategy?
49. `RevRecEngine` — ASC 606 Variable Consideration has the "constraint" concept. Is this implemented? What about contract modifications (prospective vs. cumulative catch-up)?
50. `LeaseEngine` — ASC 842 requires a specific present value calculation for ROU assets. Is the incremental borrowing rate variable or fixed? How is it stored?
51. `TaxEngine` — which jurisdictions are supported? Federal US only? State? International? Transfer pricing calculations are jurisdiction-specific.
52. `BondPricingEngine` with `YieldCurveEngine` — is this interpolating from a yield curve? What day count convention? ACT/360? ACT/365? 30/360?
53. `OptionPricingEngine` — Black-Scholes only? No binomial tree? No Monte Carlo pricing for path-dependent options?
54. `CreditRiskEngine` — what model? Altman Z-Score? Merton model? Internal rating? What inputs does it require?
55. `ESGEngine` — which carbon accounting standard? GHG Protocol? ISO 14064? TCFD? CSRD? These have different Scope 1/2/3 definitions.
56. `DataMaskingEngine` — what masking strategies? Static vs. dynamic masking? What about referential integrity after masking?
57. `ICMatchingEngine` vs `IntercompanyMatchingEngine` — why are there TWO intercompany matching engines? What does each do differently?
58. `AllocationEngine` vs `AllocationRuleEngine` — same question. What's the distinction?
59. `SmartImportMapper` vs `SmartImportMapping` — same question again. Two engines for mapping?
60. `WorkflowEngine` + `WorkflowBuilderEngine` + `WorkflowActionEngine` + `WorkflowTriggerEngine` + `WorkflowSchedulerEngine` + `WorkflowTemplateEngine` + `VisualWorkflowEngine` — **7 workflow engines**. What does each one actually do that couldn't be in one engine?
61. `AuditLogEngine` + `CellAuditTrailEngine` + `AuditEngine` — 3 audit engines. Why?
62. `ReportBuilderEngine` + `ReportLayoutEngine` + `ReportCacheEngine` + `ReportVersionEngine` + `ReportSchedulerEngine` + `ReportDistributionEngine` + `ReportBookEngine` — 7 report engines. Why not one with modules?
63. `ExportEngine` + `ProfessionalExportEngine` + `ExportTemplateEngine` — 3 export engines. What makes "Professional" different?
64. `AnomalyDetectionEngine` — what algorithm? Z-score? Isolation Forest? LSTM? What's the false positive rate?
65. `AnomalyExplainer` + `AutoCommentaryEngine` — are these using the HuggingFace transformers? Which model? What's the model size? Can it run in a browser?
66. `HuggingFace transformers 4.2.0` on WebGPU/WASM — what's the model? Distil-BERT? What's the minimum GPU/RAM requirement? What happens on a 2015 MacBook?
67. `NLQEngine` — Natural Language Query — what's the NLP backend? Rule-based? LLM? Can it handle "show me Q3 variance for the East region excluding intercompany"?
68. `IncrementalCalcEngine` — how does it know which cells are dirty? Does it maintain a dirty flag per cell? What's the data structure?
69. `BatchOperationEngine` — what constitutes a "batch"? Is there a size limit? Does it block the UI thread?
70. `StateMachine` — a generic state machine engine. What states and transitions are actually defined? Who uses this?

---

## 🔴 SECTION 5: PAGE ARCHITECTURE — CHALLENGED

71. **140+ pages** — are these all fully implemented with real data, or are many showing `<EmptyState />` or mock data?
72. `BudgetDetailPage` — a spreadsheet grid. How many rows/columns can it handle before performance degrades? What's been tested?
73. `WhatIfPage` vs `WhatIfSandbox` (component) vs `WhatIfSandboxEngine` — three different what-if constructs. Are they the same thing with different names?
74. `GLExplorerPage` — GL entries can be millions of records in a real company. How is pagination handled? Virtual scrolling? Server-side filtering?
75. `ConsolidationDashboard` — with 50 entities, each with 500K GL entries, how long does consolidation take? Is it run on the client? In a worker?
76. `ThreeStatementDashboardPage` — the three statements must balance (Assets = Liabilities + Equity, Net Income flows to Retained Earnings, Cash reconciles). Is this actually enforced?
77. `OwnershipTreePage` — how deep can the entity hierarchy go? 10 levels? 50? What renders an ownership tree with 200 entities?
78. `BoardPackPage` — board packs are typically 50-100 page PDFs. How long does generation take? Is it blocking? What's the maximum size tested?
79. The `DashboardPage` shows KPIs — from which store? Calculated how? With what date range? What entity?
80. `AIIntelligencePage` — what does this actually show? Anomalies? NLQ? Predictions? Is it connected to real engine output or is it a UI mockup?
81. How does `ClinicalTrialCostPage` work for a company that isn't in healthcare? Does it crash? Does it show an empty state? Does it even render?
82. `EmissionsTradingPage` — emissions trading requires real-time carbon credit prices. Where does this data come from?
83. `CSRDReportPage` — CSRD (Corporate Sustainability Reporting Directive) is an EU regulation. How does it handle US-only companies with no EU operations?
84. `ApprovalQueuePage` — approvals require server-side state in a real system. How do offline approvals work? Can two people approve simultaneously? What's the conflict resolution?
85. `NotFoundPage` — is this actually implemented? What does it look like?

---

## 🔴 SECTION 6: COMPONENT SYSTEM — INTERROGATED

86. **103 UI primitive components** — that's a lot. How much is actually custom vs wrapping Radix UI primitives? What's the actual custom code percentage?
87. `SpreadsheetGrid` component — is this custom or wrapping AG Grid? If AG Grid, why build a separate wrapper? What does the wrapper add?
88. `DataGrid`, `DataTable`, `FinancialTable`, `SpreadsheetGrid` — **four grid components**. What are the actual differences? When do you use each?
89. `FormulaBar` is in both `components/ui/` and `components/reports/`. Are these the same component? If not, why not?
90. `WaterfallChart` vs `WaterfallBridge` vs `CashFlowWaterfallEngine` — three waterfall constructs. What's the relationship?
91. `KPICard` vs `KPICardEnhanced` — why is there an "enhanced" version? Why not just improve the original?
92. `ErrorBoundary` + `AsyncErrorBoundary` + `PageErrorBoundary` + `EngineErrorBoundary` + `GridErrorBoundary` + `PluginErrorBoundary` — **6 error boundaries**. Each catches what specifically?
93. `GaugeChart` in `components/ui/` and `GaugeChart` in `components/charts/` — duplicate?
94. `BenchmarkRadar` — benchmarking against what? Industry peers? Historical data? Who provides the benchmark data?
95. `PresenceIndicator` — shows who is online. But this is an offline-first app. If two people are both offline with conflicting edits, what does the presence indicator show?
96. `CommandPalette` (Ctrl+K) with 80+ routes — does it search through all routes? Actions? Data? Or just navigation?
97. `GuidedTour` — how many tours are there? Are they triggered automatically on first use? Can users replay them?
98. `SandboxMode` — what exactly is sandbox mode? Does it prevent saves? Create a temp copy of state? What's the exit strategy?
99. `VersionDiffViewer` — versioning of what? Budgets? Reports? All data? How are versions stored?
100. `ChartAnnotationEngine` — can users add annotations that persist? Where are they stored? Per-user or shared?

---

## 🔴 SECTION 7: DATA FLOW — SCENARIO CHALLENGES

101. **Concurrent editing scenario:** User A edits budget cell B5. User B edits the same cell simultaneously. Both are offline. They come back online. What happens? Who wins?
102. **Large import scenario:** User imports an Excel file with 500,000 rows. `StreamImportEngine` handles it — in the browser? How much RAM does this require? What's the timeout?
103. **Formula dependency scenario:** Cell A1 = B1 + C1. Cell B1 = A1 * 2. This is circular. `IterativeCalculationEngine` resolves it. But what's the user experience when it can't converge?
104. **Multi-entity consolidation scenario:** 100 entities, each with 1M GL entries. Consolidation runs client-side? In a worker? How long? What if the browser tab closes mid-consolidation?
105. **Undo/redo across stores:** User creates a budget (budgetStore), imports GL data (glStore), runs consolidation (entityStore). They hit Ctrl+Z three times. What undoes in what order?
106. **Token expiry scenario:** User is filling a complex budget form (20 minutes of work). Token expires. Axios interceptor tries to refresh. Refresh fails. What happens to the unsaved form data?
107. **Schema migration scenario:** User has v1 budget data in IndexedDB. App updates to v2 with different budget schema. Migration runs automatically? Can it fail? What happens to the data?
108. **Plugin failure scenario:** A plugin crashes. `PluginErrorBoundary` catches it. But the plugin modified store state before crashing. Is that state rolled back?
109. **WebSocket disconnect scenario:** User is collaborating. WebSocket drops. They continue editing for 30 minutes. WebSocket reconnects. How are 30 minutes of local changes synced without conflicts?
110. **Report with cross-entity data scenario:** A report references Entity A's revenue and Entity B's expenses. Both entities have different fiscal calendars. How is period alignment handled?
111. **Currency translation scenario:** A German subsidiary reports in EUR. Consolidation translates to USD. Rates change mid-period. Which rate applies to income statement vs balance sheet? (CTA — Cumulative Translation Adjustment)
112. **Approval workflow scenario:** Budget is submitted for approval. Approver modifies it before approving. Does the modified version go back for re-review? Is the modification tracked?
113. **AI model loading scenario:** User opens the AI page on a slow connection. HuggingFace model downloads (could be 100MB+). What's the UX during download? What if download fails?
114. **Fiscal calendar scenario:** Company uses 4-4-5 fiscal calendar. User creates a formula spanning fiscal year end. Does the formula engine understand 4-4-5 period boundaries?
115. **Intercompany elimination scenario:** Entity A sells $1M to Entity B. Entity B hasn't recorded the purchase yet (hasn't imported GL). Elimination runs. What happens? Partial elimination? Error?

---

## 🔴 SECTION 8: SECURITY — EXTREME SCRUTINY

116. You say "Access tokens stored in memory (not localStorage)" but `persist` middleware writes to IndexedDB. Is the **JWT token being written to IndexedDB**? Yes or no?
117. `EncryptionEngine` — what algorithm? AES-256-GCM? Where is the encryption key stored? If it's in IndexedDB, that's circular.
118. `DataMaskingEngine` — masking is applied where? In the engine layer? In the UI? Can a user with browser devtools see unmasked data in the Zustand store?
119. RBAC is enforced "at both UI and engine levels" — how? Does every engine method check permissions? What's the mechanism? Show the actual enforcement code pattern.
120. **Viewer role** can read budgets. But budgets are in IndexedDB on their machine. They can open browser devtools and read ALL data regardless of RBAC. How is this addressed?
121. `SOXComplianceEngine` — SOX requires specific audit trail requirements (who changed what, when, provenance). Is the audit trail truly immutable? Can an Admin delete audit entries?
122. `DataClassificationEngine` classifies data as PII/sensitive. What happens after classification? Is PII encrypted differently? Is access logged differently?
123. `DataRetentionEngine` — retention policies delete data. Can an Admin accidentally delete 7 years of financial data that's required by law?
124. `tokenRotation.ts` — how often do tokens rotate? What's the refresh token lifetime? Where is the refresh token stored?
125. `securityHeaders.ts` in a **Vite config** — these headers only apply during Vite dev server. A static deploy to S3/CloudFront doesn't get these headers. How are they applied in production?
126. `MFA support` is mentioned — what MFA methods? TOTP? SMS? Hardware key? Where is TOTP secret stored?
127. `DataGovernanceEngine` — governance rules are stored where? Who can modify them? Is there an audit trail of governance rule changes?
128. The plugin architecture allows custom `ImportConnector` and `ExportFormat`. A malicious plugin could exfiltrate all financial data. What's the plugin sandboxing strategy?
129. `QuickBooksConnector` and `XeroConnector` — OAuth tokens for these are stored where? IndexedDB? That means they're accessible to any JS on the page (XSS).
130. `WebSocketManager` — what authentication is used for WebSocket connections? Is the JWT sent as a query param (bad) or in a header?

---

## 🔴 SECTION 9: PERFORMANCE — STRESS TESTED

131. **293KB bundle (92KB gzip)** — that seems impossibly small for an app with AG Grid, Recharts, Framer Motion, HuggingFace, ExcelJS, jsPDF, and 40+ dependencies. Is this the initial chunk only?
132. You have **6 vendor chunks** plus page chunks. How many total chunks are there? What's the total uncompressed asset size?
133. **140+ lazy-loaded pages** — each page load triggers a network request. On a slow connection, what's the first paint to interactive time for `/budgets/:id`?
134. `WorkerPool` with min 2, max 4 threads — on a single-core device (cheap Chromebook), what happens? Workers on single-core machines can degrade performance.
135. `persistenceDebouncer.ts` — what's the debounce interval? If a user types quickly in a budget cell (50 changes in 10 seconds), are all intermediate states lost if the browser crashes?
136. `memoryMonitor.ts` — what's the memory limit threshold? What happens when it's breached? Auto-garbage collection? Warning to user? App crash?
137. AG Grid with virtual scrolling on 1M rows — AG Grid's enterprise license is required for some virtualization features. Is the enterprise license included?
138. `Framer Motion 12.38.0` — animations on financial data tables. Has this been tested with reduced motion preferences? Does `useReducedMotion` actually disable all animations?
139. `routePreloader.ts` — which routes are preloaded? How is this determined? Does it preload based on user behavior, static heuristics, or both?
140. `QueryCache` for OLAP queries — what's the cache key strategy? If a user changes a GL entry, which cached OLAP queries are invalidated? All of them?
141. `CubePartitioner` — how does it partition? By time? By entity? By account? What's the partition size? How many partitions before query planning becomes expensive?
142. The entire app re-renders when `uiStore` changes (`theme`, `sidebarCollapsed`). With 24 stores, how many components subscribe to multiple stores simultaneously?
143. `Recharts` for charts — Recharts re-renders the entire SVG on any data change. For dashboards with 10 charts, each with real-time data, is this performant?
144. `@tanstack/react-virtual` is listed but so is AG Grid which has its own virtualization. Where is react-virtual actually used vs AG Grid's built-in virtualization?
145. `ExcelJS 4.4.0` — generating a complex Excel file with 50 sheets and 10K rows each. How long? Blocking? Does it run in the export worker?

---

## 🔴 SECTION 10: TESTING — CHALLENGED

146. **473 test files** — what's the actual test coverage percentage? Lines covered, branches covered?
147. The engines claim to be "pure TypeScript with static methods" — are they actually tested with realistic financial data or just unit tests with `expect(2+2).toBe(4)` style assertions?
148. `ConsolidationEngine` — tested for ASC 810 compliance how? Are there test cases that represent actual accounting standards?
149. `LeaseEngine` (ASC 842) — is there a test that generates an amortization table and verifies it against an independently calculated result?
150. `FormulaEngine` — tested with circular references? Division by zero? Very large numbers? Locale-specific decimal separators (European comma vs period)?
151. **Smoke tests** — smoke tests only verify the page renders without crashing. They don't verify correctness. What percentage of tests are actually correctness tests?
152. `playwright` E2E tests — what's the E2E test coverage? How many user journeys are fully covered end-to-end?
153. The tests run in `jsdom` — but the app uses IndexedDB, WebSockets, Web Workers, and Tauri APIs. How are these mocked? Is the mock realistic?
154. `monte-carlo.worker.ts` — is the Monte Carlo worker tested? How do you test statistical correctness of random simulations?
155. With 473 test files, what's the **CI runtime**? Is this acceptable for a development workflow?
156. Are there **performance regression tests**? If someone adds a slow engine, will CI catch it?
157. Are there **snapshot tests**? If a financial statement format changes, is that caught?
158. `DataQualityEngine` — how is data quality tested? What constitutes "quality" and how is it measured in tests?
159. Security tests — is there any **penetration testing** or input fuzzing? Especially for the formula parser (injection risk)?
160. Are tests run against the **Tauri desktop app** or only the web version? Are there any desktop-specific tests?

---

## 🔴 SECTION 11: INTERNATIONALIZATION — DEEP QUESTIONS

161. The app has financial data — numbers, currencies, dates. Different locales format these completely differently. Does `en` format `1,234.56` and `de` format `1.234,56`? Is this actually implemented throughout every grid and chart?
162. Arabic is RTL. Does the spreadsheet grid support RTL text direction? Does AG Grid support RTL properly?
163. Financial terminology differs by country. "Net Income" (US) = "Profit After Tax" (UK) = "Résultat Net" (France). Are the translation files actually written by native-speaking finance professionals?
164. Japanese and Chinese have different number formatting (万 = 10,000 unit). Is this handled?
165. IFRS vs US GAAP — the app mentions both. Different countries use different standards. Is the app's accounting logic configurable by standard?
166. `i18next-browser-languagedetector` — what if the browser is in German but the company is a US company? Language vs locale vs accounting standard can all differ.
167. Currency formatting — `$1,234.56` vs `€1.234,56` vs `¥1,234` (no decimals). Is every currency formatted correctly in every locale combination?
168. Date formatting across locales — fiscal periods displayed as "Q3 FY2024" (US) vs "Q3 GJ2024" vs "2024年度第3四半期" (Japanese). Is this handled?
169. Translation file completeness — are all 8 languages 100% complete, or are some pages still in English when the locale is Japanese?
170. The translation files are JSON — who maintains them? If a developer adds a new feature with new strings, is there a process to get all 8 languages translated?

---

## 🔴 SECTION 12: REAL-TIME COLLABORATION — INTERROGATED

171. `RealtimeCollaborationManager` — what's the underlying protocol? OT (Operational Transformation)? CRDT? Last-write-wins? The choice determines conflict resolution behavior.
172. `WebSocketManager` — what server handles the WebSocket? The app is described as offline-first with no backend. Where does the WebSocket connect to?
173. If there's no backend server (offline-first), how does real-time collaboration work? Two clients can't share state without a coordination server.
174. `PresenceService` — presence requires a server to broadcast. Same problem. How?
175. `SyncEngine` — syncs offline changes when reconnecting. To what server? What's the sync protocol?
176. `ChangeBroadcaster` — broadcasts changes to other users. Via WebSocket? Direct P2P? Server relay?
177. **Conflict resolution:** Two users edit the same budget simultaneously. Both are online. User A changes cell B5 to $1000. User B changes the same cell to $2000. What does User C see?
178. **Approval workflows** — if an approval requires a majority vote and one approver is offline, does the workflow pause? Is there a timeout?
179. `CellCommentEngine` — cell comments in a spreadsheet. If User A deletes a row and User B is viewing a comment on that row, what happens?
180. The `activityLog` in `collaborationStore` — how large does this get? Is it paginated? Is it persisted to IndexedDB or memory-only?

---

## 🔴 SECTION 13: EXTERNAL INTEGRATIONS — CHALLENGED

181. `QuickBooksConnector` — QuickBooks Online API has rate limits (500 calls per minute). How is rate limiting handled?
182. `XeroConnector` — Xero's API requires OAuth 2.0 PKCE flow in a browser context. How is the redirect URI handled in a desktop Tauri app?
183. `RestApiClient` — generic REST client. What authentication methods does it support? API keys? OAuth? Basic auth?
184. `ConnectorRegistry` — plugins can add connectors. A malicious connector could make arbitrary HTTP requests. What's the sandboxing?
185. The mock data files — 19 of them. Is mock data used in production builds? How is it disabled? Is there a `NODE_ENV` check?
186. `exchangeRates.ts` mock data — mock exchange rates in production means currency translation is wrong. How is this replaced with real data?
187. If QuickBooks integration is live, financial data is leaving the device. How is this disclosed to the user? Is there consent management?
188. `ConnectorEngine` — is the same connector engine used for both QuickBooks/Xero AND the plugin connectors? Are there permission differences?
189. What happens when the QuickBooks API is down? Does the import fail silently? Does the UI communicate the error clearly?
190. Is there any **data validation** when importing from external APIs? Could a malformed API response corrupt the GL store?

---

## 🔴 SECTION 14: DESKTOP APP (TAURI) — QUESTIONED

191. Tauri uses Rust — what Rust crates are used? What's the Tauri version? Tauri 2.0 is a major breaking change from 1.x.
192. SQLite for desktop persistence — what's the SQLite schema? Is it normalized? What happens with schema migrations across app versions?
193. `WindowStateManager` — if the user moves the window to a second monitor and then disconnects it, what happens on next launch?
194. `.finplan` native file format — what's the format? Binary? JSON? XML? What's the versioning strategy?
195. Code signing — is the desktop app code-signed? macOS Gatekeeper will block unsigned apps. Windows SmartScreen same.
196. Auto-update — is there an auto-update mechanism? How are updates delivered? Tauri's built-in updater?
197. **Platform support:** macOS, Windows, Linux? Which versions? M1/M2 Macs? ARM Windows?
198. The web app and desktop app share the same codebase but different storage (IndexedDB vs SQLite). Are there any behaviors that work in web but not desktop or vice versa?
199. `tauriSqlStorage.ts` — is this using `tauri-plugin-sql`? What's the query interface? Prepared statements? Or string concatenation (SQL injection risk)?
200. The `checkTauri()` function detects if running in Tauri. What's the detection mechanism? Is it reliable? Can it be spoofed?

---

## 🔴 SECTION 15: PLUGIN ARCHITECTURE — INTERROGATED

201. Plugins can add `formulaSpecs` — custom formula functions. These run in the browser. Can a plugin formula function access the DOM? The store? Network?
202. Plugins can add `ImportConnector` — this can make network requests. What CSP restrictions apply?
203. `PluginErrorBoundary` catches plugin render errors. But what about plugin **engine** errors that happen outside React?
204. Plugin `DashboardWidget` — widgets render arbitrary JSX. Is there any security review for plugin submissions?
205. `TemplateMarketplace` — is this a real marketplace with backend, or a local template library?
206. Plugin versioning — if a plugin is built for v1.0 of FinPlan Pro and the user upgrades to v2.0, does the plugin still work?
207. Plugin storage — can plugins write to IndexedDB/SQLite? Can they read other plugins' data? Is there namespacing?
208. Who develops plugins? Third parties? Just internal team? Is there a plugin SDK?
209. Plugin activation/deactivation — if a plugin is deactivated, are its formula results still shown in cells that used its formulas?
210. `WorkflowRule` plugins — a plugin could create workflow rules that automatically approve financial transactions. What governance exists?

---

## 🔴 SECTION 16: PRODUCT & UX — REALITY CHECK

211. **8 languages, 15 industries, 174 engines, 140 pages** — how long did this take to build? How large is the team?
212. A new user lands on this app. There are 80+ routes, 15 sectors, 24 stores worth of features. What does the **onboarding experience** look like?
213. `OnboardingWizard` — what does it ask? How does it configure the app? If a user picks the wrong sector, can they change it?
214. A CFO who has used Adaptive Insights for 10 years — what's the **migration path**? How do they import their existing models?
215. `MigrationWizard` — from which systems? Adaptive Insights? Hyperion? Excel only?
216. The `CommandPalette` (Ctrl+K) — how does a user discover it exists?
217. `GuidedTour` — is it actually useful or annoying? Can users skip it permanently?
218. The sidebar has "Main, Analysis, Management" sections — with 80+ pages, how do users find what they need?
219. A user builds a complex budget model — 50 line items, 20 drivers, multiple scenarios. If they lose their work (browser crash, power outage), what exactly is recoverable?
220. `AutoSaveEngine` — what's the save interval? What happens if autosave fails?
221. `CrashRecoveryEngine` — how does it detect a crash vs a clean close? What data is recovered?
222. The financial grid (`BudgetDetailPage`) — can users insert rows, delete rows, merge cells, format cells? Is this a full spreadsheet or a fixed-template grid?
223. **Error messages** — when a formula error occurs (div/0, circular ref, invalid ref), what does the user see? Is it Excel-style `#DIV/0!` or something more helpful?
224. `SystemHealthMonitor` — what does this monitor? Engine performance? Store size? WebSocket status? What does the user see?
225. `SaveStatusIndicator` — what are the states? "Saving...", "Saved", "Save failed"? What happens on "Save failed"?

---

## 🔴 SECTION 17: COMPLIANCE & LEGAL — SERIOUS QUESTIONS

226. The app claims **ASC 810, ASC 842, ASC 606, IFRS 16, SOX** compliance. Has this been reviewed by a certified public accountant?
227. **GDPR** — financial data of EU employees/customers. Is there a privacy policy? Data processing agreement? Right to erasure in IndexedDB?
228. **SOC 2** — enterprise customers will ask for SOC 2 Type II. How does an offline-first app get SOC 2 certified?
229. **Data residency** — if a German company uses this, can they guarantee financial data never leaves Germany? It's all client-side, so yes — but what about WebSocket sync and QuickBooks integration?
230. **Audit trail immutability** — SOX requires audit trails that cannot be modified. But the app runs client-side where users have full IndexedDB access. How is immutability guaranteed?
231. **Financial calculations accuracy** — if a company makes a business decision based on an incorrect NPV calculation from `FormulaEngine`, what's the liability?
232. **Software warranty** — is there a disclaimer that this is not certified financial software?
233. **Accessibility law** — WCAG 2.1 AA is not just good practice; it's legally required in many jurisdictions for commercial software. Has it been independently audited?
234. **Export regulations** — financial modeling software with encryption may be subject to export controls (EAR/ITAR). Has this been evaluated?
235. **Open source licenses** — AG Grid Community is AGPL. Does your project need an AG Grid Enterprise license? AGPL requires your source to be open if distributed.

---

## 🔴 SECTION 18: SCALABILITY & FUTURE — QUESTIONED

236. Today it's offline-first. If a future customer says "we need a proper backend with SSOT database" — how hard is the migration? The entire architecture is client-side.
237. **Multi-tenancy** — multiple companies using the same deployment. How are data boundaries enforced? Right now it seems single-tenant by design.
238. The `entityStore` handles multi-entity. But can different entities have different sector configurations? Different fiscal calendars? Different currencies? All simultaneously?
239. If you add a **25th Zustand store**, what's the process? Is there a checklist? How do you ensure it doesn't break existing store dependencies?
240. If you add a **175th engine**, how does someone discover it exists? The `EngineRegistry` seems to be the answer, but how is it documented?
241. **Deprecating a page** — if `/analytics/benchmarking` needs to be removed, what's the process? Remove route, remove lazy import, remove component, remove translations, remove tests — is there a checklist?
242. The **15 sector configs** — adding a 16th. What's the process? Is there a sector config template? Are there required fields?
243. **Breaking API changes** — if `budgetStore.createBudget()` signature changes, how many files reference it? How is the breaking change detected before production?
244. **Tech debt** — with 1,190 files, how is tech debt tracked? Is there any automated code quality gate?
245. **Documentation** — you have this architecture document. But is there inline code documentation? JSDoc? TSDoc? How does a new developer understand `CubePartitioner`?

---

## 🔴 SECTION 19: SPECIFIC TECHNICAL INCONSISTENCIES

246. `React 19.2.6` is listed. React 19 stable was released December 2024. Version `19.2.6` doesn't exist as of knowledge cutoff. Is this a typo?
247. `Vite 7.3.2` — Vite 7 doesn't exist as of knowledge cutoff. Is this aspirational? A mistake?
248. `Zustand 5.0.13` — is this stable? What breaking changes from Zustand 4?
249. `Framer Motion 12.38.0` — extraordinarily high version. Is this real?
250. `Lucide React 1.14.0` — also unusually high. Confirmed real version?
251. `Recharts 3.8.1` — Recharts 3.x is in development/alpha. Is this stable?
252. `@huggingface/transformers 4.2.0` — the package name is `@xenova/transformers` or `@huggingface/transformers`. The v4 API changed significantly. Which API are you using?
253. `i18next 26.2.0` — i18next at v26? That seems high. Confirmed?
254. `react-i18next 17.0.8` — v17? Confirmed?
255. `jsPDF 4.2.1` — jsPDF at v4? Current version is 2.x. Is this a fork or future version?
256. `Zod 4.4.3` — Zod v4 introduced breaking changes from v3. Which Zod API is being used?
257. `Vitest 4.1.6` — Vitest at v4? Current is v1.x/v2.x. Confirmed?
258. `Playwright 1.60.0` — plausible but high. Confirmed?
259. `tailwind CSS 4.1.17` — Tailwind v4 is a complete rewrite. Is the configuration compatible? v4 eliminated `tailwind.config.js`.
260. `class-variance-authority 0.7.1` — current version is around 0.7.x. Plausible.

---

## 🔴 SECTION 20: THE HARDEST QUESTIONS

261. If you had to **cut this project to 20% of its current scope** to ship something real to a paying customer in 3 months, what would you keep?
262. Has **any real FP&A professional** actually used this application and given feedback? Or is it built based on assumptions about what FP&A professionals need?
263. What is the **single most likely failure mode** of this entire system? What keeps you up at night?
264. If a **Big 4 accounting firm** did a code review of the financial calculation engines, what would they say?
265. If a **security firm** did a penetration test, what would they find first?
266. If a **performance engineer** profiled the app with realistic data (50 entities, 5 years of GL data), what would be the bottleneck?
267. The document says "1,190 TypeScript files." How many of these files are **actually complete** vs scaffold/placeholder?
268. What's the **honest completion percentage** of each of the 174 engines? 100%? 50%? 10%?
269. What would it take to **get this audited and certified** for use by a publicly traded company subject to SEC reporting requirements?
270. If this project **fails** — what's the single biggest architectural decision you'd do differently?

# 270 MORE BRUTAL QUESTIONS — GOING DEEPER

---

## 🔴 SECTION 21: THE FORMULA ENGINE — MICROSCOPICALLY EXAMINED

271. The FormulaEngine supports `VLOOKUP` — what's the lookup array? If it's looking up values across different budget line items, what's the reference syntax? `=VLOOKUP(A1, Budget!B:C, 2, FALSE)` — does "Budget!" cross-sheet referencing work?
272. `=NPV(rate, value1, value2...)` — does your NPV implementation match Excel's convention where the first cash flow is at period 1, not period 0? This is a known Excel "bug" that everyone has standardized on.
273. `=XIRR` requires date-based cash flows. Where do the dates come from in your system? Are they fiscal periods? Calendar dates? What if the periods are irregular?
274. `=IRR` — what's your initial guess for Newton-Raphson iteration? What happens with multiple IRR solutions (non-conventional cash flows)?
275. `=SLN`, `=SYD`, `=DDB` are depreciation functions. Are these coordinated with the `CapExEngine`? If a user calculates depreciation via formula AND via the CapEx module, can they get different answers?
276. `=PERCENTILE` — which convention? Excel's PERCENTILE.INC vs PERCENTILE.EXC? These give different results for small datasets.
277. `=FORECAST` — is this linear regression only? Or does it support exponential smoothing, seasonal adjustments?
278. `=CORREL` — Pearson correlation. Do you also support Spearman rank correlation? Finance teams often need both.
279. What's the maximum formula string length supported? Excel supports 8,192 characters. What's yours?
280. Formula parsing — do you support array formulas? `{=SUM(IF(A1:A10>0, A1:A10, 0))}` — the curly brace array entry syntax?
281. Named ranges — can users define `Revenue` as a named range and write `=Revenue * 0.3`? Where are named ranges stored?
282. Cross-budget references — can a formula in Budget A reference a cell in Budget B? Syntax? Performance implications?
283. Cross-entity formula references — `=ENTITY("Germany").Revenue.Q3` — is any syntax like this supported?
284. Formula localization — in Germany, the decimal separator is a comma. Does `=SUM(1,5; 2,3)` work in German locale? The function argument separator changes too.
285. Formula error propagation — if Cell A1 has `#DIV/0!`, and Cell B1 has `=A1 + 100`, does B1 show `#DIV/0!` or `#ERROR!` or something else?
286. `=IFERROR` — is this supported? `=IFNA`? These are essential for robust formula writing.
287. Volatile functions — `=TODAY()`, `=NOW()`, `=RAND()` recalculate on every sheet change. How does your `CalculationGraph` handle volatile functions without infinite recalculation?
288. Text in numeric formulas — `=SUM("100", 200)` — does this coerce the text to a number like Excel does?
289. Empty cell treatment — in Excel, an empty cell in `=A1 + B1` treats A1 as 0. Does yours?
290. Formula auditing — can users trace precedents and dependents visually (like Excel's blue arrows)? Is this implemented?

---

## 🔴 SECTION 22: THE THREE-STATEMENT MODEL — ACCOUNTING REALITY

291. The three statements must **balance mathematically**. Net Income → Retained Earnings (Balance Sheet). Cash from Operations reconciles Net Income to Cash. Is this enforced as a hard constraint or just calculated?
292. If the Balance Sheet **doesn't balance** (Assets ≠ Liabilities + Equity), what happens? Error? Warning? Silent imbalance?
293. Deferred Tax Assets/Liabilities — do they appear on the Balance Sheet? Are they calculated from the `TaxEngine`? Do they flow correctly?
294. **Goodwill impairment** — if an entity acquires another with goodwill, is goodwill on the Balance Sheet? Does it amortize or get impairment tested?
295. **Working capital in the Cash Flow statement** — changes in AR, AP, inventory must reconcile. Is this automated from GL entries or manually entered?
296. **CapEx in Cash Flow** — capital expenditures flow through investing activities. Does the `CapExEngine` automatically populate the Cash Flow statement?
297. **Debt proceeds and repayments** — financing activities. Does the `DebtScheduleEngine` automatically populate the Cash Flow statement?
298. **Stock-based compensation** — non-cash expense that's added back in operating activities. Is SBC tracked as a line item and properly handled in Cash Flow?
299. **Depreciation & Amortization** — non-cash charges added back. Does the `CapExEngine` feed D&A back to the Cash Flow statement automatically?
300. **Minority interest / Non-controlling interest** — in consolidation, NCI appears on both Balance Sheet and Income Statement. Is this calculated and presented per ASC 810?
301. **Comprehensive income** — OCI (Other Comprehensive Income) items like foreign currency translation adjustments, unrealized gains on AFS securities. Is OCI captured and presented?
302. **Earnings Per Share** — basic and diluted. Is this calculated? Does it require share count data? Where is that stored?
303. **EBITDA reconciliation** — EBITDA is not a GAAP measure but every board pack has it. How is it calculated and is it reconciled to GAAP Net Income?
304. **Cash and cash equivalents definition** — what qualifies? Money market funds? Commercial paper under 90 days? Is this configurable?
305. **Beginning and ending cash reconciliation** — Cash Flow statement ends with ending cash. Does this match Cash on the Balance Sheet? Is this enforced?

---

## 🔴 SECTION 23: BUDGET & FORECAST METHODOLOGY

306. **Driver-based planning** — what are the driver types? Volume × Price? Headcount × Cost per head? Percentage of revenue? Are all these supported?
307. **Driver cascade** — if Revenue grows 10%, and COGS is 60% of Revenue, does COGS automatically cascade? What about multi-level cascades?
308. **Budget versions** — v1, v2, v3 budget. Can you compare budget versions? Lock previous versions? Audit who changed what between versions?
309. **Rolling forecast** — 12-month rolling vs. year-end forecast. Does the `RollingForecastEngine` handle the period boundary correctly when rolling from December to January?
310. **Phasing/seasonality** — how is annual budget distributed across months? Equally? By historical pattern? Manually? Is there a seasonality curve library?
311. **Bottom-up vs top-down budgeting** — can department heads enter their own budgets which then roll up? Is there a submission workflow?
312. **Budget locking** — can a budget be locked after approval so no further changes are possible? Who can unlock it?
313. **Budget comments/notes** — can users add commentary to specific budget line items? Separate from cell comments?
314. **Zero-based budgeting** — a specific methodology where every expense must be justified from zero. Is this supported as a mode?
315. **Incremental budgeting** — taking last year's actuals and applying a percentage increase. Is there a wizard for this?
316. **Activity-based budgeting** — budget based on cost drivers and activities. Is this distinct from driver-based planning?
317. **Forecast accuracy tracking** — comparing past forecasts to actuals. Is there a forecast accuracy KPI dashboard?
318. **Consensus forecasting** — multiple stakeholders submit forecasts, system generates consensus. Is this supported?
319. **Assumption documentation** — every budget has assumptions. Is there a formal assumption register with owner, date, and sensitivity?
320. **Budget vs. actual cadence** — is BvA available weekly, monthly, quarterly? Can users configure the comparison period?

---

## 🔴 SECTION 24: CONSOLIDATION — ACCOUNTING DEPTH

321. **ASC 810 VIE (Variable Interest Entity)** — is VIE consolidation analysis supported? Identifying the primary beneficiary? This is distinct from voting interest consolidation.
322. **Push-down accounting** — when an entity is acquired, does the system support push-down accounting (adjusting the subsidiary's books to acquisition-date fair values)?
323. **Acquisition accounting (ASC 805)** — purchase price allocation, fair value of assets/liabilities at acquisition date. Is this in scope?
324. **Step acquisitions** — owning 30% then buying another 40% to reach 70% control. The accounting treatment changes at control. Is this handled?
325. **Loss of control** — divesting from 70% to 25% (now an equity method investment). The accounting is a remeasurement event. Is this transition handled?
326. **Equity method details** — under equity method, you record your proportionate share of earnings. But you also have basis differences (excess purchase price). Is amortization of basis differences supported?
327. **Proportionate consolidation** — some joint ventures use proportionate consolidation (line-by-line, proportionate share). Is this a consolidation method option?
328. **Statutory consolidation vs management consolidation** — companies often run both. Different eliminations, different adjustments. Can the system produce both simultaneously?
329. **Currency translation — functional vs presentation currency** — ASC 830 distinguishes these. The functional currency determination affects whether you use temporal method or current rate method. Is this modeled?
330. **CTA (Cumulative Translation Adjustment)** — the plug that makes the Balance Sheet balance after currency translation. Is CTA calculated and does it appear in OCI?
331. **Intercompany loan elimination** — IC loans have interest income/expense. Both sides must be eliminated. Does the IC matching also handle interest on intercompany balances?
322. **Dividend elimination** — parent receives dividends from subsidiary. These must be eliminated in consolidation. Is dividend elimination separate from other IC eliminations?
333. **Intercompany inventory profit elimination** — if Entity A sells inventory to Entity B at a markup, the unrealized profit must be eliminated until sold externally. Is this calculated?
334. **Minority interest in IC eliminations** — when an 80%-owned subsidiary has IC transactions, 20% of the IC elimination affects NCI. Is this split calculated correctly?
335. **Consolidation adjustments vs. elimination entries** — are these tracked separately? Can you report pre-elimination, eliminations, and post-elimination columns separately?

---

## 🔴 SECTION 25: CASH & TREASURY — SPECIFIC GAPS

336. **Cash concentration / cash pooling** — many multinationals pool cash across entities. Is notional pooling or physical pooling supported?
337. **Restricted cash** — some cash is restricted (security deposits, bond reserves). Does the system distinguish restricted from unrestricted cash?
338. **Bank reconciliation** — reconciling book cash to bank statements. Is this a feature? Where is bank statement data imported?
339. **Cash flow classification disputes** — under ASC 230, some items can be classified as operating, investing, or financing (e.g., debt issuance costs, contingent consideration). Is classification configurable?
340. **Short-term vs long-term debt reclassification** — debt due within 12 months of the balance sheet date must be classified as current. Does the `DebtScheduleEngine` handle this reclassification?
341. **Interest rate swaps** — a company might have a fixed-rate loan plus an interest rate swap, effectively creating a variable rate. Is this scenario modeled?
342. **Cross-currency swaps** — hedging FX exposure on a foreign currency debt. Both the debt and the swap must be marked to market. Is this in scope?
343. **Treasury stock** — share buybacks. Does treasury stock appear on the Balance Sheet as a contra-equity account?
344. **Cash burn rate and runway** — startup-relevant metric. Is this automatically calculated from the cash forecast?
345. **Lockbox and payment terms** — DPO, DSO, DIO metrics. Are these calculated from actual GL data or from manual inputs?

---

## 🔴 SECTION 26: WORKFORCE — SPECIFICS QUESTIONED

346. **Headcount plan granularity** — is headcount tracked at individual level, job code level, or department level? What's the minimum granularity?
347. **Compensation components** — base salary, bonus, commission, benefits, payroll taxes, equity. Are all components modeled separately?
348. **Benefits burden rate** — employer payroll taxes + benefits as a percentage of base salary. Is this configurable by country/state?
349. **Annual merit increases** — can you model a 3% across-the-board raise effective March 1st? How does this flow through the monthly payroll forecast?
350. **Attrition modeling** — voluntary and involuntary attrition. Can you model different attrition rates by department or tenure band?
351. **Hiring plan** — new hires in future months. Does a hire in March cost a partial month in March? How is the start date handled in the payroll forecast?
352. **Termination costs** — severance, notice periods. Is this modeled as a separate cost line?
353. **Equity compensation (options, RSUs)** — vesting schedules, cliff vesting vs. graded vesting. Is the P&L expense calculated per ASC 718?
354. **Geographic compensation differences** — an engineer in San Francisco vs. Bangalore. Can you model compensation by location?
355. **Productivity metrics** — revenue per employee, cost per employee. Are these KPIs calculated and visualized?

---

## 🔴 SECTION 27: INDUSTRY ENGINES — TECHNICAL DEPTH

356. **SaaS: ARR calculation** — ARR = MRR × 12. But what's MRR? Contracted MRR? Billed MRR? GAAP-recognized MRR? These are different. Which does `SaaSMetricsEngine` use?
357. **SaaS: Expansion MRR** — upgrades, seat additions, price increases. Is expansion MRR tracked separately from new MRR?
358. **SaaS: LTV calculation** — LTV = (Average Revenue per Account × Gross Margin) / Churn Rate. Does your engine use this formula? What if churn rate is 0?
359. **SaaS: Cohort analysis** — cohort retention curves. Is this monthly cohorts? Annual? What's the maximum cohort history?
360. **Manufacturing: OEE (Overall Equipment Effectiveness)** — OEE = Availability × Performance × Quality. Each component requires specific data inputs. Where does this data come from?
361. **Manufacturing: BOM (Bill of Materials)** — multi-level BOM with sub-assemblies. Does the engine handle multi-level explosion for cost rollup?
362. **Manufacturing: Standard cost vs. actual cost variances** — material price variance, material usage variance, labor rate variance, labor efficiency variance. Are all four calculated?
363. **Banking: NIM (Net Interest Margin)** — NIM = (Interest Income - Interest Expense) / Average Earning Assets. What's the definition of "earning assets" in your engine?
364. **Banking: Loan loss reserve (CECL)** — ASC 326 replaced the incurred loss model with CECL (Current Expected Credit Loss). Is your `BankingEngine` CECL-compliant or still incurred loss?
365. **Banking: Capital adequacy (Basel III)** — Tier 1 capital, Tier 2 capital, RWA (Risk-Weighted Assets). Are the Basel III risk weights applied to each asset class?
366. **Healthcare: Value-based care** — VBC contracts have complex payment mechanics (shared savings, downside risk). Does `HealthcareEngine` model VBC payment structures?
367. **Healthcare: Patient revenue deductions** — contractual adjustments, charity care, bad debt. Net patient revenue is dramatically different from gross charges. Is this modeled?
368. **Real Estate: Cap rate calculation** — Cap Rate = NOI / Property Value. But NOI can be trailing 12 months or forward 12 months. Which does your engine use?
369. **Real Estate: DSCR (Debt Service Coverage Ratio)** — DSCR = NOI / Annual Debt Service. Is debt service from the `DebtScheduleEngine` or manually entered?
370. **Insurance: Loss triangle development** — chain-ladder method. How many development periods? What's the maximum triangle size? Is tail factor applied?
371. **Insurance: IBNR (Incurred But Not Reported)** — IBNR is a critical reserve. Is it calculated from the loss triangle or manually entered?
372. **Energy: Proved reserves classification** — SEC definitions of proved, probable, possible reserves (1P, 2P, 3P). Is your energy engine using SEC definitions?
373. **Energy: Emissions (Scope 1, 2, 3)** — Scope 3 has 15 categories. How many Scope 3 categories does `ESGEngine` support?
374. **Retail: Comparable store sales (comps)** — same-store sales growth is a key retail metric. Does the engine correctly exclude new and closed stores?
375. **Retail: Markdown optimization** — promotional pricing impact on margin. Does `RetailEngine` model markdown impact on GMROI?

---

## 🔴 SECTION 28: THE CUBE ENGINE — OLAP SPECIFICS

376. What **dimensions** does the CubeEngine support out of the box? Time, Entity, Account, Department, Cost Center — are these all standard dimensions?
377. **Dimension hierarchies** — Account has Parent Account → Account Group → Account. Can you query at any level and get rollup values?
378. **Sparse cube handling** — most real-world OLAP cubes are sparse (>90% empty cells). How does `CubeEngine` store sparse data? Hash map? Compressed array?
379. **Write-back** — can users type into a pivot table cell and have it write back to the cube? Is allocation (spreading) applied when writing to a parent?
380. **MDX queries** — does `MDXEngine` support the full MDX spec? Or a subset? What MDX functions are not supported?
381. **Calculated members** — in MDX, `WITH MEMBER [Revenue Growth] AS ([Revenue], [Current Year] - [Revenue], [Prior Year])`. Are calculated members supported?
382. **Named sets** — MDX named sets for reusable dimension filters. Supported?
383. **KPI definitions in the cube** — OLAP KPIs with goal, status, and trend. Are these standard OLAP KPI objects?
384. **Cube security at cell level** — can you restrict access to specific cells (not just dimensions)? E.g., hiding executive compensation from non-HR users?
385. **Cube partitioning strategy** — if you partition by time, what's the partition size? Monthly? Quarterly? What's the query plan when a query spans partition boundaries?
386. **Pre-aggregation** — `AggregationDesigner` designs aggregations. Which aggregations are created? Who decides? Is it automatic or manual?
387. **Cube snapshot** — `cubeStore` has `snapshots`. What triggers a snapshot? How large is a snapshot? How many are retained?
388. **Cube migration** — `CubeMigrationEngine` migrates flat data to cube. What flat data format does it accept? GL entries? Budget line items?
389. **OLAP vs. ROLAP vs. MOLAP** — is the cube stored in memory (MOLAP), in a relational structure (ROLAP), or hybrid (HOLAP)?
390. **Concurrent cube writes** — two users write to the cube simultaneously. Is there locking? Optimistic concurrency? Last-write-wins?

---

## 🔴 SECTION 29: REPORTING — PROFESSIONAL STANDARDS

391. **XBRL tagging** — SEC filers must submit XBRL-tagged financial statements. Does the report engine support XBRL output?
392. **iXBRL** — inline XBRL (HTML with embedded XBRL tags). Required for UK FCA filings. Supported?
393. **PDF/A compliance** — archival-quality PDF (PDF/A-1b or PDF/A-3b). Is jsPDF generating PDF/A compliant output? (It doesn't by default.)
394. **Report templates** — are templates truly WYSIWYG? Can a user position elements by dragging on a canvas? Or is it code/config based?
395. **Dynamic date ranges in reports** — "Current Quarter," "Last 12 Months," "Year-to-Date" as relative date references. Are these supported in saved reports that run automatically?
396. **Conditional formatting in reports** — if a variance is negative, show it in red in the exported PDF. Is this applied in the PDF output or only in the UI?
397. **Report subscriptions** — users can subscribe to receive reports by email. But this is an offline-first app. Where does the email come from? What SMTP server?
398. **Board pack narrative** — written commentary alongside charts and tables. Is there a rich text editor? Does it support embedded dynamic values (e.g., "Revenue grew [%]% vs prior year")?
399. **Report versions** — `ReportVersionEngine` — when does a new version get created? On every export? On demand? Is there a version comparison view?
400. **Watermark on draft reports** — can you add a "DRAFT" or "CONFIDENTIAL" watermark to PDFs? Is this supported in jsPDF?
401. **Table of contents** — board packs need auto-generated TOC with page numbers. Is this in `ReportBookEngine`?
402. **Report caching** — `ReportCacheEngine` — cache key is what? Report ID + date range + filters? What invalidates it?
403. **Multi-currency reports** — a report showing EUR subsidiary alongside USD parent. Can columns show both currencies simultaneously?
404. **Drill-down in exported reports** — interactive PDF drill-down is technically possible. Is any interactivity embedded in exported PDFs?
405. **Report access control** — can specific reports be restricted to specific roles? Who can see the CEO's compensation modeling report?

---

## 🔴 SECTION 30: WORKFLOW & APPROVAL — PROCESS QUESTIONS

406. **Approval chain** — sequential vs. parallel approvals. Can an approval require sign-off from both the CFO AND the Controller simultaneously?
407. **Delegation of authority** — if an approver is on vacation, can they delegate to another person temporarily?
408. **Approval thresholds** — "amounts over $1M require CFO approval, under $1M require Controller." Is threshold-based routing implemented?
409. **Escalation** — if an approval sits for 5 days without action, does it escalate? Who gets notified?
410. **Rejection with comments** — when an approver rejects, they must provide a reason. Is this enforced?
411. **Partial approval** — can an approver approve line items 1-5 and reject line items 6-10 in the same budget?
412. **Approval audit trail** — every approval action (submitted, approved, rejected, escalated) must be logged with timestamp and user. Is this in the `AuditLogEngine`?
413. **Budget submission workflow** — does submitting a budget for approval lock it from further editing? What if the owner finds an error after submission?
414. **Multiple approval workflows** — budget approval might be different from forecast approval which is different from journal approval. Are multiple workflow templates supported?
415. **SLA reporting** — how long does each approval step take? Is there a dashboard showing approval cycle times?

---

## 🔴 SECTION 31: DATA QUALITY & GOVERNANCE

416. `DataQualityEngine` — what specific checks does it run? Completeness? Accuracy? Consistency? Timeliness? Which of these are implemented?
417. **Duplicate detection** — if a user imports the same GL file twice, are duplicates detected and rejected?
418. **Referential integrity** — if a GL entry references Account #5000 but that account doesn't exist in the Chart of Accounts, is this flagged?
419. **Period balance validation** — total debits must equal total credits for each period. Is this enforced on import?
420. **Sign convention consistency** — in accounting, assets are positive, contra-assets are negative. Revenue has a credit balance (negative in the typical trial balance). How does `SignConventionEngine` handle this?
421. **Data lineage** — `DataLineageEngine` tracks where data came from. How far back does it track? Import → Transformation → Calculation → Report?
422. **Data catalog** — `DataCatalogEngine` — what metadata is stored? Business definition, data type, owner, sensitivity classification, update frequency?
423. **Master data management** — `MasterDataEngine` — what is "master data" in this context? Chart of accounts? Entity list? Cost centers? Employees?
424. **Golden record** — when the same entity exists in multiple systems (QuickBooks + Xero + manual entry), how is the golden record determined?
425. **Data stewardship** — who is the data steward for each data domain? Is this role assigned in the system?

---

## 🔴 SECTION 32: IMPORT/EXPORT — EDGE CASES

426. **Excel import with merged cells** — a typical finance Excel file has merged cells for headers. How does `ExcelImportEngine` handle merged cells?
427. **Excel import with multiple sheets** — a workbook with 12 monthly sheets. Can the engine combine them automatically?
428. **Excel with formulas** — cells that contain `=SUM(B2:B10)` — does the import read the calculated value or the formula string?
429. **CSV with embedded commas** — a description field containing "Revenue, Net of Returns" breaks naive CSV parsing. Does your CSV parser handle RFC 4180 correctly?
430. **UTF-8 BOM** — Excel often adds a BOM (byte order mark) to CSV files. Does your CSV parser handle this?
431. **Large file streaming** — `StreamImportEngine` handles large files. What's the streaming chunk size? What's the maximum file size tested?
432. **Encoding detection** — a file might be UTF-8, UTF-16, Windows-1252, or Latin-1. How is encoding detected automatically?
433. **Date parsing ambiguity** — `01/02/2024` — is this January 2nd or February 1st? How is this resolved during import?
434. **Number format during import** — `1,234.56` (US) vs `1.234,56` (European). Is the locale of the source file detected?
435. **Negative number formats** — `(1,234.56)` vs `-1,234.56` vs `1,234.56-`. All three represent negative numbers. Are all formats recognized?
436. **Excel export fidelity** — if a user exports a budget to Excel and re-imports it, does it come back identical? Round-trip fidelity?
437. **PDF export on mobile browsers** — jsPDF works differently on mobile. Is the app even intended for mobile use?
438. **Excel export with charts** — can charts be embedded in Excel export? ExcelJS supports this but it's complex.
439. **CSV export encoding** — does CSV export include BOM for Excel compatibility? Is the delimiter configurable (comma vs semicolon for European locales)?
440. **Export with filters applied** — if a user has filtered the grid to show only Department A, does the export show filtered data or all data?

---

## 🔴 SECTION 33: HOOKS — ALL 30 QUESTIONED

441. You have 30 custom hooks. Name them all. What does each one do that couldn't be done inline?
442. `useFocusManagement` — how does it know which element should receive focus after a modal closes? Is focus state tracked globally?
443. `useAnnounce` — live region announcements. What's the politeness level (polite vs assertive)? Who decides?
444. `useReducedMotion` — does disabling motion affect ALL Framer Motion animations? Or just ones explicitly wrapped with this hook?
445. `useOffline` — beyond `navigator.onLine`, does it do a health check ping? What endpoint? What if the ping fails but the connection is fine (firewall blocking)?
446. `useTauriMenu` — how does this hook rebuild native menus when the route changes? Is this expensive?
447. What hook manages the `CommandPalette` state? Is it a custom hook or just Zustand `uiStore`?
448. Is there a `usePermission` hook that checks RBAC before rendering? Or is permission checking done inline in each component?
449. Is there a `useEngine` hook that handles engine loading via `EngineRegistry`? Or is loading done ad-hoc?
450. Is there a `useFormula` hook for formula evaluation, or do components call `FormulaEngine` directly?

---

## 🔴 SECTION 34: UTILITIES — INTERROGATED

451. `masterStorage.ts` — what happens if IndexedDB is full (storage quota exceeded)? Safari has a 1GB limit. Chrome has a dynamic limit based on disk space.
452. `offlineCache.ts` — what's the cache invalidation strategy? Time-based? Version-based? Manual clear?
453. `backupRestore.ts` — the backup is JSON of all store data. How large is this JSON for a company with 5 years of data? Can it be split into chunks?
454. `memoization.ts` — what memoization utilities? `memo`, `useMemo` wrappers? Or a standalone function cache?
455. `validation.ts` — what validations beyond Zod schema validation? Business rule validation?
456. `encryption.ts` — what key derivation function? PBKDF2? Argon2? What's the iteration count?
457. `security.ts` — what specific utilities? XSS sanitization? CSRF token generation?
458. `performanceBudget.ts` — what are the performance budget thresholds? LCP? FID? CLS? What happens when a budget is exceeded?
459. `bundleAnalyzer.ts` — is this used at build time or runtime? How does it surface results to developers?
460. `tokenRotation.ts` — token rotation logic in the frontend. Is there a server endpoint that issues new tokens? Or is this mocking a server response?

---

## 🔴 SECTION 35: THE MOCK DATA PROBLEM

461. `mockData/accounts.ts` — how many accounts are mocked? A realistic chart of accounts has 200-2000 accounts. What's the mock count?
462. `mockData/glData.ts` — how many GL entries are mocked? A realistic mid-size company has 100,000-1,000,000 entries per year. What's the mock count?
463. `mockData/exchangeRates.ts` — which currencies are mocked? Are the rates realistic for a specific date?
464. `mockData/generators.ts` — are there data generators that create realistic random financial data? Or is the mock data static?
465. In production, how does the app get real data if there's no backend? Only through the QuickBooks/Xero connectors and file imports?
466. Are the mock data files tree-shaken out of production builds? Or are they included?
467. The dashboard KPIs on first launch (before any data is imported) — what do they show? Mock data? Zeros? Empty state?
468. Is there a "demo mode" using mock data for sales demos and onboarding? Is this separate from the mock data files?
469. `mockData/collaboration.ts` — collaboration requires multiple users. How does mock collaboration data work in a single-user local app?
470. `mockData/notifications.ts` — notification triggers are events. How do mock notifications get triggered? On a timer?

---

## 🔴 SECTION 36: DEVELOPER EXPERIENCE

471. A new developer joins the team. How long until they can make a meaningful contribution? What's the onboarding documentation beyond this architecture doc?
472. With 1,190 files, how does a developer find the right file for a bug? Is there a naming convention enforcement?
473. Are there **ESLint rules** specific to this project? E.g., "don't import engines directly in components, go through stores"?
474. **Barrel exports** — `engines/index.ts` exports 35 engines. Does this barrel export prevent tree-shaking of unused engines?
475. **Circular imports** — with 1,190 files and complex dependencies, are there circular imports? Does TypeScript catch them? Does Vite throw?
476. The `@/` path alias — is this consistently used? Are there any relative import paths that should be aliases?
477. **TypeScript strict mode** — all 1,190 files in strict mode. Are there any `// @ts-ignore` or `as any` casts?
478. **Prettier + ESLint** — are they configured to agree? Any formatting conflicts that cause constant merge conflicts?
479. **Git hooks** — pre-commit hooks running lint/format? Pre-push running tests? Is this enforced or optional?
480. **VSCode workspace settings** — is there a `.vscode/settings.json` with recommended extensions and formatting settings?

---

## 🔴 SECTION 37: OPERATIONAL CONCERNS

481. **Error logging** — when an engine throws an error in production, where does it go? Browser console? A logging service? Is there Sentry integration?
482. **Analytics** — is user behavior tracked? Which pages are used most? Which engines are slowest? Product analytics?
483. **Feature flags** — can features be toggled without a code deploy? Is there a feature flag system?
484. **A/B testing** — can different users see different UI variants? Is this architecture even compatible with A/B testing given the offline-first nature?
485. **Versioning strategy** — semantic versioning? Calendar versioning? How are breaking changes communicated to users?
486. **Changelog** — is there a user-facing changelog? Does the app notify users of new features?
487. **Support model** — when a user's data is corrupted, how does support help them? They can't access the user's IndexedDB.
488. **Telemetry opt-in/out** — is any usage data sent anywhere? Is there an opt-out? GDPR requires explicit consent.
489. **Crash reporting** — `CrashRecoveryEngine` recovers from crashes. But are crashes reported to the development team? How?
490. **Health check** — `SystemHealthMonitor` — what does it show in the UI? A status page? Warning banners?

---

## 🔴 SECTION 38: FINANCIAL MODELING EDGE CASES

491. **Negative revenue** — can revenue be negative? (Returns, refunds, credit notes). Does the UI handle negative values in revenue rows correctly?
492. **100%+ growth rates** — a startup might show 500% YoY growth. Does the growth rate display handle extreme values without breaking chart scales?
493. **Zero revenue periods** — a seasonal business with zero revenue in certain months. Does division by zero in growth rate calculations crash anything?
494. **Penny rounding** — `$0.001` rounding differences accumulate. Does the system have a configurable rounding precision?
495. **Currency precision** — Japanese Yen has no decimal places. Bitcoin has 8. Does currency formatting respect the currency's native precision?
496. **Negative equity** — a company with accumulated losses can have negative total equity. Does the Balance Sheet display handle this?
497. **Intercompany profit in inventory (unrealized)** — if Entity A sells to Entity B and B hasn't sold externally yet, the consolidated inventory must be reduced. Tested?
498. **Tax loss carryforward** — NOLs (Net Operating Losses) can be carried forward to offset future taxable income. Does `TaxEngine` model NOL carryforwards?
499. **Foreign tax credits** — taxes paid in foreign jurisdictions can offset US tax liability. Does `TaxEngine` model FTCs?
500. **Transfer pricing adjustments** — `TransferPricingPage` exists. What methods are supported? CUP? Cost plus? TNMM? Profit split?
501. **Functional currency determination** — ASC 830 requires determining the functional currency of each entity. Is this a configuration option per entity?
502. **Highly inflationary economies** — ASC 830 has special rules for subsidiaries in highly inflationary economies (>100% cumulative inflation over 3 years). Is this handled?
503. **Hyperinflation adjustment (IAS 29)** — IFRS requires restating financials for hyperinflationary economy subsidiaries. Is IAS 29 in scope?
504. **Going concern** — if KPIs indicate financial distress (negative cash flow, debt covenant violations), does the system flag going concern risk?
505. **Debt covenant calculations** — many loan agreements have financial covenants (Debt/EBITDA < 3x, Interest Coverage > 2x). Are these automatically calculated and monitored?

---

## 🔴 SECTION 39: THE COMPETITION

506. How does FinPlan Pro compare to **Anaplan** specifically? Anaplan's hyperblock model handles connected planning differently. What's your answer when a prospect says they're evaluating Anaplan?
507. **Adaptive Insights (Workday)** — enterprise SaaS, unlimited users, admin-managed. Your offline desktop model is the opposite. Why would an Adaptive Insights customer switch?
508. **Vena Solutions** — Excel-in-the-browser approach that meets finance teams where they are. Your app requires learning a new tool. How do you compete?
509. **Planful** — strong in consolidation and reporting. Your consolidation module competes directly. What's your differentiation?
510. **Board (software)** — unified BI + planning. Your analytics and planning in one app is similar. But Board has 25 years of market presence. How?
511. **Power BI + Excel** — Microsoft's free/cheap tools that finance teams already know. Every feature you have can be approximated in this stack. Why pay for FinPlan Pro?
512. **Mosaic Tech** — modern FP&A for startups, strong QuickBooks/Xero integration. You also have QuickBooks/Xero. What's your SaaS/startup story vs. Mosaic?
513. **Cube (software)** — spreadsheet-native FP&A. Claims no ramp-up time because it works in Excel/Sheets. You require learning your own spreadsheet interface. Response?
514. **Why not just build on top of Excel?** — Add-ins (like Datarails or Kepion) enhance existing Excel. Finance teams love Excel. Why build a new tool?
515. **Open source competition** — are there open source FP&A tools? Could someone fork this and offer it free?

---

## 🔴 SECTION 40: EXISTENTIAL QUESTIONS

516. Is this project **already obsolete**? LLMs can now build financial models from natural language. In 2 years, will finance teams just talk to Claude/GPT to generate P&L forecasts?
517. With **AI writing 90% of code**, how do you know the engines are mathematically correct? Did a human verify every calculation?
518. The document mentions `@huggingface/transformers` for "on-device AI." But truly useful financial AI (GPT-4 level) doesn't run on-device. What can your on-device AI actually do well?
519. The app is offline-first. But **collaboration requires connectivity**. **AI requires connectivity** (for useful models). **Real-time data requires connectivity**. Is "offline-first" a genuine architectural requirement or an arbitrary constraint?
520. Who **owns the financial data** stored in IndexedDB? The user? The company? If an employee leaves, can they take all the company's financial models with them on their laptop?
521. **Data portability** — can a user export everything and move to a competitor? What format? Is there a standard?
522. If this project were to be **acquired**, what's the most valuable part? The 174 engines? The 15 sector configs? The customer data (in IndexedDB, so no)? The codebase?
523. **Build vs buy** — a company could build a custom FP&A tool for their specific needs. Why would they use FinPlan Pro instead?
524. At what **company size** does this tool become impractical? 10 users? 100 users? 1,000 users? The offline-first model doesn't scale to large teams.
525. If you had to bet on which of the 174 engines is **actually used by 80% of users** and which ones are used by less than 1%, what would you guess — and why did you build the 1% ones?
526. The app has 8 languages but is offline-first. If a Japanese user needs Japanese support, who do they call? There's no server-side support infrastructure.
527. What's the **minimum viable version** of this that could make money? Not 174 engines and 140 pages. What's the core?
528. Is there a risk that the **complexity of the codebase** (1,190 files, 174 engines) makes it impossible to maintain without a large team?
529. Five years from now, will **React, Zustand, and Vite** still be the right choices? What's the migration path if not?
530. The ultimate question: **Is this a product or a portfolio piece?** And is the answer to that question reflected honestly in every architectural decision made?

# 500 MORE BRUTAL QUESTIONS — THE DEEPEST CUT YET

---

## 🔴 SECTION 41: RENDERING & REACT 19 SPECIFICS

531. You're on **React 19** — are you using `useTransition` anywhere to keep the UI responsive during heavy state updates like budget recalculations?
532. `useDeferredValue` — when a user types a formula and the engine recalculates thousands of cells, is the recalculation deferred? Or does it block the input?
533. React 19 has `use()` for reading promises and context. Are you using it? Or is this just a regular React 18-style codebase running on React 19?
534. `Suspense` boundaries — the document shows `<Suspense fallback={<LoadingScreen />}>` at the router level. Are there nested Suspense boundaries for individual components, or does the entire page flash to a loading screen on any lazy component load?
535. React 19 server components — are these in use? This is a client-side app, so server components make no sense. But are you inadvertently importing server-only patterns?
536. `React.memo` usage — with 150+ components, how many are memoized? Which ones SHOULD be memoized but aren't? What's the re-render analysis story?
537. `useCallback` and `useMemo` usage — are these used appropriately, or is there either over-memoization (wasted memory) or under-memoization (wasted renders)?
538. `useRef` — is it used for any non-DOM purposes? Storing mutable values that don't trigger re-renders? What mutable values exist outside Zustand?
539. **Key prop** — in lists of budgets, forecasts, scenarios — are stable keys used? Or is `index` used as a key (a common anti-pattern that breaks animations and state)?
540. **Context usage** — besides `ThemeContext`, are there other contexts? Context re-renders ALL consumers when the value changes. How many consumers does `ThemeContext` have?
541. **StrictMode** — is React's `<StrictMode>` enabled in development? It double-invokes effects. Does your app handle double-invocation gracefully?
542. **Error boundary recovery** — when an error boundary catches an error, can the user click "Try Again"? Or is the entire page permanently broken until refresh?
543. **Component unmounting cleanup** — when a page unmounts, are all subscriptions (Zustand, WebSocket, timers, intervals) cleaned up? Or do they leak?
544. **DOM manipulation** — is there any direct DOM manipulation outside React (`document.getElementById`, `querySelector`)? This breaks React's mental model.
545. **Portal usage** — modals, tooltips, dropdowns — are these rendered via React portals? Or are they nested inside parent components and clipped by `overflow: hidden`?

---

## 🔴 SECTION 42: CSS & DESIGN SYSTEM — DEEPEST LEVEL

546. **Tailwind v4** eliminates `tailwind.config.js` and uses CSS-first configuration. How is your design token system (`designTokens.ts`) connected to Tailwind v4's new `@theme` directive?
547. Tailwind v4 has **native CSS layers** (`@layer base, components, utilities`). Are your styles organized into these layers? Is specificity controlled?
548. **Custom colors** — the app needs brand colors, semantic colors (success, warning, error), and financial colors (positive green, negative red). How many custom colors are defined?
549. **Dark mode implementation** — Tailwind v4 uses `@media (prefers-color-scheme: dark)` by default. Is dark mode class-based (`dark:` prefix) or media-query-based? Can users toggle?
550. **Financial red/green** — positive values green, negative values red. But approximately 8% of males are red-green colorblind. Is there an alternative encoding (icons, up/down arrows)?
551. **Typography scale** — what's the typographic hierarchy? How many font sizes? Is there a monospace font for numbers? Financial apps need tabular figures (monospaced digits).
552. **Spacing scale** — is there a consistent spacing scale (4px base? 8px base)? How many spacing values are used? Are there one-off arbitrary values?
553. **Z-index management** — what's the z-index scale? Sidebar, navbar, modal, dropdown, tooltip, toast — are z-indices managed in a central registry or ad-hoc?
554. **Border radius** — consistent across the app? Cards, buttons, inputs, badges — are they all the same radius?
555. **Shadow system** — how many shadow levels? Are shadows different in dark mode?
556. **Container widths** — what's the max content width? Is the dashboard full-width? Are forms constrained?
557. **Responsive design** — are there mobile breakpoints? Is this app usable on a 768px tablet? 375px phone? Or is it desktop-only (1280px+)?
558. **Print stylesheet** (`print.css`) — what elements are hidden in print? How does a budget grid look when printed? Does it paginate correctly?
559. **CSS custom properties** — are design tokens exposed as CSS custom properties for use in components that need dynamic values (e.g., chart colors)?
560. **Class name conflicts** — `tailwind-merge` resolves conflicts between Tailwind classes. But what about conflicts between Tailwind and custom CSS in `index.css`?
561. **Global styles in `index.css`** — how many global styles? Are they documented? Do they affect components unexpectedly?
562. **Scrollbar styling** — custom scrollbars in webkit? Firefox? Does AG Grid's scrollbar match the app's scrollbar style?
563. **Selection color** — when a user selects text, what color is the highlight? Is it consistent across themes?
564. **Focus ring styling** — `:focus-visible` ring color and width. Is it consistent across all interactive elements?
565. **Disabled state styling** — are disabled buttons, inputs, and links styled consistently with reduced opacity or a specific disabled color?
566. **Loading state styling** — skeleton screens, spinners, progress bars. Are these consistent across all pages or does each page invent its own?
567. **Empty state styling** — when a table has no data, what's shown? A message? An illustration? A CTA button?
568. **Error state styling** — when an API call fails, what's shown? An error banner? A full-page error? A toast?
569. **Success state styling** — when a budget is saved, when an import completes — is there a success indication? How long does it persist?
570. **Hover states** — every interactive element has a hover state? Consistently applied? Tested with keyboard focus as well?

---

## 🔴 SECTION 43: AG GRID — DETAILED INTERROGATION

571. **AG Grid license** — AG Grid Community is AGPL. AG Grid Enterprise requires a commercial license. Which features are you using? Do you have a license?
572. AG Grid **column definitions** — how are they defined for the budget grid? Are they static or dynamically generated based on the budget's periods?
573. AG Grid **row model** — which row model? Client-side? Server-side? Infinite? Viewport? Virtual pagination?
574. AG Grid **cell editing** — is it full-row editing (like a database) or single-cell editing (like Excel)? Can you tab between cells?
575. AG Grid **cell renderers** — are custom cell renderers used for currency formatting, percentage bars, sparklines in cells?
576. AG Grid **cell editors** — are custom cell editors used? A date picker for date cells? A dropdown for account selection?
577. AG Grid **sorting** — can users sort by any column? Is multi-column sorting supported? How does sorting interact with calculated/total rows?
578. AG Grid **filtering** — what filter types? Text, number, date, set? Are financial-specific filters supported (e.g., "amount > $10,000")?
579. AG Grid **grouping** — can rows be grouped by department, cost center, account type? Is grouping done client-side or server-side?
580. AG Grid **tree data** — for chart of accounts (hierarchical). Does AG Grid's tree data mode handle account hierarchies?
581. AG Grid **pivoting** — can the grid pivot data (rows become columns)? Is this used for period-over-period analysis?
582. AG Grid **clipboard** — can users Ctrl+C from the grid and paste into Excel? Can they paste from Excel into the grid?
583. AG Grid **undo/redo** — AG Grid has built-in undo/redo. Your app also has `UndoRedoEngine`. Are these the same? Coordinated? Conflicting?
584. AG Grid **selection** — single row? Multi-row? Range selection (click and drag)? How does selection interact with keyboard shortcuts?
585. AG Grid **context menu** — right-click menu on cells. What options? Copy, paste, format, comment, history? Is this custom or AG Grid default?
586. AG Grid **row dragging** — can users reorder rows by dragging? Does this affect the underlying data order?
587. AG Grid **column pinning** — can users pin columns to the left or right? Essential for wide spreadsheets.
588. AG Grid **column resizing** — manual column resize? Auto-size to content? Double-click to fit?
589. AG Grid **column reordering** — can users drag columns to reorder them?
590. AG Grid **column groups** — "Q1 2024" with sub-columns "Jan, Feb, Mar". Are column groups used for period display?
591. AG Grid **row height** — fixed or auto? Can rows expand to show wrapped text?
592. AG Grid **header component** — custom header with sort indicator, filter icon, column menu?
593. AG Grid **footer rows** — summary/footer rows showing totals? Configurable aggregations (sum, average, count)?
594. AG Grid **master-detail** — can a row expand to show detail data (e.g., budget line item expanding to show monthly breakdown)?
595. AG Grid **integrated charts** — AG Grid Enterprise can create charts from grid data. Is this used or are charts handled by Recharts separately?
596. AG Grid **server-side row model** — if data is huge, is the server-side model used? But there's no server in this offline-first app. Contradiction?
597. AG Grid **viewport row model** — renders only visible rows. Is this used for the GL explorer with millions of entries?
598. AG Grid **custom overlay** — "loading" and "no rows" overlays. Are these customized or stock AG Grid overlays?
599. AG Grid **theme** — which AG Grid theme? Alpine? Balham? Material? Quartz? Or a fully custom theme?
600. AG Grid **performance with formulas** — when a formula cell changes and 1,000 dependent cells update, how does the grid handle the mass cell refresh? Batch update? `applyTransaction`?

---

## 🔴 SECTION 44: RECHARTS — VISUALIZATION DEEP DIVE

601. **Recharts performance** — Recharts re-renders the entire SVG chart on any prop change. With 10+ charts on the dashboard, what's the render time?
602. **Chart types** — which Recharts chart types are used? Line, Bar, Area, Pie, Radar, Scatter, Composed, Treemap, Funnel, Sankey?
603. **Recharts does NOT natively support** waterfall charts, gauge charts, Gantt charts, calendar heatmaps, box plots, or Sankey diagrams. These are listed in your components. How are they implemented?
604. **Waterfall chart** — custom SVG? A library wrapper? What library handles waterfall charts?
605. **Gauge chart** — custom SVG? Radial gauge for KPIs? What library?
606. **Gantt chart** — for project timelines. Custom component? What library handles this?
607. **Calendar heatmap** — like GitHub's contribution graph. Custom? What library?
608. **Box plot chart** — statistical box-and-whisker. Custom? What library?
609. **Sankey diagram** — flow visualization (revenue streams). Recharts doesn't have Sankey natively. How?
610. **Treemap chart** — Recharts has Treemap. Is it used for budget allocation visualization?
611. **Chart responsiveness** — do charts resize when the window resizes? Is `ResponsiveContainer` from Recharts used?
612. **Chart accessibility** — can a screen reader interpret a chart? Are there `aria-label` descriptions for each chart?
613. **Chart tooltips** — custom tooltip components showing exact values, percentages, comparisons?
614. **Chart legends** — interactive legends? Click to toggle series on/off?
615. **Chart zoom** — can users zoom into a time period on a line chart? Brush selection?
616. **Chart annotations** — `ChartAnnotationEngine` exists. Can users add text annotations, lines, or markers to charts?
617. **Chart export** — can a single chart be exported as PNG/SVG/PDF independently of the page?
618. **Chart color palette** — what are the chart colors? Are they accessible (colorblind safe)?
619. **Combo charts** — bar + line on the same axes (e.g., revenue bars + margin line). Is `ComposedChart` from Recharts used?
620. **Chart data density** — a line chart with 10,000 data points (daily data for 27 years). Does Recharts handle this without browser freeze?

---

## 🔴 SECTION 45: WEB WORKERS — COMMUNICATION & LIFECYCLE

621. **WorkerPool** manages 2-4 workers. How is work distributed? Round-robin? Least-busy? Queue-based?
622. **Worker communication** — `postMessage`/`onmessage`? Or `BroadcastChannel`? Or a structured RPC protocol?
623. **Message serialization** — when sending 1M GL entries to a worker for consolidation, the data is cloned via structured clone algorithm. How much memory does this consume (original + clone)?
624. **Transferable objects** — `ArrayBuffer` can be transferred (zero-copy) to workers. Are transferable objects used for large datasets?
625. **Worker termination** — if a worker crashes or takes too long, is it terminated? What's the timeout? How is the user notified?
626. **Worker error handling** — `onerror` event on workers. Is this caught? Does it trigger a retry? An error boundary?
627. **Worker chunk loading** — Vite bundles workers separately. Are worker bundles pre-fetched or loaded on demand?
628. **Formula worker** — `formulaWorker.ts`. How many cells can it evaluate per second? What's the benchmark?
629. **Monte Carlo worker** — how many iterations per second? For 10,000 iterations with 50 variables, what's the wall clock time?
630. **Export worker** — generating a PDF with 50 pages. Is this blocking the UI or truly in a worker? jsPDF might not be worker-compatible (DOM dependency).
631. **Scenario worker** — what calculations happen in the scenario worker vs. the main thread? Is the split clean?
632. **Consolidation worker** — with 100 entities, each with currency translation and IC elimination. What's the elapsed time? Benchmarked?
633. **Worker memory limit** — workers share the same memory budget as the main thread. With 4 workers each holding 100MB of financial data, you're at 500MB+. Is this sustainable?
634. **Worker initialization time** — cold-starting a worker takes time. Is the worker pool pre-warmed at app startup?
635. **Worker state management** — workers are stateless functions. Or do they maintain state between calls? If stateful, how is consistency ensured?

---

## 🔴 SECTION 46: WEBGPU/WASM & ON-DEVICE AI

636. `@huggingface/transformers` on WebGPU — **what's the model**? BERT? GPT-2? DistilBERT? T5? The model name matters enormously for capability.
637. **Model size** — is this a 10MB model or a 500MB model? The download and memory requirements are critical.
638. **WebGPU support** — Safari doesn't fully support WebGPU yet. What's the fallback? WASM with SIMD? Pure JS (extremely slow)?
639. **GPU detection** — does the app detect GPU capability before loading the model? What happens on integrated graphics (Intel UHD) vs discrete (NVIDIA RTX)?
640. **Model loading UX** — a 100MB model download could take minutes. What's the progress indication? Can the user cancel?
641. **Inference latency** — for anomaly detection, how long does one inference take? 10ms? 10 seconds? If it's slow, the UX is broken.
642. **What tasks does the AI actually perform?** Anomaly detection — what constitutes an anomaly in financial data? What's the model's accuracy?
643. **Auto-commentary** — "Revenue increased 12% due to strong SaaS growth." Is this genuinely AI-generated or template-based text with variable substitution?
644. **NLQ (Natural Language Query)** — "Show me Q3 revenue by region." Does this actually work? What NLP pipeline parses the query and generates the correct filter/aggregation?
645. **Model updates** — ML models improve over time. How are model weights updated? App update? Online download? Version pinned?
646. **Privacy** — financial data is fed into an AI model. Is the data sent to any server? Or is inference purely on-device?
647. **Model correctness for finance** — a general-purpose language model has no understanding of accounting, GAAP, or financial terminology. How can it generate meaningful financial commentary?
648. **Hallucination risk** — AI-generated commentary might be factually wrong. What guardrails prevent the system from displaying incorrect financial analysis?
649. **User trust** — will a CFO trust AI-generated anomaly detection? What's the false positive rate that would make this useful vs. annoying?
650. **Regulatory risk** — if an AI-generated insight leads to a material misstatement in a financial report, who is liable?

---

## 🔴 SECTION 47: ROUTING — NERVOUS SYSTEM

651. **80+ routes** — is there a route definition file? Or are routes scattered across `App.tsx`? How do you prevent route conflicts?
652. **Dynamic route segments** — `/budgets/:id` — what validates the `:id` parameter? What if it's not a valid UUID? Does it 404 gracefully?
653. **Nested routes** — are any routes nested with shared layouts? Or is every route a flat page component?
654. **Route guards** — `ProtectedRoute` component. How does it work? Redirect to `/login` if no token? What if the token is expired but not yet refreshed?
655. **Role-based route access** — can a Viewer navigate to `/budgets/create`? Is the route blocked at the router level, the component level, or both?
656. **Route preloading** — `routePreloader.ts`. Which routes are preloaded? Based on what heuristic? The sidebar navigation order?
657. **404 handling** — `NotFoundPage` at `*`. Does it show useful navigation options or just "Page not found"?
658. **Deep linking** — if a user bookmarks `/budgets/abc123/bva` and the budget is deleted, what happens?
659. **Route transitions** — when navigating between pages, is there a transition animation? A loading skeleton? Or a jarring full-page swap?
660. **Browser back/forward** — does the browser back button work correctly with all 80+ routes? What about state preservation (scroll position, filter state)?
661. **URL state** — are filters, sort orders, date ranges reflected in the URL? Can a user share a URL that reproduces their exact view?
662. **Hash routes vs history routes** — are you using `BrowserRouter` or `HashRouter`? HashRouter is more compatible but less clean.
663. **Route-level code splitting** — each route lazy-loads its page. But does it also lazy-load the page's specific dependencies?
664. **Route error boundaries** — if a page component throws during render, does the error boundary show an error for that page or crash the entire app?
665. **Redirect chains** — if a logged-in user visits `/login`, are they redirected to `/`? If `/dashboard` redirects to `/`, is there a redirect loop risk?

---

## 🔴 SECTION 48: AUTHENTICATION & SESSION — ATTACK SURFACE

666. **JWT structure** — what claims are in the JWT? `sub`, `exp`, `iat`, `roles`, `entityId`? What's the token size?
667. **Token storage** — you say "in memory." Does this mean the token is lost on page refresh? How does the user stay logged in across refreshes?
668. **Refresh token** — is there a refresh token? Where is it stored? If in a cookie, is it `HttpOnly`, `Secure`, `SameSite`?
669. **Token refresh race condition** — multiple concurrent API calls discover an expired token simultaneously. Do they all try to refresh? Is there a refresh queue?
670. **MFA setup flow** — how does a user set up MFA? QR code for TOTP? Is the MFA secret stored on the server? (But there's no server.)
671. **MFA verification flow** — when MFA is required, what's the UX? A modal asking for the TOTP code? What if the code is wrong?
672. **Password hashing** — if there's no backend, how are passwords stored? They aren't? Is this just a mock auth system?
673. **Login flow** — does the login form actually call an API? Or does it compare against mock credentials? What endpoint?
674. **Registration flow** — can a new user register? Where is their account created?
675. **Password reset flow** — without a server, how does password reset work? Via email? How is the email sent?
676. **Session timeout** — is there an idle timeout? After 30 minutes of inactivity, does the session expire?
677. **Idle detection** — how is idle time measured? Mouse movement? Keyboard activity? What if the user is reading a report (no input)?
678. **Concurrent sessions** — can a user be logged in on two devices simultaneously? Is this detected and handled?
679. **Logout** — what happens on logout? Are all stores cleared? Is IndexedDB wiped? Are WebSocket connections closed?
680. **Account lockout** — after 5 failed login attempts, is the account locked? For how long?

---

## 🔴 SECTION 49: PERSISTENCE — FAILURE MODES

681. **IndexedDB transaction model** — IndexedDB uses transactions. A store persist write is a transaction. What happens if another tab has the same store open and a write conflict occurs?
682. **IndexedDB version upgrades** — when the app schema changes, IndexedDB's `onupgradeneeded` fires. What if the upgrade fails? Is the old data preserved?
683. **IndexedDB quota** — Safari limits IndexedDB to 1GB total. Chrome limits to ~80% of disk. What happens when the quota is exceeded? Error handling?
684. **IndexedDB corruption** — IndexedDB can become corrupted (rare but real). Is there a repair/recovery mechanism?
685. **SQLite corruption** — on desktop, SQLite can corrupt on power failure. Is WAL mode enabled? Are backups automatic?
686. **masterStorage async race condition** — `getItem` is async. Two components call `getItem` simultaneously. Could they get different versions of the data?
687. **Hydration mismatch** — the app starts, renders with default state (empty arrays), then hydrates from IndexedDB (populated arrays). Is there a flash of empty content?
688. **Partial write** — a persist middleware write fails halfway (tab crashes). Does Zustand's persist middleware handle partial writes? Is the data left in a consistent state?
689. **Storage key collision** — 24 stores, each with a unique storage key. What if two stores accidentally share a key? Is there validation?
690. **Migration on version update** — the app updates from v1.0 to v2.0. The budget schema changes. Is the migration applied automatically? Can it be undone?
691. **Backup frequency** — `backupRestore.ts` — how often should backups be created? Is there auto-backup? Or is it manual only?
692. **Backup size** — a full backup of all stores. What's the size for a medium-sized company's data? 10MB? 100MB? 1GB?
693. **Restore fidelity** — after a restore, is the app guaranteed to work correctly? Are store dependencies re-established?
694. **Selective restore** — can a user restore just the budget data without affecting the GL data? Or is it all-or-nothing?
695. **Data export for compliance** — GDPR right to data portability. Can a user export all their data in a structured format? Is this implemented?

---

## 🔴 SECTION 50: TOAST, MODAL, & OVERLAY SYSTEMS

696. **Toast notifications** — how many variants? Success, error, warning, info? Are they auto-dismissed? After how long?
697. **Toast stacking** — if 5 toasts appear simultaneously, do they stack vertically? Where? Top-right? Bottom-right? Center?
698. **Toast accessibility** — are toasts announced by screen readers? What ARIA role? `role="alert"` or `role="status"`?
699. **Modal stacking** — can modals open on top of modals? (e.g., a confirmation dialog on top of a form dialog). What's the z-index management?
700. **Modal close behavior** — clicking outside the modal closes it? Always? Or only for non-critical modals? What about unsaved changes warning?
701. **Modal keyboard behavior** — Escape closes modal? What if the modal has an inner dropdown open? Does Escape close the dropdown first?
702. **Focus trap** — when a modal opens, focus must be trapped inside. Is `FocusTrap` component used? Does it handle edge cases (tab wrapping, shift+tab)?
703. **Body scroll lock** — when a modal is open, is the background scroll locked? iOS Safari has notorious issues with this.
704. **Dialog sizes** — how many dialog sizes? Small, medium, large, full-screen? Is there a responsive behavior for small screens?
705. **Confirmation dialogs** — destructive actions (delete budget, clear GL data). Are confirmation dialogs mandatory? Or can the user bypass them?
706. **Dropdown menus** — in the sidebar, navbar, context menus. How many dropdown variants? Are they Radix UI DropdownMenu?
707. **Dropdown positioning** — Radix handles positioning, but what about dropdowns that would overflow the viewport? Does `flip` and `shift` middleware handle this?
708. **Tooltip system** — are tooltips on delay? Immediate? Positioned above or below? Accessible (not just hover-based)?
709. **Popover system** — filter panels, date pickers, color pickers — are these popovers? Radix Popover?
710. **Command palette** — is it a Radix Dialog? What does the search algorithm use? Fuzzy matching? Prefix matching?

---

## 🔴 SECTION 51: FORM SYSTEM — EXHAUSTIVE

711. **React Hook Form** — are all forms using `useForm`? Or are some using uncontrolled components?
712. **Zod schemas** — are all forms validated with Zod? Is there a single schema per form? Or are schemas shared across forms?
713. **Form state persistence** — if a user fills a 20-field form and navigates away accidentally, is the form state preserved?
714. **Dirty state tracking** — does the app warn users about unsaved changes when navigating away from a dirty form?
715. **Form submission** — on submit, is the form disabled (preventing double submit)? Is there a loading indicator?
716. **Server-side validation** — with no server, there's no server-side validation. Is all validation client-side only? Can it be bypassed?
717. **Dynamic forms** — can form fields change based on sector selection? Does changing sector from "Banking" to "Manufacturing" change available form fields?
718. **Multi-step forms** — the `MigrationWizard` is a wizard. How is state maintained between steps? What happens on back navigation?
719. **Form arrays** — variable-length lists (e.g., budget line items, entity list). Is `useFieldArray` from React Hook Form used?
720. **Dependent validation** — "End date must be after start date." Cross-field validation with Zod's `.refine()`?
721. **File upload validation** — file type, file size, file count limits. Where is this validated?
722. **Number input specifics** — currency inputs must accept `1,234.56` but reject `abc`. Is `CurrencyInput` locale-aware?
723. **Date input specifics** — date pickers for fiscal periods. Must these be calendar dates or period names (Q1, Q2)?
724. **Autocomplete/search inputs** — for account selection, entity selection. Is there debounced search? Minimum character threshold?
725. **Rich text input** — for report narratives, board pack commentary. Is there a rich text editor? What library?

---

## 🔴 SECTION 52: SIDEBAR — ARCHITECTURE DEEP DIVE

726. **Sidebar sections** — "Main, Analysis, Management." How are these sections defined? Hardcoded? Configurable? Sector-dependent?
727. **Sidebar navigation items** — 80+ routes but the sidebar can't show 80 items. How are items prioritized? Does the sector config control visibility?
728. **Sidebar collapse** — collapsed to icons only. Are all icons unique and meaningful enough without text labels?
729. **Sidebar state persistence** — collapsed/expanded state persisted? Across sessions?
730. **Sidebar search** — a "Quick Search" in the sidebar. What does it search? Routes? Data? Both?
731. **Sidebar sections accordion** — can sections be expanded/collapsed independently? Or is it all or nothing?
732. **Sidebar badge counts** — do nav items show badges (e.g., "3 pending approvals")? Where does the count data come from?
733. **Sidebar footer** — "Settings, Help, Theme Toggle, Collapse." Are these always visible? Can they be moved?
734. **Sidebar on mobile** — is the sidebar a drawer on mobile? Full-screen overlay? Push content?
735. **Sidebar active state** — how is the active route highlighted? What happens with nested routes (e.g., `/budgets/:id` highlights "Budgets")?

---

## 🔴 SECTION 53: NAVBAR — SPECIFICS

736. **Entity Selector** — "multi-entity switching." How does the entity selector work? A dropdown with all entities? Search? Hierarchy tree?
737. **Entity switching effect** — when the user switches entity, what changes? All stores flush? All pages reload? Is it a soft or hard switch?
738. **Search in Navbar** — global search. Searches what? Budgets, forecasts, GL entries, entities? What's the search scope?
739. **Notifications in Navbar** — badge count? Dropdown list? What triggers a notification?
740. **Quick Create Menu** — "create budget, create forecast, create scenario" — is this a dropdown? Radix Dropdown?
741. **User Menu** — profile, settings, logout. What else? Theme toggle? Language selector?
742. **Navbar responsive** — on a narrow screen, how does the navbar collapse? Hamburger menu? Priority pattern (hide low-priority items)?
743. **Navbar sticky** — does the navbar scroll with content or stay fixed at the top?
744. **Navbar height** — fixed or variable? Does it change based on content (e.g., notification count)?
745. **Navbar accessibility** — is the navbar a `<nav>` element? Are landmarks properly used?

---

## 🔴 SECTION 54: COMMAND PALETTE — IMPLEMENTATION

746. **Search algorithm** — when the user types "bud", does it fuzzy-match to "Budget List," "Budget Create," "Budget vs Actual"? What library handles fuzzy matching?
747. **Command categories** — are commands grouped? Navigation, actions, recent, favorites?
748. **Keyboard navigation** — arrow keys to navigate results? Enter to select? Escape to close?
749. **Recent commands** — does the command palette remember recently used commands?
750. **Scoped commands** — when on the budget detail page, does the command palette show budget-specific commands?
751. **Command palette performance** — with 80+ routes and potentially hundreds of actions, does search feel instant? Is the command list pre-computed?
752. **Command palette vs help** — is there a distinction between "navigate to X" and "do X"? Can the command palette trigger actions, not just navigation?
753. **Command palette i18n** — do command names change with language? Is search in the current locale?
754. **Command palette accessibility** — is it announced as a dialog? Is focus managed correctly?
755. **Command palette customization** — can users add custom commands? Via plugins?

---

## 🔴 SECTION 55: KEYBOARD SHORTCUTS — SYSTEM-LEVEL

756. **Shortcut conflict with browser** — Ctrl+K opens the command palette, but Ctrl+K is also used by some browsers for link creation. How are conflicts resolved?
757. **Shortcut conflict with OS** — Ctrl+W closes a browser tab. Does the app intercept this?
758. **Shortcut customization** — can users rebind shortcuts? Where are custom bindings stored?
759. **Context-sensitive shortcuts** — Ctrl+Z for undo. But undo in a text input (form field) vs undo in the budget grid are different. How is context determined?
760. **Shortcut discovery** — the `KeyboardOverlay` shows shortcuts. Is it always accessible? Is there a cheat sheet?
761. **Shortcut accessibility** — are shortcuts announced to screen readers? What if a screen reader user can't use Ctrl+K?
762. **Shortcut groups** — are shortcuts organized by context? Global, grid-specific, chart-specific, modal-specific?
763. **Single-key shortcuts** — Excel uses single keys (Enter, Tab, Delete, F2 to edit). Are these supported in the budget grid?
764. **Shortcut conflicts between grid and page** — if Arrow keys navigate the grid AND scroll the page, which wins?
765. **Shortcut documentation** — is there a help page listing all shortcuts? Is it searchable?

---

## 🔴 SECTION 56: UNDO/REDO — THE HARD PROBLEM

766. **Undo granularity** — does each keystroke in a cell create an undo point? Or does the entire cell edit (focus to blur) create one?
767. **Cross-store undo** — undoing a budget change that was triggered by an import. Is the import also undone?
768. **Undo stack size** — how many undo levels? 50? 100? Infinite? Each level consumes memory.
769. **Undo memory** — with 100 undo levels each holding a snapshot of the entire budget store, how much memory is consumed?
770. **Redo clearing** — in most systems, a new action clears the redo stack. Is this implemented?
771. **Undo persistence** — does the undo stack survive across page navigations? Across app restarts?
772. **Collaborative undo** — if User A edits a cell and User B edits the same cell, who can undo it? What happens when both try?
773. **Undo across operations** — undoing a bulk operation (fill 100 cells) should be one undo, not 100. Is bulk undo atomic?
774. **Undo feedback** — when the user undoes, do they see a toast "Undone: Budget line item updated"? Or is it silent?
775. **Undo for destructive operations** — if a user deletes a budget and the store persists to IndexedDB, can they undo the deletion after persistence?

---

## 🔴 SECTION 57: CELL EDITING — SPREADSHEET FIDELITY

776. **Cell selection model** — single cell? Range selection? Multiple ranges (Ctrl+click)?
777. **Cell editing entry** — does double-click enter edit mode? F2? Or is typing sufficient to start editing?
778. **Cell editing exit** — Enter confirms? Escape cancels? Tab moves to next cell? What about Shift+Tab?
779. **Cell edit mode** — does the cell show an inline text input? Or does it transform into an input field?
780. **Formula entry** — when a user types `=` in a cell, does the formula bar activate? Does autocomplete suggest functions?
781. **Cell references in formula bar** — clicking on a cell while editing a formula should insert its reference (e.g., `B5`). Is this implemented?
782. **Drag to fill** — can users drag the cell corner to fill a series (1, 2, 3, ...)? `DragFillEngine` exists, but does it work with formulas (relative references)?
783. **Copy/paste from external sources** — can users paste from Excel into the grid? Does the paste preserve values, formulas, or both?
784. **Copy/paste within the grid** — does Ctrl+C/Ctrl+V work for cells? Does it copy formatting or just values?
785. **Cut and paste** — Ctrl+X. Does this correctly update formula references in other cells that pointed to the cut location?
786. **Cell protection** — `CellProtectionEngine` exists. Can certain cells be locked (read-only)? How is the lock visually indicated?
787. **Cell validation** — `CellValidationEngine` exists. Can cells have data validation rules (e.g., "must be positive," "must be a date")?
788. **Conditional formatting** — `ConditionalFormattingEngine` exists. Rules like "if value > 100, highlight green." How many rules per cell?
789. **Number formatting per cell** — can each cell have its own format? Currency, percentage, date, number of decimals?
790. **Cell borders and backgrounds** — can users add borders or background colors to cells? Is this stored with the data?

---

## 🔴 SECTION 58: LAZY LOADING & CODE SPLITTING — REALITY CHECK

791. **140+ lazy-loaded pages** — that means 140+ separate JS chunks. What's the average chunk size? How many are over 50KB?
792. **Shared dependencies** — if two lazy pages both use Recharts, is Recharts included in both chunks or in a shared chunk?
793. **Manual chunk configuration** — the document lists 6 vendor chunks. What determines which dependencies go into which chunk?
794. **Dynamic import error handling** — if a chunk fails to load (network error), what's the UX? A retry button? Automatic retry?
795. **Chunk prefetching** — are likely-next pages prefetched after the current page loads? Based on what heuristic?
796. **Engine lazy loading** — `EngineRegistry` lazy-loads 129 engines. Are these individual chunks or grouped? What's the loading latency?
797. **Tree shaking** — are unused exports from barrel files (`index.ts`) actually eliminated? Barrel files can defeat tree shaking.
798. **Side effects** — do any modules have top-level side effects (global state mutations, event listener registration) that prevent tree shaking?
799. **CSS code splitting** — are CSS files split per route? Or is all CSS in one file (larger initial load but smaller route loads)?
800. **Font loading** — are fonts loaded as part of the initial bundle or lazy-loaded? What's the FOUT (Flash of Unstyled Text) strategy?

---

## 🔴 SECTION 59: ERROR HANDLING — SYSTEMATIC ANALYSIS

801. **Error boundaries hierarchy** — 6 error boundaries. How are they nested? Does `PageErrorBoundary` wrap each page? What happens if it also throws?
802. **Unhandled promise rejections** — are all async operations (engine calls, storage operations, WebSocket messages) wrapped in try/catch?
803. **Global error handler** — is there a `window.onerror` or `window.addEventListener('unhandledrejection')` global handler?
804. **Error recovery strategies** — retry? Fallback data? Cached data? Graceful degradation? Which errors get which strategy?
805. **Error reporting** — when an error is caught, what's logged? Stack trace? Component name? User action? State snapshot?
806. **Silent failures** — are there operations that fail silently (catch with empty catch block)? These are the worst bugs.
807. **Network errors** — what happens when an API call fails? Toast notification? Retry logic? Exponential backoff?
808. **Engine errors** — if `FormulaEngine.parseFormula` throws, is the error isolated to that cell? Or does it crash the page?
809. **Storage errors** — if IndexedDB write fails, is the user notified? Is the data held in memory until the write succeeds?
810. **WebSocket errors** — on disconnect, is there auto-reconnect? Exponential backoff? Maximum retry count?
811. **Worker errors** — if a Web Worker throws, does the fallback run on the main thread? Does the UI freeze?
812. **Plugin errors** — a plugin throws during a formula evaluation. Does this corrupt the calculation graph?
813. **Import errors** — a malformed Excel file causes `ExcelImportEngine` to throw at row 50,000. Are the first 49,999 rows preserved?
814. **Export errors** — PDF generation runs out of memory. Is the error communicated clearly?
815. **Circular reference errors** — displayed as an error in the cell? A warning? Can the user choose to proceed with iterative calculation?

---

## 🔴 SECTION 60: THE CRUD PATTERNS — EVERY ENTITY

816. **Budget CRUD** — Create: form. Read: list + detail. Update: inline cell editing. Delete: confirmation dialog. Soft delete or hard delete?
817. **Forecast CRUD** — same questions. Can forecasts be soft-deleted and recovered?
818. **Scenario CRUD** — same. Can scenarios be duplicated? Archived?
819. **Report CRUD** — can reports be created, edited, duplicated, deleted, archived?
820. **Entity CRUD** — can entities be added/removed dynamically? What happens to consolidated data when an entity is removed?
821. **Account CRUD** — chart of accounts. Can accounts be added, renamed, deactivated (not deleted because GL entries reference them)?
822. **User CRUD** — user management. Can users be created, deactivated, role-changed? What happens to their data when deactivated?
823. **Role CRUD** — can custom roles be created beyond the 5 predefined ones?
824. **Template CRUD** — can templates be created, shared, imported, exported?
825. **Comment CRUD** — create, edit, delete comments on cells. Can the original author delete another user's reply?
826. **Task CRUD** — create, assign, complete, reopen tasks. What happens to tasks when the assignee is removed?
827. **Notification CRUD** — create on event, mark as read, delete. Are notifications persisted? Paginated?
828. **Audit log entries** — create only. Can audit entries be deleted? (They shouldn't be, for SOX compliance.)
829. **GL entry CRUD** — can GL entries be manually added? Edited after import? Deleted? What's the audit trail?
830. **Journal entry CRUD** — can users create manual journal entries? What approval workflow applies?

---

## 🔴 SECTION 61: SEARCH — COMPREHENSIVE

831. **Global search** — what's the search scope? Routes only? Data entities? GL entries?
832. **Search implementation** — client-side search over IndexedDB data? Full-text search? Fuzzy search?
833. **Search indexing** — is there a search index (like FlexSearch or Lunr.js) for fast client-side search?
834. **Search result ranking** — how are results ranked? Relevance? Recency? Frequency of use?
835. **Search result preview** — does the result show a snippet with the search term highlighted?
836. **Search filters** — can the user filter by entity type (budget, forecast, report)?
837. **Search history** — does the search bar remember previous searches?
838. **Search on large datasets** — searching 1M GL entries client-side. What's the performance?
839. **Grid-level search** — can users search within a grid (Ctrl+F within the budget grid)? Is this AG Grid's built-in search or custom?
840. **Search accessibility** — is the search input labeled? Do search results announce count to screen readers?

---

## 🔴 SECTION 62: NOTIFICATION SYSTEM — DETAILED

841. **Notification triggers** — what events create notifications? Budget submitted, task assigned, comment added, approval needed, import complete?
842. **Notification storage** — in `notificationStore`. Persisted to IndexedDB? How large can the notification list grow?
843. **Notification read/unread** — stored per notification. Does the badge count update immediately?
844. **Notification grouping** — "5 new comments on Budget Q3" vs 5 individual notifications. Is grouping supported?
845. **Push notifications** — in a web app, are Web Push Notifications supported? Requires service worker subscription.
846. **In-app vs push** — which notifications are in-app only and which trigger push notifications?
847. **Notification preferences** — can users configure which notifications they receive?
848. **Notification sound** — does the app play a sound for critical notifications? Is it configurable?
849. **Notification action** — clicking a notification navigates to the relevant page/item. Is deep linking implemented?
850. **Notification expiration** — do old notifications auto-expire? After how long?

---

## 🔴 SECTION 63: FISCAL CALENDAR — THE FOUNDATION

851. **4-4-5 calendar** — each quarter has months of 4, 4, and 5 weeks. This means the year is 364 days. What happens to the extra day(s)? 53rd week?
852. **13-period calendar** — 13 four-week periods. Period 13 is an extra month. Does the entire app understand 13 periods (not 12)?
853. **Fiscal year start** — can it start in any month? July fiscal year (government)? October fiscal year (federal government)?
854. **Fiscal period display** — how are periods displayed in the UI? "FY2024 P1" vs "Jan 2024" vs "FY2024 Q1"?
855. **Period comparison** — comparing FY2024 Q1 to FY2023 Q1 when the fiscal year starts in different calendar months. Are the dates aligned?
856. **YTD calculation** — if the fiscal year starts in July and it's October, YTD = Jul + Aug + Sep + Oct (4 months). Does the YTD calculation respect the fiscal year?
857. **Fiscal period validation** — can a user enter data for a closed fiscal period? Is period closing enforced?
858. **Period close workflow** — `PeriodCloseEngine`. Does closing a period lock data? Who can close a period? Who can reopen it?
859. **Period close checklist** — reconciliation complete, adjustments posted, reviews done. Is there a checklist before period close?
860. **Multi-calendar entities** — different entities might have different fiscal calendars. In consolidation, how are periods aligned?

---

## 🔴 SECTION 64: DEPRECIATION — ACCOUNTING SPECIFICS

861. **Depreciation methods** — SLN (straight-line), SYD (sum-of-years-digits), DDB (double declining balance). Are there additional methods? MACRS (tax)?
862. **Depreciation start date** — mid-month convention? Half-year convention? Full-month convention? Is this configurable?
863. **Asset groups** — can assets be grouped (buildings, vehicles, IT equipment) with different depreciation lives?
864. **Salvage value** — is salvage value (residual value) accounted for in depreciation calculations?
865. **Impairment** — if an asset's fair value drops below book value, an impairment charge is required (ASC 360). Is impairment modeled?
866. **Asset disposal** — selling or scrapping an asset. Does disposal generate the correct journal entry (remove cost and accumulated depreciation, record gain/loss)?
867. **Partial period depreciation** — an asset bought on March 15 in a monthly system. Is March depreciation a full month or half month?
868. **Component depreciation** — significant components of an asset (roof of a building) may have different useful lives. Is component depreciation supported?
869. **Depreciation forecast** — `DepreciationForecastPage`. Does it show future depreciation expense from current and planned assets?
870. **Tax vs book depreciation** — tax depreciation (MACRS) differs from book depreciation (straight-line). Is the temporary difference tracked for deferred taxes?

---

## 🔴 SECTION 65: INTERCOMPANY TRANSACTIONS — EXTREME DETAIL

871. **IC transaction types** — loans, goods/services, dividends, management fees, royalties. Are all types supported in matching?
872. **IC transaction matching** — automatic matching by amount? By date? By description? What tolerance for rounding differences?
873. **IC reconciliation** — Entity A says it's owed $1M by Entity B. Entity B says it owes $999K. Is the $1K difference flagged?
874. **IC netting** — if Entity A owes Entity B $1M and Entity B owes Entity A $800K, is the net $200K payable calculated?
875. **IC interest** — intercompany loans accrue interest. Is IC interest calculated and eliminated?
876. **IC profit in inventory** — Entity A sells goods to Entity B at a 20% markup. Entity B holds 50% in inventory. The consolidated inventory must be reduced by 50% of the 20% markup. Is this calculated?
877. **IC profit in fixed assets** — Entity A sells a building to Entity B at a gain. In consolidation, the gain is eliminated and the building is at original cost. Depreciation must be adjusted. Is this handled?
878. **IC tax implications** — intercompany transactions can create tax implications (withholding tax on dividends, transfer pricing). Are these considered?
879. **IC elimination entries** — are elimination entries stored separately from regular entries? Can they be reviewed and audited?
880. **IC elimination reversal** — when an intercompany receivable is eliminated in one period, does it need to be reversed when the cash is actually collected?

---

## 🔴 SECTION 66: AUDIT TRAIL — SOX DEPTH

881. **What events are audited?** Every data change? Every navigation? Every export? What's the audit scope?
882. **Audit entry structure** — who, what, when, where (which page), before value, after value. All of these captured?
883. **Audit immutability** — can audit entries be modified or deleted? By anyone? Even an admin?
884. **Audit storage** — where are audit entries stored? In IndexedDB? That means the user can delete them via browser devtools.
885. **Audit query** — can users search the audit trail? By user? By date? By entity? By action type?
886. **Audit export** — can the audit trail be exported for external review? In what format?
887. **Audit retention** — SOX requires 7-year retention. Does the app enforce this? Or could old audit entries be pruned?
888. **Audit tampering** — if a user modifies IndexedDB directly to alter financial data, does the audit trail detect the tampering?
889. **Audit performance** — with millions of audit entries, is the audit trail searchable without performance degradation?
890. **Audit for compliance** — does the audit trail satisfy SOX Section 302 (CEO/CFO certification) and Section 404 (internal controls)?

---

## 🔴 SECTION 67: VERSION CONTROL — BUDGET VERSIONING

891. **Version creation** — when is a new budget version created? Manual action? Automatic on save? On approval?
892. **Version naming** — "Draft 1," "Submitted," "Approved," "Final"? Custom naming?
893. **Version comparison** — can the user diff two versions cell-by-cell? Like a code diff?
894. **Version rollback** — can the user roll back to a previous version? Does this overwrite the current version?
895. **Version branching** — can a user create a branch of a budget (e.g., "Budget - Conservative" and "Budget - Aggressive")?
896. **Version merging** — if two branches are created, can they be merged? What about conflicting cell values?
897. **Version locking** — can a version be locked (read-only) after approval?
898. **Version metadata** — created by, created on, status, comments. Is this stored per version?
899. **Version storage** — are versions full copies (expensive) or deltas (complex)? What's the storage strategy?
900. **Version history depth** — how many versions are retained? Is there a limit?

---

## 🔴 SECTION 68: TEMPLATE SYSTEM — DEEP DIVE

901. **Template types** — budget templates, forecast templates, report templates, dashboard templates. Are all four supported?
902. **Template creation** — can a user save their current budget as a template?
903. **Template parameters** — are templates parameterized? (e.g., "Create budget from template with [Fiscal Year] and [Entity]")
904. **Template sharing** — can templates be exported and shared between installations?
905. **Template marketplace** — `TemplateMarketplace` component. Is this a real marketplace with online catalog? Or a local gallery?
906. **Template versioning** — can templates be updated? What happens to budgets already created from an older template version?
907. **Template validation** — if a template references a sector-specific feature (SaaS ARR) and is used in a Banking context, what happens?
908. **Template documentation** — does each template have a description, author, version, and usage instructions?
909. **Industry templates** — `src/engines/templates/` has 8 industry templates. Are these template objects or engine logic?
910. **Default templates** — does every new installation come with default templates? For each sector?

---

## 🔴 SECTION 69: PRINT & PDF — FIDELITY

911. **Page size** — does the PDF use Letter (US) or A4 (international)? Configurable?
912. **Margins** — what are the default PDF margins? Can they be customized?
913. **Headers and footers** — company name, report title, page number, date. All present?
914. **Font embedding** — does jsPDF embed fonts? Or does it use built-in fonts only (Helvetica, Courier)?
915. **Color printing** — charts with color. Are colors preserved in PDF? Or does it degrade to grayscale?
916. **Table of contents** — auto-generated with page numbers? For multi-page reports?
917. **Page breaks** — does the PDF paginate tables correctly? Or does it break rows mid-way?
918. **Watermarks** — can the user add a "CONFIDENTIAL" or "DRAFT" watermark?
919. **PDF security** — can the PDF be password-protected? Can printing be restricted?
920. **Accessibility of exported PDF** — is the PDF tagged for accessibility (PDF/UA)? Can a screen reader read the PDF?

---

## 🔴 SECTION 70: ENVIRONMENT & BUILD CONFIGURATION

921. **Environment variables** — what env vars are needed? `VITE_API_URL`? `VITE_WS_URL`? Where are they documented?
922. **Build modes** — development, staging, production. What changes between modes? API endpoints? Mock data? Logging level?
923. **Source maps** — are source maps generated for production builds? They help debugging but expose source code.
924. **Build cache** — does Vite cache builds? What's the rebuild time after a single file change?
925. **Build output** — what's in the `dist/` folder? HTML, JS chunks, CSS, assets, service worker, manifest?
926. **Asset optimization** — are images optimized during build? SVGs minified? Fonts subset?
927. **Gzip/Brotli** — is the build pre-compressed? Or does the server compress on-the-fly?
928. **Deployment target** — where is the web app deployed? S3 + CloudFront? Netlify? Vercel? A server?
929. **CDN configuration** — are static assets served from a CDN? What cache headers?
930. **CI/CD pipeline** — what runs on every push? Lint? Test? Build? Type check? Bundle size check?

---

## 🔴 SECTION 71: TYPESCRIPT — TYPE SAFETY DEEP DIVE

931. **Strict mode** — all strict flags enabled? `strictNullChecks`, `noImplicitAny`, `noUncheckedIndexedAccess`?
932. **Type assertions** — are there `as any` or `as unknown` casts? Each one is a potential runtime error.
933. **Generic types** — are stores typed generically? `create<StoreState>()`? Is the state shape fully typed?
934. **Engine return types** — do all engines have explicit return types? Or does TypeScript infer them (which can change unexpectedly)?
935. **Union types for financial data** — `AccountType = 'asset' | 'liability' | 'equity' | 'revenue' | 'expense'`. Are these strict unions or loose strings?
936. **Discriminated unions** — for error types, engine results, or API responses. Are discriminated unions used for exhaustive `switch` statements?
937. **Zod inference** — does the app use `z.infer<typeof schema>` to get TypeScript types from Zod schemas? Or are types maintained manually (risk of drift)?
938. **External library types** — AG Grid, Recharts, React Hook Form — are their types compatible with your TypeScript version? Any `@ts-expect-error` comments?
939. **Type coverage** — what's the TypeScript type coverage percentage? How many `any` types exist?
940. **Type generation** — are there any generated types (from API schemas, database schemas, etc.)? Or all manually written?

---

## 🔴 SECTION 72: ACCESSIBILITY — BEYOND THE BASICS

941. **Screen reader testing** — has the app been tested with NVDA, JAWS, and VoiceOver? By an actual screen reader user?
942. **AG Grid accessibility** — does AG Grid expose cells as `role="gridcell"` to screen readers? Can a blind user navigate the spreadsheet?
943. **Chart accessibility** — how does a screen reader user consume a bar chart? Is there a data table alternative?
944. **Color contrast** — every text/background combination meets WCAG AA (4.5:1)? Even in dark mode?
945. **Motion accessibility** — `useReducedMotion` exists. Does it disable ALL Framer Motion animations? Or only some?
946. **Skip navigation** — is there a "Skip to content" link on every page? Does it work with nested layouts?
947. **Focus management on route change** — when navigating to a new page, does focus move to the main heading? Or remain on the sidebar link?
948. **Live regions** — `useAnnounce` exists. When does it announce? On data load complete? On error? On save?
949. **Form error accessibility** — when a form validation fails, are errors announced? Are error messages associated with inputs via `aria-describedby`?
950. **Data table accessibility** — do financial tables use proper `<table>`, `<th>`, `<td>` semantics? Or are they `div`-based (inaccessible)?
951. **Keyboard trap detection** — can a user always Tab out of any component? Or are there keyboard traps in modals, dropdowns, or editors?
952. **High contrast mode** — Windows High Contrast mode (forced colors). Does the app look acceptable?
953. **Text resizing** — does the layout work at 200% browser zoom? At 400% (WCAG AAA requirement)?
954. **Touch targets** — minimum 44x44px touch targets? Especially important for mobile/tablet use.
955. **Cognitive accessibility** — is the navigation consistent? Are labels clear? Is jargon minimized?

---

## 🔴 SECTION 73: THE 36 UTILITY MODULES — EVERY ONE

956. You claim 36 utility modules. List every one with a one-line description. Which ones overlap in functionality?
957. Are utility modules pure functions? Or do they depend on store state, making them impure and hard to test?
958. Do any utilities have hidden side effects (writing to localStorage, making network calls)?
959. Are any utilities duplicated across files? Same logic written twice in different utils?
960. Are utility functions tested individually? Or only indirectly through engine/page tests?
961. `offlineCache.ts` vs `storeCache.ts` vs `QueryCache` — three caching utilities. Which caches what? Can they conflict?
962. `performance.ts` vs `performanceMonitor.ts` vs `performanceBudget.ts` — three performance utilities. How do they differ? Do they share data?
963. `security.ts` vs `securityHeaders.ts` vs `encryption.ts` — three security utilities. Is there a single security facade?
964. `validation.ts` vs Zod schemas — are there validation rules in `validation.ts` that duplicate Zod rules?
965. `backupRestore.ts` — does it use `masterStorage` or directly access IndexedDB? If the latter, it bypasses the storage abstraction.

---

## 🔴 SECTION 74: CONCURRENCY & RACE CONDITIONS

966. **Store persistence race** — store A persists, store B persists simultaneously. IndexedDB handles concurrent transactions, but are they ordered correctly?
967. **Engine computation race** — a formula recalculation starts, but the user edits a cell before it finishes. Does the calculation use stale data?
968. **WebSocket race** — a state change arrives via WebSocket while the user is editing the same cell locally. What takes precedence?
969. **Auto-save race** — auto-save fires while the user is mid-edit. Does it save the partial edit?
970. **Import race** — user starts a GL import, then navigates away before it completes. Is the import aborted? Partial import stored?
971. **Consolidation race** — consolidation starts, but an entity's GL data changes mid-consolidation. Does it use a snapshot?
972. **Undo race** — user undoes while an engine recalculation is in flight. Is the undo delayed until recalculation completes?
973. **Double-submit race** — user clicks "Save" twice quickly. Is the second click ignored?
974. **Multiple tab race** — user opens the app in two tabs. Both edit the same budget. IndexedDB writes from Tab 1 may be overwritten by Tab 2.
975. **Engine loading race** — two pages need the same engine simultaneously. `EngineRegistry` deduplicates — is this actually tested?

---

## 🔴 SECTION 75: MEMORY MANAGEMENT

976. **Memory baseline** — what's the memory usage on initial load? Before any data?
977. **Memory with data** — with 100K GL entries, 50 budgets, 10 scenarios loaded — what's the memory footprint?
978. **Memory leaks** — are there known memory leak patterns? Event listeners not cleaned up? Zustand subscriptions not unsubscribed?
979. **Large dataset memory** — importing 1M GL entries. How much memory does the structured clone to a worker consume?
800. **Chart memory** — 10 Recharts components, each with thousands of data points. SVG DOM nodes are expensive. Memory impact?
981. **Worker memory** — each worker has its own memory space. 4 workers × 100MB = 400MB. Is this budgeted?
982. **AI model memory** — a HuggingFace model loaded in the browser. 100MB+ model + inference buffers. Memory impact?
983. **IndexedDB memory** — does IndexedDB cache data in memory? How much?
984. **Garbage collection** — are there circular references preventing GC? Object references across stores?
985. **memoryMonitor.ts** — what does it monitor? `performance.memory`? What's the warning threshold? What action is taken?

---

## 🔴 SECTION 76: SENSITIVE DATA HANDLING

986. **PII in financial data** — employee compensation data is PII. Is it encrypted differently?
987. **Executive compensation** — board-level compensation is highly sensitive. Is access restricted to specific roles?
988. **Bank account numbers** — if financial data includes bank account numbers (for treasury/cash management), are they masked?
989. **Tax IDs** — if entity data includes EIN/TIN, is this masked or encrypted?
990. **Credit card data** — if processing payments or treasury data, PCI DSS applies. Is any PCI-relevant data handled?
991. **Data classification labels** — does `DataClassificationEngine` actually label data cells? Or is it an unused concept?
992. **Access logging** — when a user views sensitive data (executive compensation, tax IDs), is the access logged?
993. **Data masking in exports** — if a user exports a report, are sensitive fields masked in the export?
994. **Data masking in charts** — if a chart shows salary data, are individual salaries masked?
995. **Data in browser memory** — financial data is in Zustand state, which is in JS memory, accessible via browser devtools. How is this mitigated?

---

## 🔴 SECTION 77: EDGE CASE FINANCIAL SCENARIOS

996. **Fiscal year spanning calendar years** — FY2024 starting April 2023. Period 1 = April 2023. Does the entire date handling understand this?
997. **Negative budgets** — can a line item have a negative budget amount? (E.g., refund budget.)
998. **Zero-line-item budget** — a budget with no line items. Can it be created? Does it crash any engine?
999. **Budget with one line item** — edge case. Does aggregation, totals, percentage calculation work correctly?
1000. **Deeply nested account hierarchy** — 10 levels deep. Does the tree component render efficiently? Does the rollup calculation work?
1001. **Entity with no GL data** — an entity exists but has no imported GL entries. What does consolidation produce?
1002. **Scenario with all zero inputs** — all drivers are 0. Does Monte Carlo handle this? Does the result make sense?
1003. **Forecast with infinite growth rate** — a user enters a growth rate of 99999%. Does the calculation overflow? Does the UI handle infinity?
1004. **Currency with no decimal places** — JPY (¥1,234, not ¥1,234.00). Are all financial calculations adjusted?
1005. **Mixed currencies in one budget** — line items in EUR and USD. Is this supported? Or must all line items be in one currency?
1006. **Negative interest rate** — ECB has had negative rates. Does the `BondPricingEngine`, `LoanAmortizationEngine`, `CashEngine` handle negative rates?
1007. **Leap year** — February 29. Does the fiscal calendar handle leap years correctly? Daily depreciation on leap year vs non-leap year?
1008. **Day count conventions** — ACT/360, ACT/365, 30/360, ACT/ACT. Different conventions produce different interest amounts. Is this configurable?
1009. **Multi-GAAP** — a company reports under both US GAAP and IFRS. Can the app produce both sets of financial statements?
1010. **Restatements** — if prior period financials are restated, how is the restatement tracked? Is the original preserved?
1011. **Extraordinary items** — IFRS no longer has extraordinary items, but US GAAP retains some concepts. Is the treatment configurable by standard?
1012. **Discontinued operations** — income/loss from discontinued operations must be shown separately on the income statement. Is this supported?
1013. **Segment reporting** — ASC 280 requires segment-level financial data. Is segment reporting supported?
1014. **Related party transactions** — disclosures required. Are related party transactions flagged?
1015. **Subsequent events** — events after the balance sheet date. Are they tracked and disclosed?
1016. **Off-balance-sheet items** — operating leases (pre-842), guarantees, letters of credit. Are these tracked?
1017. **Fair value hierarchy** — Level 1 (quoted prices), Level 2 (observable inputs), Level 3 (unobservable). Is fair value hierarchy tracked for financial instruments?
1018. **Goodwill and intangibles** — impairment testing for goodwill (ASC 350). Is this modeled?
1019. **Stock compensation waterfall** — SBC expense by vesting tranche over time. Is the waterfall calculated?
1020. **Pension accounting** — defined benefit pension (ASC 715) with PBO, ABO, plan assets, funded status. Is pension accounting in scope?

---

## 🔴 SECTION 78: WORKFLOW ENGINE — TECHNICAL DEPTH

1021. **7 workflow engines** — what's the call sequence? `WorkflowBuilderEngine` defines → `WorkflowEngine` orchestrates → `WorkflowTriggerEngine` triggers → `WorkflowActionEngine` executes?
1022. **Workflow state machine** — does `StateMachine` represent the workflow lifecycle? States: Draft → Submitted → In Review → Approved → Published?
1023. **Workflow conditions** — can a workflow branch based on conditions? "If amount > $1M, route to CFO; else route to Controller"?
1024. **Workflow timers** — can a workflow step have a timer? "If not approved within 5 days, escalate"?
1025. **Workflow notifications** — when a workflow transitions, are notifications sent? Via `notificationStore`?
1026. **Workflow audit** — every state transition logged? Can the workflow history be viewed?
1027. **Workflow templates** — predefined workflows for common processes. How many templates ship with the app?
1028. **Workflow serialization** — can a workflow definition be exported as JSON and imported in another installation?
1029. **Workflow execution persistence** — if the app closes mid-workflow, is the workflow state persisted? Does it resume on next launch?
1030. **Workflow visual builder** — `VisualWorkflowEngine` + `WorkflowBuilderEngine`. Is there a drag-and-drop workflow designer? Or is it form-based?

---

## 🔴 SECTION 79: THE 30 HOOKS — FULL LIST DEMANDED

1031. Name all 30 custom hooks with their file location and purpose.
1032. Are hooks exported from a barrel file? Or imported directly from their files?
1033. Are any hooks dependent on other hooks? What's the dependency graph?
1034. Are there hooks that combine multiple store subscriptions? These can cause unnecessary re-renders.
1035. Are hooks tested independently with `renderHook` from testing-library?
1036. Is there a `useDebounce` hook? Where is debouncing used?
1037. Is there a `useThrottle` hook? Where is throttling used?
1038. Is there a `useLocalStorage` hook separate from Zustand persist?
1039. Is there a `useMediaQuery` hook? For responsive behavior? For reduced motion? For dark mode preference?
1040. Is there a `useIntersectionObserver` hook? For lazy-loading components in view? For infinite scroll?

---

## 🔴 SECTION 80: THE 15 SECTOR CONFIGS — CROSS-CUTTING

1041. **Sector switching** — when a user switches from "Banking" to "Manufacturing," what changes in the UI?
1042. **Multi-sector** — can a company operate in multiple sectors simultaneously? A conglomerate in both manufacturing and real estate?
1043. **Sector-specific engines** — when the sector is "Banking," is `BankingEngine` loaded at startup? Or still lazy?
1044. **Sector-specific KPIs** — are KPIs on the dashboard dynamically determined by the sector config?
1045. **Sector-specific formulas** — does the formula engine include sector-specific functions? (e.g., `=NIM()` for banking)
1046. **Sector templates** — each sector ships with templates. Are these mandatory or optional?
1047. **Sector defaults** — default currency, default fiscal calendar, default chart of accounts. All from the sector config?
1048. **Sector validation** — can a user enter data that's inconsistent with their sector? (e.g., "Number of beds" for a manufacturing company)
1049. **Sector migration** — changing sectors after data has been entered. What happens to sector-specific data?
1050. **Custom sectors** — can a user create a custom sector config? Or are only the 15 predefined sectors available?

---

## 🔴 SECTION 81: THE SIDEBAR & NAVIGATION — FULL INTERROGATION

1051. **Sidebar sections** — are sections hardcoded or configurable by role? A Viewer doesn't need "Create" links.
1052. **Sidebar ordering** — `sidebarOrder` in sector config. Does this control the order of all nav items or just section order?
1053. **Sidebar icon system** — Lucide React icons. Are all icons semantically correct? Is "Budgets" using a calculator icon or a money icon?
1054. **Sidebar collapse animation** — when collapsing, does the text fade? Slide? Or abruptly disappear?
1055. **Sidebar width** — expanded width? Collapsed width? Fixed or resizable?
1056. **Sidebar persistence** — collapsed state persisted across sessions? In which store? `uiStore`?
1057. **Sidebar on route change** — does the sidebar navigate immediately or wait for the page to load?
1058. **Sidebar breadcrumbs** — are breadcrumbs shown below the navbar for nested routes?
1059. **Sidebar overflow** — if there are more nav items than the viewport height, does the sidebar scroll? Is the footer always visible?
1060. **Sidebar quick actions** — besides navigation, can the sidebar contain quick actions (new budget, new forecast)?

---

## 🔴 SECTION 82: THEME SYSTEM — VISUAL FIDELITY

1061. **Theme toggle** — where is the toggle? Sidebar footer? User menu? Both?
1062. **Theme persistence** — stored in `uiStore`, persisted to IndexedDB. On first visit, what's the default? System preference?
1063. **Dark mode colors** — are dark mode colors manually defined for every component? Or is it a mechanical color inversion?
1064. **Dark mode contrast** — dark mode often has lower contrast. Are WCAG contrast ratios maintained in dark mode?
1065. **Chart colors in dark mode** — do chart colors adapt? Are they still readable against a dark background?
1066. **Dark mode transition** — when toggling, is there a smooth transition? Or does the page flash white/dark?
1067. **PDF export theme** — if the user is in dark mode, does the PDF export use light mode (standard for print)?
1068. **System theme detection** — `prefers-color-scheme` media query. Does the app respect the OS setting?
1069. **Theme tokens** — are colors defined as CSS custom properties for dynamic theming?
1070. **Third-party component themes** — AG Grid, Recharts, Radix UI. Do all support dark mode? Is the theming consistent across all libraries?

---

## 🔴 SECTION 83: ANIMATION — MOTION SYSTEM

1071. **Framer Motion usage** — which components use Framer Motion? Page transitions? Modals? Charts? List animations?
1072. **Animation performance** — are animations GPU-accelerated (transform, opacity)? Or layout-triggering (width, height, top, left)?
1073. **Animation duration scale** — is there a consistent duration scale? (100ms, 200ms, 300ms, 500ms)
1074. **Animation easing** — consistent easing curves? Ease-in-out? Spring? Custom cubic-bezier?
1075. **Reduced motion** — `useReducedMotion` — does it disable transitions in Tailwind classes too? Or only Framer Motion?
1076. **Layout animations** — when items reorder in a list (sorting), does Framer Motion's `layout` prop animate the reorder?
1077. **Enter/exit animations** — `AnimatePresence` for modals, toasts, notifications. Does the exit animation complete before the component unmounts?
1078. **Skeleton animations** — skeleton screens should have a shimmer animation. Is this CSS or Framer Motion?
1079. **Chart animation** — do charts animate on first render? On data update? Recharts has built-in animation — is it enabled?
1080. **Performance budget for animations** — 60fps target? How is frame rate monitored during animations?

---

## 🔴 SECTION 84: ERROR STATES — EVERY SCENARIO

1081. **Network error** — user action triggers an API call that fails. What's shown? Toast? Inline error? Full-page error?
1082. **Auth error (401)** — token expired, refresh failed. Redirect to login? Is the user's unsaved work preserved?
1083. **Permission error (403)** — user tries to access a feature they don't have permission for. What's shown?
1084. **Not found error (404)** — user navigates to `/budgets/nonexistent-id`. What's shown?
1085. **Validation error** — form submission fails Zod validation. Inline field errors? Summary at top? Both?
1086. **Engine error** — FormulaEngine throws on a malformed formula. Is the error in the cell? A toast? Both?
1087. **Import error** — Excel file is corrupt. What error message? Does it show which row/field failed?
1088. **Export error** — PDF generation runs out of memory. What's the UX?
1089. **Storage error** — IndexedDB quota exceeded. What's the message? What's the recommended action?
1090. **WebSocket error** — connection lost during collaboration. Does the UI show a reconnection status?

---

## 🔴 SECTION 85: EMPTY STATES — EVERY PAGE

1091. **Empty dashboard** — no budgets, no forecasts, no GL data. What does the dashboard show?
1092. **Empty budget list** — no budgets created. What CTA is shown? "Create your first budget"?
1093. **Empty GL data** — no imports yet. What's on the GL Explorer page?
1094. **Empty scenario comparison** — no scenarios selected for comparison. What's shown?
1095. **Empty consolidation** — only one entity. Can consolidation even run? What's the UX?
1096. **Empty report list** — no reports. "Create a report" CTA?
1097. **Empty notifications** — no notifications. "You're all caught up"?
1098. **Empty search results** — no matches. What's shown? Suggestions?
1099. **Empty activity feed** — no recent activity. What's displayed on the dashboard?
1100. **Empty approval queue** — no pending approvals. "No approvals needed"?
1101. **Empty audit trail** — no audit entries. Expected on a new install?
1102. **Empty chart of accounts** — no accounts. Can the app function without accounts?
1103. **Empty entity list** — no entities added. Is this possible? What breaks?
1104. **Empty collaboration comments** — no comments. "Start a conversation"?
1105. **Empty template gallery** — no templates. Where do they come from on a fresh install?

---

## 🔴 SECTION 86: LOADING STATES — EVERY PAGE

1106. **Initial app load** — what's shown before React hydrates? A blank white screen? A splash screen? An HTML skeleton?
1107. **Page loading** — `<Suspense fallback={<LoadingScreen />}>` — is this a spinner? A skeleton? A progress bar?
1108. **Data loading** — Zustand store has `isLoading: true`. What's shown during data load? Per-page loading state?
1109. **Engine loading** — EngineRegistry loading a lazy engine. Is there a loading indicator? How long does it take?
1110. **AI model loading** — HuggingFace model downloading. Progress bar? Estimated time?
1111. **Import loading** — large Excel file importing. Progress bar? Row count? Cancel button?
1112. **Export loading** — PDF generation. Spinner? "Generating PDF..."?
1113. **Consolidation loading** — multi-entity consolidation. How long? Progress indicator?
1114. **Monte Carlo loading** — simulation running. Progress (iteration count)?
1115. **Calculation loading** — formula recalculation for 10,000 cells. Is there any indication?

---

## 🔴 SECTION 87: DATA GRID FEATURES — MISSING FEATURES AUDIT

1116. **Find and Replace** — `FindReplaceDialog` component. Does it work across all grids? Or only specific ones?
1117. **Multi-cell editing** — select a range of cells and type a value. Does it fill all cells?
1118. **Auto-sum** — select a column of numbers and press Alt+=. Does auto-sum work?
1119. **Data bars** — in-cell bar charts showing relative magnitude. Supported?
1120. **Sparklines in cells** — tiny line charts within grid cells showing trend. Supported?
1121. **Heat map coloring** — cells colored by value (red-yellow-green gradient). Conditional formatting handles this?
1122. **Column freeze/lock** — freeze the first 3 columns while scrolling right. Supported?
1123. **Row freeze/lock** — freeze header rows while scrolling down. Supported?
1124. **Multi-level headers** — "FY2024" spanning "Q1 | Q2 | Q3 | Q4" spanning "Jan | Feb | Mar" etc. Supported?
1125. **Calculated rows** — a "Total" row that automatically sums the column. Configurable (sum, average, count)?
1126. **Percentage of total** — a column showing each row as a percentage of the column total. Calculated automatically?
1127. **Growth rate columns** — "FY2024 vs FY2023" with percentage change. Auto-calculated?
1128. **Variance columns** — "Actual - Budget" and "% Variance." Auto-calculated?
1129. **Drill-down** — clicking a KPI drills down into the detail. Is drill-down path configurable?
1130. **Drill-through** — clicking a cell navigates to a related page (e.g., account detail). Is drill-through configured per grid?

---

## 🔴 SECTION 88: IMPORT PIPELINE — EVERY STAGE

1131. **Stage 1: File selection** — `FileUploader` component. Accepts which file types? Max size?
1132. **Stage 2: File parsing** — `ExcelImportEngine`. Handles .xlsx, .xls, .csv, .json? What about .ods (LibreOffice)?
1133. **Stage 3: Column mapping** — `SmartImportMapper`. AI-powered mapping. What AI? Rules-based? ML?
1134. **Stage 4: Data preview** — `ImportPreview`. Shows first N rows before import. How many rows in preview?
1135. **Stage 5: Validation** — `DataQualityEngine`. What checks? Missing required fields? Invalid data types? Duplicate entries?
1136. **Stage 6: Transformation** — data type conversion, sign convention adjustment, period date mapping. Configurable?
1137. **Stage 7: Conflict resolution** — if importing data for existing periods, overwrite or merge? User choice?
1138. **Stage 8: Import execution** — `ImportEngine.importData()`. Batch size? Progress reporting?
1139. **Stage 9: Post-import verification** — trial balance check (debits = credits). Automatic?
1140. **Stage 10: Import history** — `glStore.importHistory`. Records what was imported, when, by whom. Audit trail?

---

## 🔴 SECTION 89: EXPORT PIPELINE — EVERY STAGE

1141. **Export format selection** — PDF, Excel, CSV. Are all three equally feature-rich?
1142. **Export scope** — entire budget? Selected rows? Filtered view? Current page?
1143. **Export template** — predefined layouts for export? Or raw data dump?
1144. **Export customization** — can users add headers, footers, logos, watermarks?
1145. **Export scheduling** — `ReportSchedulerEngine`. Can exports be scheduled? Where does the schedule run (offline-first, no server)?
1146. **Export to email** — can the exported file be emailed? Requires an email service.
1147. **Export file naming** — auto-generated filename? Configurable pattern?
1148. **Export compression** — large exports zipped? `.zip` file download?
1149. **Export progress** — for large reports, is there a progress indicator?
1150. **Export cancellation** — can the user cancel a long-running export?

---

## 🔴 SECTION 90: DASHBOARD — COMPLETE INTERROGATION

1151. **Dashboard layout** — fixed grid? Drag-and-drop? Widget-based? Can users customize?
1152. **KPI cards** — how many on the default dashboard? Which KPIs? Configurable by sector?
1153. **KPI trend** — does each KPI card show a sparkline or trend indicator?
1154. **KPI comparison** — "vs prior period" or "vs budget" shown on each KPI?
1155. **Charts on dashboard** — how many charts? Which chart types? Configurable?
1166. **Activity feed** — what activities? Budget changes? Approvals? Imports? Configurable?
1167. **Date range filter** — does the dashboard have a global date range filter that affects all KPIs and charts?
1168. **Entity filter** — does the dashboard show data for the active entity only? Or all entities?
1169. **Dashboard refresh** — is there a manual refresh button? Auto-refresh interval?
1170. **Dashboard templates** — `DashboardBuilderEngine` + `DashboardTemplate`. Can users create custom dashboard layouts?

---

## 🔴 SECTION 91: SETTINGS — EVERY SETTING

1171. **Organization settings** — org name, logo, address, fiscal year, default currency. All editable?
1172. **User settings** — name, email, password, MFA, language, timezone, theme preference?
1173. **Role management** — create/edit/delete roles? Assign permissions to roles?
1174. **User management** — create/edit/deactivate users? Assign roles to users?
1175. **Sector settings** — change active sector? What happens to existing data?
1176. **Fiscal calendar settings** — change fiscal year start month? Change period structure?
1177. **Data import settings** — default date format? Default number format? Default encoding?
1178. **Notification settings** — enable/disable notification categories? Email preferences?
1179. **Security settings** — password policy? Session timeout? MFA requirement? IP whitelist?
1180. **Backup settings** — auto-backup frequency? Backup location? Retention policy?
1181. **Display settings** — number of decimal places? Thousands separator? Negative number format?
1182. **Accessibility settings** — font size? High contrast? Reduced motion? Keyboard shortcuts?
1183. **Plugin settings** — installed plugins list? Enable/disable? Configure?
1184. **Integration settings** — QuickBooks connection? Xero connection? API keys?
1185. **Advanced settings** — cache management? Storage management? Debug mode?

---

## 🔴 SECTION 92: PROFILE PAGE — SPECIFICS

1186. **Profile fields** — name, email, avatar, role, department. Editable?
1187. **Avatar upload** — can users upload a profile picture? Where is it stored? IndexedDB?
1188. **Password change** — old password + new password + confirm. Validation rules?
1189. **Activity log** — can users see their own activity history?
1190. **Preferences** — language, theme, timezone, date format, number format. All personal preferences?

---

## 🔴 SECTION 93: HELP PAGE — SPECIFICS

1191. **Help content** — is it static documentation? Or context-sensitive?
1192. **Searchable help** — can users search help articles?
1193. **Help categories** — organized by feature? By task? By role?
1194. **Video tutorials** — any video content embedded?
1195. **Contact support** — is there a support form? Email? Chat?

---

## 🔴 SECTION 94: ONBOARDING — FIRST-TIME EXPERIENCE

1196. **Onboarding steps** — how many steps? What data is collected?
1197. **Sector selection** — the first decision. Is guidance provided? Recommendations?
1198. **Data import prompt** — does onboarding prompt the user to import existing data?
1199. **Sample data** — can the user load sample data during onboarding to explore the app?
1200. **Guided tour integration** — does the onboarding wizard connect to the `GuidedTour` system?
1201. **Onboarding skip** — can the user skip onboarding? Can they return to it later?
1202. **Onboarding progress** — can the user save progress and return later?
1203. **Onboarding validation** — can the user proceed without completing required fields?
1204. **Post-onboarding** — after onboarding, what page does the user land on?
1205. **Onboarding completion tracking** — is onboarding completion stored? So the user doesn't see it again?

---

## 🔴 SECTION 95: COLLABORATION — EVERY FEATURE

1206. **Comments on cells** — can users @mention other users? Are mentions notified?
1207. **Comment threads** — are comments threaded (reply to a comment)?
1208. **Comment rich text** — can comments include formatting? Links? Images?
1209. **Comment search** — can users search across all comments?
1210. **Tasks from comments** — can a comment be converted to a task?
1211. **Task assignment** — can tasks be assigned to specific users?
1212. **Task due dates** — can tasks have due dates? What happens when they're overdue?
1213. **Task priorities** — low, medium, high, critical? Color-coded?
1214. **Task completion** — marking a task as complete. Does it notify the creator?
1215. **Activity log filtering** — can the activity log be filtered by user, date, action type?
1216. **Presence indicators** — "User A is viewing this page." Real-time? Updated via WebSocket?
1217. **Editing conflict indicators** — "User B is editing cell B5." Is this shown in the grid?
1218. **Chat functionality** — `ChatPanel`, `ChatMessage`, `ChatChart` components. Is there a real-time chat?
1219. **Document attachments** — `DocumentEngine`. Can users attach files to budgets or reports?
1220. **Version history** — per-item version history with user attribution. Full diff view?

---

## 🔴 SECTION 96: TEMPLATE MARKETPLACE — REALITY CHECK

1221. **Template gallery** — how many templates ship by default?
1222. **Template categories** — organized by sector? By use case? By complexity?
1223. **Template preview** — can users preview a template before applying it?
1224. **Template import/export** — `.template` file format? JSON?
1225. **Template marketplace backend** — is there a server hosting templates? Or is it all local?
1226. **Template versioning** — template v1.0, v1.1. Can users update templates?
1227. **Template customization** — after applying a template, can the user modify it?
1228. **Template documentation** — does each template have a description, screenshots, requirements?
1229. **Template rating/reviews** — can users rate templates? If there's a marketplace?
1230. **Template sharing** — can users share their custom templates with colleagues?

---

## 🔴 SECTION 97: THE HARDEST TECHNICAL QUESTIONS

1231. **IndexedDB in Safari Private Mode** — Safari severely limits IndexedDB in private browsing. Does the app work at all in private mode?
1232. **IndexedDB cross-tab** — if the user opens two tabs, can they corrupt each other's data via concurrent IndexedDB writes?
1233. **Service Worker scope** — does the service worker control the entire app? What about pages loaded before the SW activates?
1234. **Cache invalidation strategy** — "There are only two hard things in computer science: cache invalidation and naming things." How many caching layers exist, and how is each invalidated?
1235. **Timezone handling** — financial data has dates. The user is in New York, the server (if any) is in UTC, the entity is in London. How are dates handled?
1236. **Date arithmetic** — "add 1 month to January 31." Is it February 28? March 3? How does `FiscalCalendar` handle this?
1237. **Floating point precision** — `0.1 + 0.2 !== 0.3`. Financial calculations MUST use exact decimal arithmetic. Is `SafeMathParser` handling this? Or does it use IEEE 754 floats?
1238. **BigInt support** — for very large financial numbers (government budgets in trillions). Does the app use BigInt? Or regular JS numbers (safe up to 2^53)?
1239. **Decimal.js / big.js** — for precise financial math, many apps use decimal libraries. Does this app? Or rely on native JS floats?
1240. **Number overflow** — a calculation produces a number larger than `Number.MAX_SAFE_INTEGER`. What happens? `Infinity`? Error? Silent data corruption?
1241. **Sign convention** — expenses are positive in budgets but negative in GL (credit). When mixing data sources, does the sign convention align?
1242. **Calendar vs fiscal period mapping** — January 2024 in a July fiscal year is FY2024 H2 P7. Is this mapping available to all engines?
1243. **Accrual vs cash basis** — is the app accrual-only? Or does it support cash basis accounting?
1244. **Multi-book accounting** — companies may maintain parallel books (tax books, management books, statutory books). Is this supported?
1245. **Chart of accounts mapping** — different entities may have different CoA. In consolidation, how are accounts mapped to a common structure?

---

## 🔴 SECTION 98: REGULATORY & COMPLIANCE — DEEPEST LEVEL

1246. **SEC reporting** — 10-K, 10-Q formats. Does the app produce SEC-ready financial statements?
1247. **XBRL tagging** — SEC requires XBRL. Is the output XBRL-tagged?
1248. **SOX 302/404** — CEO/CFO certification requires internal controls documentation. Does the app support this?
1249. **Basel III** — for banking sector. Capital adequacy reporting. Is the output format compliant?
1250. **Solvency II** — for insurance sector. Regulatory capital requirements. Is this in scope?
1251. **CSRD/ESRS** — EU sustainability reporting. Does the ESG output comply with European Sustainability Reporting Standards?
1252. **TCFD** — Task Force on Climate-related Financial Disclosures. Are TCFD-aligned reports supported?
1253. **GDPR** — financial data of EU data subjects. Data processing records? DPIA? Right to erasure?
1254. **CCPA** — California privacy rights. Does the app handle CCPA data subject requests?
1255. **Data residency** — can data be restricted to a specific geographic region? In an offline app, data stays on device — but what about collaboration sync?

---

## 🔴 SECTION 99: DEPLOYMENT & OPERATIONS — PRODUCTION READINESS

1256. **Deployment process** — what's the release process? Manual deploy? Automated CI/CD?
1257. **Rolling deployment** — can a new version be deployed without downtime? For a desktop app, how are updates distributed?
1258. **Desktop update mechanism** — Tauri's auto-updater? Manual download? App Store?
1259. **Update data migration** — when the app updates, does IndexedDB/SQLite data migrate automatically?
1260. **Update rollback** — if a new version has a critical bug, can users roll back to the previous version?
1261. **Beta channel** — can users opt into a beta/preview channel?
1262. **Feature flags** — can features be toggled remotely? Without an update?
1263. **Telemetry** — crash reports, usage analytics. Opt-in? Opt-out? GDPR-compliant?
1264. **Monitoring** — how do you know if production users are experiencing errors? No server = no server-side monitoring.
1265. **Support infrastructure** — how does a user report a bug? Is there a bug report feature in the app?

---

## 🔴 SECTION 100: THE FINAL 100 — EXISTENCE JUSTIFIED

1266. If you deleted all 174 engines and rebuilt only 20, which 20 would you keep and why?
1267. If you deleted all 140 pages and rebuilt only 20, which 20 would you keep and why?
1268. If you deleted all 24 stores and rebuilt only 10, which 10 would you keep and why?
1269. If you deleted 12 of the 15 sectors, which 3 would you keep and why?
1270. If you deleted 5 of the 8 languages, which 3 would you keep and why?
1271. What's the **bus factor** — how many people understand the full architecture? If that person leaves, what happens?
1272. What's the **technical debt inventory** — which parts of the codebase would you rewrite if you had 3 months?
1273. What's the **most dangerous bug** you suspect exists but haven't found yet?
1274. What's the **most valuable feature** that's actually complete and working?
1275. What's the **most complex feature** that's only 10% done?
1276. Has this codebase been **reviewed by anyone outside the team**? An external auditor? A security firm? A senior FP&A professional?
1277. What's the **performance of the slowest page** in milliseconds? Tested with realistic data?
1278. What's the **bundle size of the slowest page** in kilobytes? The user downloads this before they can interact.
1279. What's the **largest IndexedDB record** that can be stored without performance degradation?
1280. How many **concurrent users** can the collaboration system support before WebSocket degrades?
1281. What's the **maximum formula complexity** (number of nested functions, reference depth) before the FormulaEngine chokes?
1282. What's the **maximum Monte Carlo iterations** before the browser kills the worker?
1283. What's the **maximum consolidation entity count** before the process takes more than 60 seconds?
1284. What's the **maximum PDF page count** before jsPDF runs out of memory?
1285. What's the **maximum Excel file size** that can be imported without freezing the browser?
1286. Is there a **known limitations document**? Every software has limitations. What are yours?
1287. What's the **browser compatibility matrix**? Chrome, Firefox, Safari, Edge — minimum versions? What about mobile browsers?
1288. Is there a **load testing strategy**? How do you simulate 100 users, 1000 budgets, 1M GL entries?
1289. Is there a **disaster recovery plan**? If the user's laptop dies, what's recoverable?
1290. What's the **SLA for bug fixes**? Critical bugs fixed in how many hours/days?
1291. What's the **deprecation policy**? When a feature is deprecated, how much notice do users get?
1292. What's the **data retention policy**? How long is financial data kept? Is it configurable?
1293. What's the **audit retention policy**? SOX requires 7 years. Is this enforced in an offline app?
1294. Is there a **data deletion capability**? GDPR right to erasure. Can all user data be deleted?
1295. Is there a **data export capability**? GDPR right to portability. Can all user data be exported in a standard format?
1296. What happens to a user's data **if the company (FinPlan Pro) goes out of business**? Is there a data escrow?
1297. Is there an **open source version**? Community edition? Enterprise edition? What's the licensing model?
1298. What's the **pricing model**? Per seat? Per entity? Per feature tier? Flat fee? How does it compete with free alternatives?
1299. What's the **go-to-market strategy**? Self-serve SaaS? Enterprise sales? Channel partners?
1300. And the final brutal question: **If you started this project over from scratch today, knowing everything you know now, what would you build differently — and would you build it at all?**

# 500 MORE BRUTAL QUESTIONS — THE UNCHARTED TERRITORIES

---

## 🔴 SECTION 101: THE PROMPT DIRECTORY — 18 SPECIFICATION DOCUMENTS

1301. The `prompt/` directory contains **18 specification documents**. List every single one with its title and purpose.
1302. Who wrote these specs? A human? An AI? A combination? What's the authorship story?
1303. Are these specs **up to date** with the current codebase? Or has the code diverged from the specs?
1304. When a spec says "the engine should do X" and the code does "X + Y", which is authoritative? The spec or the code?
1305. Are the specs **machine-readable** (YAML, JSON schema) or **human-readable** (Markdown prose)?
1306. Can the specs be **consumed by AI agents** to generate code automatically? Is that how the codebase was built?
1307. Are there specs for features that are **not yet implemented**? How many specs describe planned vs. implemented features?
1308. Is there a spec for **every one of the 174 engines**? Or only high-level specs for engine categories?
1309. Do the specs include **acceptance criteria**? How is "done" defined for each feature?
1310. Do the specs include **test scenarios**? Are the test files generated from specs?
1311. Do the specs include **UI mockups/wireframes**? Or text-only descriptions?
1312. Is there a **spec for the spec format**? A meta-spec? How is consistency maintained across 18 documents?
1313. Are there specs that **contradict each other**? E.g., one spec says "offline-first" while another describes "real-time collaboration" requiring connectivity?
1314. Is there a **changelog for the specs**? When a spec is updated, is the change tracked?
1315. Are the specs **versioned alongside the code**? If the spec changes, does the code need to change?
1316. What happens when a spec is **ambiguous**? Who makes the final decision? The developer? The AI agent? The product owner?
1317. Is there a spec for the **data model**? Entity-relationship diagrams? Data dictionary?
1318. Is there a spec for the **API contract**? Even though there's no server, there are internal APIs (store actions, engine methods).
1319. Is there a spec for **error handling**? What errors are expected, how they're handled, and what messages users see?
1320. Is there a spec for **performance requirements**? Response time targets, memory budgets, bundle size limits?

---

## 🔴 SECTION 102: THE AGENTS DIRECTORY — AI-ASSISTED DEVELOPMENT

1321. The `agents/` directory contains **agent task assignments**. What are these agents? LLMs? Custom scripts? Human agents?
1322. `AGENTS.md` is an "Agent coordination doc." What agents are coordinating? How many? What are their roles?
1323. Are agents **autonomous** (making code changes independently) or **assistive** (suggesting changes for human review)?
1324. What percentage of the **1,190 TypeScript files** were written by AI agents vs. human developers?
1325. Are AI-generated files **marked or tagged**? Can you distinguish agent-written code from human-written code?
1326. When an agent writes a file, who **reviews it**? Another agent? A human? Nobody?
1327. What happens when **two agents write conflicting code**? Agent A creates a store, Agent B deletes it. Conflict resolution?
1328. Is there a **quality gate** for agent output? Tests must pass? TypeScript must compile? Lint must pass?
1329. Are agents **specialized**? One agent for engines, one for UI components, one for tests?
1330. What's the **agent's context window**? Can an agent see the entire codebase? Or only the files it's working on?
1331. Do agents understand **cross-file dependencies**? If an agent changes a store interface, does it update all consumers?
1332. Are agents aware of the **18 spec documents**? Do they read specs before writing code?
1333. How do you **prevent agent hallucination** in code? An agent might generate syntactically valid but logically wrong financial calculations.
1334. Is there a **human-in-the-loop** for financial calculation engines? Or are agents trusted to write accounting logic?
1335. What's the **agent's failure rate**? How often does agent-generated code need human correction?
1336. Are agents used for **code review**? Can an agent review another agent's code?
1337. Are agents used for **test generation**? Are the 473 test files agent-generated?
1338. Are agents used for **documentation generation**? Is this architecture doc agent-generated?
1339. Is the `prompt/` directory the **input** to the agents? Spec → Agent → Code pipeline?
1340. What happens when an agent **misinterprets a spec**? How is the error detected?

---

## 🔴 SECTION 103: THE DESIGN TOKEN SYSTEM — designTokens.ts

1341. `designTokens.ts` defines colors, spacing, typography. What's the data structure? An object? A Map? CSS custom properties?
1342. Are design tokens the **single source of truth** for all visual properties? Or do some components use hardcoded values?
1343. How do tokens in `designTokens.ts` connect to **Tailwind v4's `@theme`** directive? Are they synced? Manually duplicated?
1344. Are tokens **exported for use in JS** (chart colors, conditional formatting) AND **consumed by CSS** (Tailwind classes)?
1345. When a designer changes a color in the design system, how does that change propagate to the codebase?
1346. Are there **semantic tokens** (e.g., `color-success`, `color-danger`) mapped to **primitive tokens** (e.g., `green-500`, `red-500`)?
1347. Do tokens support **theming**? Light theme tokens vs dark theme tokens? Or is dark mode handled differently?
1348. Are tokens **generated from a design tool** (Figma) or manually maintained in code?
1349. Are there tokens for **spacing**, **border-radius**, **shadows**, **z-index**, **breakpoints**, **font-sizes**?
1350. Is there a **token validation** step? Ensure all required tokens exist? No orphaned tokens?

---

## 🔴 SECTION 104: THE KEYBOARD SHORTCUTS CONFIG — keyboardShortcuts.ts

1351. `keyboardShortcuts.ts` defines shortcut configurations. What's the schema? `{ key, modifiers, action, description }`?
1352. Are shortcuts **registered globally** at app startup? Or registered per-page?
1353. How are shortcuts **deregistered** when a component unmounts? Is there cleanup?
1354. Can the same shortcut do **different things in different contexts**? (E.g., Ctrl+Z = undo cell edit in grid, undo budget change on budget page.)
1355. Is there a **shortcut conflict detection** system? Prevent two actions from registering the same key combo?
1356. Can shortcuts be **disabled** for specific user roles? (E.g., Viewers can't use Ctrl+S to save because they can't edit.)
1357. Is there a **shortcut testing** system? Automated tests that simulate keypresses and verify the correct action fires?
1358. How are shortcuts **displayed in menus**? Next to menu items? In tooltips?
1359. Do shortcuts work **inside form inputs**? Or are they disabled to allow normal typing?
1360. Is there a **shortcut for undo**? What does it undo? The last store action? The last cell edit? The last navigation?

---

## 🔴 SECTION 105: THE _docs.ts — PAGE HELP DOCUMENTATION

1361. `_docs.ts` is "Page help documentation." What format is this data in? A map of route → help content?
1362. Is help content **inline** (shown in the page) or **modal** (shown in a popup)?
1363. Is help content **contextual**? Does the help for `/budgets/:id` change based on the budget's state?
1364. Is help content **searchable**? Can users search across all help topics?
1365. Is help content **internationalized**? Available in all 8 languages?
1366. Who writes the help content? Developers? Technical writers? AI agents?
1367. Is there a **"Learn more" link** in each help topic that links to external documentation?
1368. Does help content include **screenshots or GIFs**? Or is it text-only?
1369. Is there a **feedback mechanism** on help content? "Was this helpful?"
1370. Is help content **versioned** with the app? If a feature changes, does the help update?

---

## 🔴 SECTION 106: THE FORMULA FUNCTION LIBRARY — DETAILED

1371. `financial.ts` — list every financial function and its signature. Are they identical to Excel's behavior?
1372. `statistical.ts` — are statistical functions tested against Excel's output for a known dataset?
1373. `logical.ts` — `IF`, `AND`, `OR`, `NOT`, `SWITCH`, `IFS`. Does `IFS` support multiple conditions like Excel's `IFS()`?
1374. `lookup.ts` — `VLOOKUP`, `HLOOKUP`, `INDEX`, `MATCH`. Does `XLOOKUP` (modern Excel) exist?
1375. `math.ts` — `SUM`, `ROUND`, `ABS`. Does `SUMIFS` exist? Multi-criteria summation is essential for finance.
1376. `text.ts` — `CONCAT`, `LEFT`, `RIGHT`, `MID`. Are these used in financial formulas? When?
1377. `helpers.ts` — what shared helpers? Are they pure functions? Are they tested?
1378. Are there **custom financial functions** not in Excel? (E.g., `=ARR()`, `=NIM()`, `=NOI()`)
1379. Is there a **function registry** that maps function names to implementations? Can plugins add functions?
1380. How are **function arguments validated**? Number of args, types, ranges?
1381. How are **function errors** reported? `#VALUE!`, `#REF!`, `#NAME!`, `#NUM!`, `#DIV/0!`?
1382. Is there **function documentation** shown in the formula bar autocomplete? Each function's description, syntax, examples?
1383. Are functions **case-sensitive**? Does `=sum(A1:A10)` work the same as `=SUM(A1:A10)`?
1384. Is there a **function wizard** like Excel's "Insert Function" dialog?
1385. Can functions accept **ranges as arguments**? `=SUM(A1:A10)` — does the parser understand range notation?

---

## 🔴 SECTION 107: THE STORE MIGRATION SYSTEM

1386. `src/store/migration/` — what files are in this directory? What's the migration framework?
1387. How is a migration **triggered**? On app startup? On store hydration?
1388. How is the **current schema version** determined? A version number in each store's persisted data?
1389. Are migrations **sequential** (v1→v2→v3) or **direct** (v1→v3)?
1390. Are migrations **idempotent**? Running the same migration twice should not corrupt data.
1391. Are migrations **reversible**? Can you downgrade from v3 to v2?
1392. What happens if a **migration fails midway**? Is there rollback? Or is data left in a half-migrated state?
1393. Are migrations **tested**? Do tests create v1 data, run migration, verify v2 output?
1394. How are **breaking schema changes** handled? Adding a field? Removing a field? Renaming a field? Changing a field's type?
1395. Can users **export pre-migration data** as a safety measure?
1396. What's the **largest migration** in the system? Migrating 1M GL entries to a new schema?
1397. Are migrations **logged**? Can you see which migrations ran and when?
1398. What happens if the **user closes the browser mid-migration**? Is the migration atomic?
1399. Are there **migrations for every store**? Or only stores that have changed schema?
1400. Is there a **migration for the migration system itself**? (Meta-migration.)

---

## 🔴 SECTION 108: THE TYPES DIRECTORY — COMPLETE EXAMINATION

1401. `types/index.ts` is **818 lines**. That's a massive types file. Why not split into domain-specific type files?
1402. Are types **co-located with their implementation**? Or centralized in one file? Each approach has trade-offs.
1403. `cube-types.ts` — OLAP cube types. Are these shared between `CubeEngine`, `AdvancedOLAPEngine`, `MDXEngine`, and `PivotTableEngine`?
1404. `sector-types.ts` — industry-specific types. Does each sector have its own type? Or is it one polymorphic type?
1405. `plugin.ts` — plugin types. Is the plugin API stable? Is it versioned?
1406. Are there **discriminated unions** for engine results? `type FormulaResult = { success: true, value: number } | { success: false, error: string }`?
1407. Are there **branded types** for money? `type USD = number & { __brand: 'USD' }` to prevent mixing currencies?
1408. Are there **opaque types** for IDs? `type BudgetId = string & { __brand: 'BudgetId' }` to prevent passing an EntityId where a BudgetId is expected?
1409. Is `AccountType` a **string literal union** or an **enum**? TypeScript enums have specific behavior that can cause issues.
1410. Are **generic types** used for store state? `StoreState<T>` where T is the entity type?
1411. Are there types for **API responses** (from QuickBooks/Xero)? Or are external API types loosely typed?
1412. Are there types for **form data** separate from **domain models**? (Form data might have optional fields that the domain model requires.)
1413. Are there **utility types** specific to this project? `type OptionalExcept<T, K extends keyof T> = Partial<T> & Pick<T, K>`?
1414. Is there a **type for money with currency**? `{ amount: number, currency: CurrencyCode }`? Or just a bare number?
1415. Is there a **type for fiscal periods**? `{ fiscalYear: number, period: number, quarter: number }`?
1416. Are there types for **engine inputs and outputs**? Each engine having typed input/output interfaces?
1417. Are there types for **Zustand store actions**? `type BudgetActions = { createBudget: (data: CreateBudgetInput) => void }`?
1418. Are there types for **Zustand store state slices**? For partial subscriptions?
1419. Is there a **types generation** step? From JSON schema? From OpenAPI? From database schema?
1420. Are there **runtime type validators** (Zod schemas) that match the compile-time types? Or can they drift?

---

## 🔴 SECTION 109: THE STYLE FILES — ACCESSIBILITY.CSS AND PRINT.CSS

1421. `accessibility.css` — what specific styles? High contrast focus rings? Skip link styles? Reduced motion?
1422. Is `accessibility.css` a **supplement** to Tailwind's accessibility utilities or a **replacement**?
1423. Does `accessibility.css` define styles for **prefers-contrast: more** media query?
1424. Does `accessibility.css` define styles for **forced-colors** (Windows High Contrast mode)?
1425. Does `accessibility.css` handle **prefers-reduced-transparency**?
1426. Does `accessibility.css` handle **prefers-color-scheme** beyond what Tailwind does?
1427. `print.css` — what's hidden in print? Sidebar? Navbar? Modals? Toast? Command palette?
1428. Does `print.css` handle **page breaks** for financial tables? `page-break-inside: avoid` for table rows?
1429. Does `print.css` handle **landscape orientation** for wide spreadsheets?
1430. Does `print.css` include **@page** rules for margins, headers, footers?
1431. Does `print.css` handle **repeating table headers** on each page? `thead { display: table-header-group }`?
1432. Are **chart colors** adjusted for print? Some colors don't print well.
1433. Is there a **"Print Preview"** feature in the app? Or does the user rely on browser print preview?
1434. Does `print.css` handle **color-adjust** for backgrounds? Some browsers strip backgrounds in print.
1435. Does the print stylesheet produce a **professional-looking financial report**? Or just "the screen layout on paper"?

---

## 🔴 SECTION 110: THE TAURI RUST BACKEND — src-tauri/

1436. What's in `src-tauri/`? `Cargo.toml`? `main.rs`? `lib.rs`? `tauri.conf.json`?
1437. What **Tauri version**? 1.x or 2.0? Tauri 2.0 has significant API changes.
1438. What **Rust crates** are used? `tauri`, `tauri-plugin-sql`, `tauri-plugin-shell`, others?
1439. Are there **custom Tauri commands** (Rust functions callable from JS)? For what?
1440. Is **SQLite** accessed via `tauri-plugin-sql` or a custom Rust implementation?
1441. What's the **SQLite schema**? Created on first launch? How many tables?
1442. Is **data encryption at rest** in SQLite? Or is the SQLite database unencrypted on disk?
1443. Are there **Tauri plugins** beyond the standard ones? Custom Rust plugins?
1444. Is there **file system access** from Rust? For the `.finplan` file format?
1445. Is there **native dialog** access? Open file dialog? Save file dialog?
1446. Is there **system tray** integration? Minimize to tray? Tray menu?
1447. Is there **deep linking** support? Clicking a `.finplan` file opens the app?
1448. Is there **auto-update** via Tauri's built-in updater? What server hosts updates?
1449. What's the **code signing** setup? macOS notarization? Windows Authenticode?
1450. What's the **build pipeline** for Tauri? GitHub Actions? Manual?
1451. What's the **app bundle size** for macOS? Windows? Linux?
1452. What **Rust version** is required? Minimum supported Rust version (MSRV)?
1453. Are there **Rust tests**? Unit tests for Rust-side logic?
1454. Is the Rust code **unsafe** anywhere? What safety invariants are maintained?
1455. Is there a **Rust-side data validation** layer? Or does all validation happen in TypeScript?

---

## 🔴 SECTION 111: THE VITE CONFIGURATION — FULL EXAMINATION

1456. `vite.config.ts` — what's the full configuration? List every plugin, every option.
1457. What **Vite plugins** are used beyond `@vitejs/plugin-react`? PWA plugin? Compression? Visualizer?
1458. **Proxy configuration** — is there a dev server proxy for API calls? What endpoints?
1459. **Path aliases** — `@/` → `src/`. Any other aliases? Are they consistent?
1460. **Define replacements** — any `define` configuration for global constants? `process.env.NODE_ENV`?
1461. **Build target** — what's the `build.target`? `es2020`? `esnext`? This affects which JS features are transpiled.
1462. **CSS modules** — are CSS modules used anywhere? Or only Tailwind?
1463. **SVG handling** — are SVGs imported as components? As URLs? Inline?
1464. **Worker configuration** — how are Web Workers bundled? `new Worker(new URL('./worker.ts', import.meta.url))` pattern?
1465. **Environment variables** — which `VITE_*` env vars are used? How are they documented?
1466. **HMR configuration** — any custom Hot Module Replacement behavior?
1467. **Dev server configuration** — port, host, HTTPS, CORS headers?
1468. **Build sourcemaps** — enabled in dev? In production? Which type (inline, hidden, source-map)?
1469. **Chunk size warning** — what's the chunk size limit before Vite warns?
1470. **Terser/esbuild** — which minifier? Options? Dead code elimination settings?

---

## 🔴 SECTION 112: THE TSCONFIG.JSON

1471. What's the **TypeScript target**? `es2020`? `esnext`? This determines emitted JS features.
1472. What's the **module system**? `esnext`? `node16`? `bundler`?
1473. Is `strict: true` set? Which strict flags are individually configured?
1474. Are there **path mappings** beyond `@/*`? Multiple path aliases?
1475. Is `noUncheckedIndexedAccess: true`? This catches `array[0]` potentially being `undefined`.
1476. Is `exactOptionalPropertyTypes: true`? This catches `undefined` vs missing property differences.
1477. Are there **project references**? Is this a monorepo with multiple tsconfig files?
1478. What's `include` and `exclude`? Are test files included in the main compilation?
1479. Is `skipLibCheck: true`? This speeds up compilation but might miss type issues in dependencies.
1480. Are there any `compilerOptions.paths` for **barrel imports**?

---

## 🔴 SECTION 113: THE PACKAGE.JSON — DEEP EXAMINATION

1481. What's the **package name**? Version? License?
1482. List **every production dependency** and its version. Any deprecated? Any with known vulnerabilities?
1483. List **every dev dependency**. Any that should be production dependencies?
1484. Are there **peer dependencies**? Are they documented?
1485. Are there **optional dependencies**? What features do they gate?
1486. What are **all the scripts**? `dev`, `build`, `test`, `lint`, `format` — what else?
1487. Is there a `postinstall` script? What does it do?
1488. Is there a `prebuild` script? Type checking before build?
1489. Is there a `prepare` script? Husky setup?
1490. What's the **engines** field? Minimum Node.js version? npm version?
1491. Is there a **`sideEffects`** field for tree shaking?
1492. Is there a **`browserslist`** configuration? What browsers are targeted?
1493. Are there **npm overrides** for dependency version conflicts?
1494. Is there an **`.npmrc`** configuration? Registry settings? Authentication?
1495. Are there any **known security vulnerabilities** (`npm audit`)?

---

## 🔴 SECTION 114: GIT & DEVELOPMENT WORKFLOW

1496. What's the **branching strategy**? GitFlow? Trunk-based? Feature branches?
1497. What's the **PR review process**? Who reviews? Minimum approvals?
1498. Are there **protected branches**? `main`? `develop`?
1499. Are there **CI checks** on PR? What runs? Lint? Test? Build? Type check?
1500. Is there a **code ownership** file (`.github/CODEOWNERS`)? Who owns what?
1501. What's the **commit message convention**? Conventional commits? Custom format?
1502. Is there **commit linting**? `commitlint`?
1503. Are there **git hooks**? `husky`? `lefthook`? Pre-commit? Pre-push?
1504. Is there a **`.gitignore`**? What's ignored? `node_modules`? `dist`? `.env`? IndexedDB files?
1505. Is there a **`.editorconfig`**? Consistent formatting across editors?
1506. Is there a **CHANGELOG.md**? Manually maintained? Auto-generated?
1507. Is there a **CONTRIBUTING.md**? How do contributors set up the project?
1508. Is there a **CODE_OF_CONDUCT.md**?
1509. Is there a **GitHub Actions** workflow? For CI/CD? For releases? For dependency updates?
1510. Are there **Renovate/Dependabot** configurations for automated dependency updates?

---

## 🔴 SECTION 115: THE COMPONENT COMPOSITION PATTERNS

1511. Do components use **render props** anywhere? Or exclusively hooks for logic sharing?
1512. Are there **compound components** (like `<Tabs>`, `<Tabs.List>`, `<Tabs.Panel>`) from Radix?
1513. Are there **polymorphic components** (`as` prop)? `<Button as="a" href="...">`?
1514. Is there a **consistent prop naming convention**? `onX` for handlers? `isX` for booleans? `x` for values?
1515. Are components **controlled or uncontrolled**? Or both? Each form component — which pattern?
1516. Are there **slot-based composition** patterns? Named slots for header, body, footer?
1517. Is there a **`forwardRef`** pattern for components that need ref access?
1518. Are there **wrapper components** that add context (like an `ErrorBoundary` wrapper)?
1519. Is there a **component factory** pattern? Functions that return configured components?
1520. Are there **higher-order components (HOCs)** anywhere? Or has everything moved to hooks?
1521. How is **cross-cutting concern** (logging, analytics, permission checking) applied to components?
1522. Is there a **component registry** for dynamic component rendering? (E.g., rendering a widget by name from config.)
1523. Are there **component presets**? Pre-configured variants of a component? `<Button variant="danger" size="lg">`?
1524. Is **`class-variance-authority` (CVA)** used consistently for all variant definitions?
1525. Is **`tailwind-merge`** used in every component that accepts `className`? Or only some?

---

## 🔴 SECTION 116: THE EVENT SYSTEM — BEYOND ZUSTAND

1526. Is there a **custom event bus**? `EventEmitter`? `CustomEvent` on `window`?
1527. Are there **DOM custom events** for cross-component communication outside of React?
1528. Are there **Zustand `subscribe` calls** outside of React components? (In engines, workers, services?)
1529. Is there a **pub/sub system** for engine events? (E.g., "formula recalculated" event.)
1530. Are there **window event listeners** (`online`, `offline`, `resize`, `keydown`) registered globally? Where?
1531. Are there **IntersectionObserver instances** for lazy loading? Where are they created and destroyed?
1532. Are there **ResizeObserver instances** for responsive components? Where?
1533. Are there **MutationObserver instances** for DOM changes? Where?
1534. Is there a **message channel** between main thread and workers? `MessageChannel` API?
1535. Is there a **BroadcastChannel** for cross-tab communication? (Sync data between two open tabs?)

---

## 🔴 SECTION 117: THE STORE ACTION PATTERNS — GRANULAR

1536. Do store actions **return values**? Or are they void (fire and forget)?
1537. Do store actions **throw errors**? Or set error state in the store?
1538. Do store actions **validate input**? Or do they trust the caller?
1539. Do store actions **call engines directly**? Or is there a service layer between stores and engines?
1540. Do store actions **trigger persistence** automatically? Or is persistence triggered separately?
1541. Do store actions **emit events** after state changes? (For other systems to react.)
1542. Are store actions **idempotent**? Calling `createBudget` twice with the same data — what happens?
1543. Are store actions **transactional**? If one step fails, are previous steps rolled back?
1544. Are store actions **composable**? Can one action call another action in the same store? In a different store?
1545. Are store actions **testable** in isolation? Can you call `useBudgetStore.getState().createBudget(data)` in a test?
1546. Is there a **middleware layer** between components and stores? Logging? Validation? Authorization?
1547. Do store actions **update loading state**? `set({ isLoading: true })` before, `set({ isLoading: false })` after?
1548. Do store actions **handle optimistic updates**? Update UI immediately, then persist? What if persist fails?
1549. Do store actions **support batching**? Multiple state changes in one action for single re-render?
1550. Are there **async store actions**? Zustand doesn't natively support async in the `set` function. How are async operations handled?

---

## 🔴 SECTION 118: THE FINANCIAL STATEMENT LINE ITEM MAPPING

1551. How do **GL accounts** map to **financial statement line items**? Is there a mapping table?
1552. Can the mapping be **customized per entity**? Different entities might have different account structures.
1553. Is the mapping **hierarchical**? Account → Sub-account → Line Item → Section → Statement?
1554. Is the mapping **versioned**? If the mapping changes, does it affect historical financial statements?
1555. Is there a **mapping validation** step? Ensure every GL account maps to exactly one line item?
1556. Can a single GL account map to **multiple line items**? (E.g., a cost center split between departments.)
1557. Is the mapping **importable/exportable**? For use in other systems?
1558. Is there a **default mapping** per sector? A manufacturing company's CoA maps differently than a bank's.
1559. Can the mapping handle **multi-level rollups**? Account → Department → Entity → Consolidated?
1560. Is the mapping **used by the FormulaEngine**? Can formulas reference line items instead of raw accounts?

---

## 🔴 SECTION 119: THE DATA MODEL RELATIONSHIPS

1561. What's the **entity-relationship model**? How do Entity, Account, Budget, Forecast, Scenario, GL Entry, Report relate?
1562. Is there a **foreign key concept**? How are relationships enforced without a relational database?
1563. When an Entity is deleted, what happens to its **GL entries, budgets, forecasts, scenarios**?
1564. When an Account is deactivated, what happens to **GL entries and budget line items** that reference it?
1565. Can a Budget reference **multiple entities**? Or is each budget for a single entity?
1566. Can a Forecast reference **multiple budgets**? Or is each forecast derived from one budget?
1567. Can a Scenario span **multiple forecasts**? For cross-forecast comparison?
1568. How are **Report-to-Data** relationships managed? Does a report store queries or pre-computed data?
1569. Is there a **data dictionary** documenting every field, its type, and its relationships?
1570. Are there **cascading deletes**? If you delete a budget, do all associated line items, comments, tasks, and versions delete too?

---

## 🔴 SECTION 120: THE CUBE VS FLAT DATA PARADIGM

1571. When is data stored in the **OLAP cube** vs **flat in Zustand stores**?
1572. Is the cube a **derived view** of the flat data? Or is flat data a **flattened view** of the cube?
1573. When a user edits a cell in the budget grid, does the change go to the **flat store**, the **cube**, or **both**?
1574. How is **consistency** maintained between the cube and flat data?
1575. Is the cube **rebuilt** when flat data changes? Or is it incrementally updated?
1576. When does the cube get **initialized**? On first budget creation? On first OLAP query?
1577. How much **memory** does the cube consume vs flat data for the same dataset?
1578. Can a user choose to **disable the cube** if they don't need OLAP features?
1579. Are all engines **cube-aware**? Or do some engines only work with flat data?
1580. Is the cube **optional**? Can the app function without ever initializing the CubeEngine?

---

## 🔴 SECTION 121: THE VARIANCE ANALYSIS — ALGORITHM DEPTH

1581. `VarianceDecompositionEngine` — what decomposition method? Rate vs. Volume? Mix vs. Quantity?
1582. How is **price variance** calculated? `(Actual Price - Budget Price) × Actual Volume`?
1583. How is **volume variance** calculated? `(Actual Volume - Budget Volume) × Budget Price`?
1584. Is there a **mix variance**? When multiple products have different margins.
1585. Is there a **FX variance**? When results differ partly due to exchange rate changes.
1586. Is there an **acquisition/divestiture variance**? When the entity base changes mid-period.
1587. What's the **materiality threshold**? Variance below a certain % is not flagged. Is this configurable?
1588. `AutoCommentaryEngine` — generates text commentary. Is it template-based or AI-generated?
1589. What **templates** are used for auto-commentary? "Revenue increased [%] due to [driver]."?
1590. Are variance commentaries **editable** by users? Can they override AI-generated text?
1591. Is there a **variance waterfall** visualization? Showing the bridge from budget to actual?
1592. Can variance analysis be done at **multiple levels**? Entity, department, cost center, account, line item?
1593. Is there a **period-over-period** variance? Actual Q3 2024 vs Actual Q3 2023?
1594. Is there a **forecast-vs-actual** variance? In addition to budget-vs-actual?
1595. Is there a **scenario-vs-scenario** variance? Comparing optimistic to pessimistic?

---

## 🔴 SECTION 122: THE SENSITIVITY ANALYSIS — ALGORITHM

1596. `SensitivityEngine` — what's the algorithm? One-at-a-time (OAT) parameter variation?
1597. How many **input variables** can be varied simultaneously? 1? 2? N?
1598. For a **tornado chart** — how is the range for each variable determined? ±10%? ±20%? User-defined?
1599. Is there a **spider diagram** (sensitivity curves for multiple variables)?
1600. Is there a **two-way sensitivity table**? (Variable A on rows, Variable B on columns, output in cells.)
1601. What **output metric** is analyzed? Net Income? EBITDA? Cash Flow? Configurable?
1602. How are **correlated variables** handled? Revenue growth and COGS % are correlated. Is this modeled?
1603. Is the sensitivity analysis **interactive**? Can users adjust the range in real-time?
1604. How many **data points** are calculated per sensitivity analysis? For 10 variables × 20 steps = 200 calculations?
1605. Is the sensitivity analysis **run in a worker**? Or on the main thread?

---

## 🔴 SECTION 123: THE GOAL SEEK / SOLVER — ALGORITHM

1606. `GoalSeekEngine` — what algorithm? Bisection? Newton-Raphson? Secant method?
1607. What's the **convergence criterion**? Absolute tolerance? Relative tolerance? Max iterations?
1608. Can goal seek handle **non-monotonic functions**? (Where increasing the input might decrease the output.)
1609. Can goal seek work with **multiple target variables**? Or only one?
1610. `SolverEngine` — is this a linear programming solver? Nonlinear? Mixed-integer?
1611. Can the solver handle **constraints**? (E.g., "maximize revenue subject to headcount ≤ 500 and CapEx ≤ $10M.")
1612. What solver **algorithm** is implemented? Simplex? Interior point? Genetic algorithm?
1613. Can the solver work with **integer constraints**? (E.g., "headcount must be a whole number.")
1614. Is the solver **deterministic**? Same inputs always produce the same output?
1615. Is the solver run in a **worker**? Or on the main thread?

---

## 🔴 SECTION 124: THE BENCHMARKING — DATA SOURCE

1616. `BenchmarkingPage` — what benchmarks are shown? Industry averages? Peer companies?
1617. Where does **benchmark data** come from? Hardcoded? External API? User-provided?
1618. If benchmark data is **hardcoded**, when was it last updated? Is it stale?
1619. Can users **upload their own benchmark data** for comparison?
1620. Are benchmarks **sector-specific**? A SaaS company benchmarks against SaaS metrics.
1621. Are benchmarks **size-specific**? A $10M company shouldn't benchmark against a $10B company.
1622. `BenchmarkRadar` component — what dimensions does the radar chart show?
1623. Can users **select which metrics** to benchmark?
1624. Is the benchmark **comparable across currencies**? Or only same-currency comparisons?
1625. Is there a **percentile ranking**? "Your ROE is in the 75th percentile for your sector."

---

## 🔴 SECTION 125: THE DRIVER CASCADE — PROPAGATION MECHANISM

1626. `DriverCascadeEngine` — what's the cascade graph structure? DAG? Tree?
1627. When a **root driver** changes (e.g., Revenue Growth = 10%), how many levels deep does the cascade propagate?
1628. Can a driver **override** a cascaded value? (E.g., COGS is calculated as 60% of Revenue, but user manually sets it.)
1629. What happens with **circular cascades**? Driver A depends on B, B depends on A.
1630. Is the cascade **deterministic**? Same driver inputs always produce the same outputs?
1631. Can the cascade be **visualized** as a dependency graph? `DriverTreeView` shows this?
1632. Are cascaded values **marked differently** from manually entered values? (Visual distinction.)
1633. Can a user **break the cascade** for a specific cell? Override without breaking the formula?
1634. Is the cascade **recalculated incrementally** or from scratch?
1635. What's the **performance** with 100 drivers and 1000 cascaded cells?

---

## 🔴 SECTION 126: THE CUSTOM FIELD ENGINE

1636. `CustomFieldEngine` — what types of custom fields? Text, number, date, dropdown, multi-select?
1637. Can custom fields be **added to any entity type**? Budget line items? GL entries? Entities?
1638. Are custom fields **type-safe**? Or stored as `any`?
1639. Can custom fields be **used in formulas**? `=SUM(CustomField("Region", "East"))`?
1640. Can custom fields be **used in filters**? "Show me all budgets where CustomField('Priority') = 'High'"?
1641. Can custom fields be **used in reports**? As columns in financial statements?
1642. Can custom fields be **required**? Validation rules?
1643. Are custom fields **indexed for search**? Can I search across custom field values?
1644. How much **storage overhead** do custom fields add to each entity in IndexedDB?
1645. Can custom fields have **default values**? Calculated values?

---

## 🔴 SECTION 127: THE FINPLAN FILE ENGINE — NATIVE FORMAT

1646. `FinPlanFileEngine` — what's the `.finplan` file format? Binary? JSON? Protocol Buffers? MessagePack?
1647. What **data is included** in a `.finplan` file? Everything (all stores)? Or selected data?
1648. Is the file format **versioned**? Can older app versions open newer files?
1649. Is the file **compressed**? What compression? gzip? zstd?
1650. Is the file **encrypted**? What encryption? What key?
1651. Is there a **file header** with metadata? (App version, creation date, entity name.)
1652. Can a `.finplan` file be **opened by other apps**? Is it an open format?
1653. Can a `.finplan` file be **imported by another FinPlan Pro instance**? Data portability?
1654. Is there a **file size limit**? What if the data is 1GB?
1655. Is there a **file validation** step on open? Checksum? Schema validation?

---

## 🔴 SECTION 128: THE RECONCILIATION ENGINE

1656. `ReconciliationEngine` — what's being reconciled? Bank balance to book balance? Sub-ledger to GL?
1657. Is reconciliation **automatic** (matching by amount/date/reference) or **manual** (user marks items as reconciled)?
1658. What **matching algorithm** is used? Exact match? Fuzzy match? Tolerance-based?
1659. Can the engine handle **one-to-many matches**? (One bank transaction matches multiple book entries.)
1660. Can the engine handle **many-to-one matches**? (Multiple bank transactions match one book entry.)
1661. What happens with **unmatched items**? Are they flagged? Reported?
1662. Is there a **reconciliation report**? Showing matched, unmatched, outstanding items?
1663. Can reconciliation be **undone**? If a user incorrectly matches items?
1664. Is reconciliation **performed per bank account**? Each account reconciled independently?
1665. Is there an **audit trail** for reconciliation actions?

---

## 🔴 SECTION 129: THE INTERCOMPANY MATCHING — DETAILED

1666. `ICMatchingEngine` vs `IntercompanyMatchingEngine` — AGAIN, why two engines? What's the actual difference?
1667. Is matching by **entity pair**? (Entity A ↔ Entity B)
1668. Is matching by **transaction type**? (Loans, goods/services, dividends.)
1669. Is matching by **amount**? With tolerance for rounding?
1670. Is matching by **date**? With tolerance for timing differences?
1671. Is matching by **reference number**? (IC invoice number.)
1672. What happens with **unmatched IC entries**? Error? Warning? Carry forward?
1673. Is there an **IC reconciliation report**? Showing matched, unmatched, and net positions?
1674. Can IC matching handle **multi-party transactions**? (Entity A → B → C chain.)
1675. Is the IC matching **automated on import**? Or does the user trigger it?

---

## 🔴 SECTION 130: THE WORKFLOW STATE MACHINE — DETAILED

1676. `StateMachine` — what states exist? Is this generic or specific to financial workflows?
1677. What **transitions** are defined? Who can trigger each transition?
1678. Are transitions **guarded** by conditions? (E.g., "Can only approve if amount < $1M OR user is CFO.")
1679. Are **entry/exit actions** supported? (Run code when entering/exiting a state.)
1680. Is the state machine **visualized**? Can users see the state diagram?
1681. Is the state machine **persisted**? If the app crashes mid-workflow, is the state recovered?
1682. Can there be **parallel states**? A budget that's simultaneously "under review" AND "being edited"?
1683. Is there a **history mechanism**? Can the state machine go back to a previous state?
1684. Is there a **timeout mechanism**? Auto-transition after N days of inactivity?
1685. Are there **events** emitted on state transitions? For notifications, audit trail?

---

## 🔴 SECTION 131: THE APPROVAL DASHBOARD — SPECIFICS

1686. `ApprovalDashboard` component — what does it show? Pending approvals? History? Statistics?
1687. Can an approver see **all pending items** across all entities? Or only their entity?
1688. Can an approver **bulk approve** multiple items?
1689. Can an approver **delegate** approval to another user?
1690. Is there an **approval comment** requirement? Must the approver explain why they approved/rejected?
1691. Can an approval be **recalled** by the submitter after submission?
1692. Is there an **approval SLA**? Visual indicator of overdue approvals?
1693. Can the approval workflow be **customized per entity**? Different entities have different approval chains.
1694. Is there **separation of duties** enforced? The person who creates a budget cannot approve it.
1695. Are approvals **digitally signed**? Any non-repudiation mechanism?

---

## 🔴 SECTION 132: THE SANKEY DIAGRAM — FINANCIAL CONTEXT

1696. `SankeyDiagram` component — what flows does it visualize? Revenue streams? Cost flows? Cash flows?
1697. Recharts doesn't have Sankey natively. What **library or custom implementation** is used?
1698. Can the Sankey diagram show **budget allocation** flows? From total budget to departments to line items?
1699. Can the Sankey diagram show **consolidation flows**? From subsidiaries to parent?
1700. Is the Sankey diagram **interactive**? Click on a flow to drill down?
1701. Can the Sankey diagram handle **negative flows**? (E.g., returns reducing revenue.)
1702. Is the Sankey diagram **accessible**? How does a screen reader describe it?
1703. How many **nodes and links** can the Sankey handle before performance degrades?
1704. Can the Sankey diagram be **exported** as an image?
1705. Can the user **configure** which data the Sankey displays?

---

## 🔴 SECTION 133: THE WATERFALL CHART — FINANCIAL CONTEXT

1706. `WaterfallChart` component — what does it show? Revenue bridge? EBITDA bridge? Cash bridge?
1707. Is it a **floating bar chart** (each bar starts where the previous ended)?
1708. Does it handle **positive and negative values** correctly? Green bars up, red bars down?
1709. Does it show **subtotal bars**? (E.g., "Gross Profit" subtotal in a revenue waterfall.)
1710. Does it show **connector lines** between bars? `WaterfallBridge` component handles this?
1711. Can the user **customize** which items appear in the waterfall?
1712. Is the waterfall chart **responsive**? Does it work on narrow screens?
1713. Can the waterfall chart be **exported**?
1714. Is the waterfall chart **accessible**? Data table alternative?
1715. Can the waterfall chart show **multi-period comparisons** side by side?

---

## 🔴 SECTION 134: THE TORNADO CHART — SENSITIVITY

1716. `TornadoChart` component — shows sensitivity analysis results. How are bars sorted?
1717. Are bars **centered on the base case**? Positive impact to the right, negative to the left?
1718. Is the tornado chart **interactive**? Click a bar to see the detailed sensitivity curve?
1719. Can the user **adjust the sensitivity range** (±10%, ±20%, ±50%) in the UI?
1720. How many **variables** can the tornado chart display? 5? 10? 50?
1721. Is the tornado chart **color-coded**? By impact direction? By category?
1722. Can the tornado chart be **exported**?
1723. Is the tornado chart **accessible**? Data table alternative for screen readers?
1724. Can the tornado chart show **both upside and downside** scenarios simultaneously?
1725. Is the tornado chart linked to the **SensitivityEngine** or standalone?

---

## 🔴 SECTION 135: THE GAUGE CHART — KPI VISUALIZATION

1726. `GaugeChart` component — what does it show? Actual vs target? Utilization percentage?
1727. Is it a **semicircle gauge**? Full circle? Linear gauge?
1728. Does it show **threshold zones**? Red/Yellow/Green?
1729. Are thresholds **configurable**? Per KPI? Per sector?
1730. Is the gauge **animated**? Does it animate from 0 to the current value?
1731. Can the gauge show **multiple values**? Current value + target + prior period?
1732. Is the gauge **accessible**? Does it have `role="meter"` and `aria-valuenow`?
1733. Can gauges be placed in a **dashboard grid** with other components?
1734. Is the gauge **responsive**? Does it resize with its container?
1735. What **SVG library** is used for the gauge? Custom SVG? D3? A gauge-specific library?

---

## 🔴 SECTION 136: THE HEATMAP — DATA VISUALIZATION

1736. `HeatmapChart` component — what data does it visualize? Variance by month? Activity by day?
1737. `HeadcountHeatmap` — headcount by department by month. What's the color scale?
1738. `CalendarHeatmap` — activity over time (like GitHub contributions). What activity?
1739. Is the heatmap **interactive**? Hover for details? Click to drill down?
1740. What's the **color scale**? Sequential (white → blue)? Diverging (red → white → green)?
1741. Is the heatmap **accessible**? Each cell needs a text alternative.
1742. Can the heatmap handle **missing data**? Null cells? Or are all cells required?
1743. Is the heatmap **responsive**? Does it work on narrow screens?
1744. Can the user **customize the color scale**?
1745. Can the heatmap be **exported** as an image?

---

## 🔴 SECTION 137: THE SPARKLINE — MICRO VISUALIZATION

1746. `Sparkline` component — inline trend lines. Where are they used? KPI cards? Table cells?
1747. What **chart types** does the sparkline support? Line? Bar? Area?
1748. Is the sparkline **interactive**? Hover for values? Or purely decorative?
1749. Does the sparkline show **reference lines**? (E.g., budget line on an actual sparkline.)
1750. Does the sparkline show **min/max markers**? Start/end markers?
1751. What's the **size**? How many pixels wide? Tall? Configurable?
1752. Is the sparkline **accessible**? `aria-label` describing the trend?
1753. Is the sparkline **animated** on data update?
1754. Can the sparkline be **color-coded**? Green for positive trend, red for negative?
1755. What **library** renders the sparkline? Custom SVG? Recharts? A sparkline-specific library?

---

## 🔴 SECTION 138: THE TREEMAP — BUDGET VISUALIZATION

1756. `TreeMap` component — what data does it visualize? Budget allocation by department?
1757. Is it Recharts' built-in Treemap? Or a custom implementation?
1758. Is the treemap **interactive**? Click to drill down into a segment?
1759. Does the treemap handle **hierarchical data**? Budget → Department → Cost Center?
1760. What's the **color encoding**? Size = amount? Color = variance?
1761. Is the treemap **accessible**? Data table alternative?
1762. Can the treemap handle **negative values**? Treemaps typically don't support negatives well.
1763. Is the treemap **responsive**?
1764. Can the treemap be **exported**?
1765. What's the **maximum number of segments** before labels become unreadable?

---

## 🔴 SECTION 139: THE GANTT CHART — PROJECT/TIMELINE

1766. `GanttChart` component — what timelines does it show? CapEx project timelines? Budget cycles?
1767. What **library** renders the Gantt chart? Custom? Third-party?
1768. Does it show **dependencies** between tasks? (Task B can't start until Task A finishes.)
1769. Does it show **milestones**? Diamond markers for key dates?
1770. Does it show **progress**? Percentage complete per task?
1771. Is it **interactive**? Drag to reschedule? Resize tasks?
1772. Is it **scrollable** for long timelines? Virtual scrolling?
1773. Can tasks be **grouped** by project, department, or phase?
1774. Is the Gantt chart **accessible**? Table alternative for screen readers?
1775. Can the Gantt chart be **exported** to PDF?

---

## 🔴 SECTION 140: THE FUNNEL CHART — PIPELINE VISUALIZATION

1776. `FunnelChart` component — what pipeline does it show? Revenue pipeline? Approval pipeline?
1777. What **library** renders the funnel? Recharts has a basic funnel. Is it sufficient?
1778. Is the funnel **interactive**? Click a stage to see details?
1779. Does the funnel show **conversion rates** between stages?
1780. Can the funnel show **actual vs target** at each stage?
1781. Is the funnel **accessible**? Data table alternative?
1782. Can the funnel be **customized** with different stages?
1783. Is the funnel **animated** on data change?
1784. Can the funnel be **exported**?
1785. Is the funnel **responsive** on narrow screens?

---

## 🔴 SECTION 141: THE BOX PLOT — STATISTICAL VISUALIZATION

1786. `BoxPlotChart` component — what statistical data does it show? Monte Carlo results?
1787. What **library** renders the box plot? Recharts doesn't have box plots. Custom SVG?
1788. Does it show **outliers**? Individual outlier points beyond whiskers?
1789. Does it show **median, Q1, Q3, min, max** clearly labeled?
1790. Can multiple box plots be shown **side by side**? (E.g., revenue distribution by region.)
1791. Is the box plot **interactive**? Hover for exact values?
1792. Is the box plot **accessible**? Data table alternative?
1793. Can the box plot be **exported**?
1794. Is the box plot linked to the **MonteCarloEngine** output?
1795. Can the user **configure** the percentile ranges?

---

## 🔴 SECTION 142: THE BULLET CHART — PERFORMANCE VISUALIZATION

1796. `BulletChart` component — what does it show? Actual vs target vs threshold?
1797. What **library** renders the bullet chart? Custom? Third-party?
1798. Does it show **qualitative ranges**? (Poor, satisfactory, good.)
1799. Does it show **target markers**? A vertical line at the target value?
1800. Is the bullet chart **interactive**? Hover for details?
1801. Is the bullet chart **accessible**? `role="meter"` with `aria-valuenow`?
1802. Can bullet charts be shown **in a table**? (One bullet chart per row for each KPI.)
1803. Can the bullet chart be **exported**?
1804. Is the bullet chart **responsive**?
1805. Can the ranges and targets be **configured per KPI**?

---

## 🔴 SECTION 143: THE COMBO CHART — MULTI-SERIES

1806. `ComboChart` component — bar + line combination. What use case?
1807. Is it Recharts' `ComposedChart`? Or custom?
1808. Can it show **multiple Y-axes**? (Revenue on left axis, margin % on right axis.)
1809. Can it show **different chart types per series**? Bars for revenue, line for margin, area for cumulative?
1810. Is the combo chart **interactive**? Shared tooltip across all series?
1811. Is the combo chart **accessible**? Data table alternative?
1812. Can the combo chart be **exported**?
1813. Is the combo chart **responsive**?
1814. Can the user **toggle series** on/off via legend?
1815. Can the combo chart handle **different scales** for different series? (Revenue in millions, margin in percentage.)

---

## 🔴 SECTION 144: THE SCATTER PLOT — ANALYTICS

1816. `ScatterPlot` component — what data pairs does it visualize? Revenue vs. headcount? Cost vs. output?
1817. Is it Recharts' `ScatterChart`? Or custom?
1818. Can it show **trend lines**? Linear regression? Polynomial?
1819. Can it show **multiple series**? Different colors for different departments?
1820. Is the scatter plot **interactive**? Click a point to see details? Brush to zoom?
1821. Can it show **bubble sizes**? (Third dimension — e.g., budget amount.)
1822. Is the scatter plot **accessible**? Data table alternative?
1823. Can the scatter plot be **exported**?
1824. Is the scatter plot **responsive**?
1825. What's the **maximum number of points** before performance degrades? 10,000? 100,000?

---

## 🔴 SECTION 145: THE CASH FLOW WATERFALL — SPECIFIC

1826. `CashFlowWaterfallEngine` — how does it differ from the generic `WaterfallChart`?
1827. Does it show **operating, investing, financing** sections?
1828. Does it show **beginning cash → adjustments → ending cash** bridge?
1829. Is it linked to the **ThreeStatementEngine** cash flow output?
1830. Can it handle **multi-period** waterfall? (Q1→Q2→Q3→Q4 cash bridge.)
1831. Is it **interactive**? Click on a bar to see the underlying GL entries?
1832. Is it **accessible**? Data table alternative?
1833. Can it be **exported** to PDF?
1834. Does it handle **negative cash flows** correctly? (E.g., CapEx as a reduction.)
1835. Can it be **customized**? Add/remove waterfall items?

---

## 🔴 SECTION 146: THE CONDITIONAL FORMATTING SYSTEM

1836. `ConditionalFormattingEngine` — what rule types? Greater than, less than, between, text contains?
1837. Can rules apply to **individual cells**, **entire rows**, **entire columns**?
1838. Can rules use **formulas**? (E.g., "highlight if this cell > the average of the column.")
1839. Can multiple rules apply to the **same cell**? What's the priority order?
1840. Can formatting include **data bars** (in-cell bar charts), **color scales**, **icon sets** (▲ ▼)?
1841. `ConditionalFormattingPanel` — is this a UI for creating/editing rules?
1842. `ConditionalFormattingRenderers` — how are rules rendered in the grid? Custom cell renderer?
1843. Is conditional formatting **persisted** with the budget? Or session-only?
1844. Can conditional formatting rules be **shared** between users?
1845. Is conditional formatting **performant** with 10,000 cells × 5 rules?

---

## 🔴 SECTION 147: THE DRAG-FILL SYSTEM

1846. `DragFillEngine` — what patterns does it recognize? Linear series? Growth series? Date series?
1847. Does drag-fill work with **formulas**? Does it adjust relative references? (=A1+10 → =A2+10)
1848. Does drag-fill work with **mixed references**? ($A1 vs A$1 vs $A$1)
1849. `DragFill` component — is there a visual **fill handle** (small square at cell corner)?
1850. Does drag-fill work **across columns** (right) AND **across rows** (down)?
1851. Can drag-fill **skip cells**? (Fill every other cell.)
1852. What happens with **protected cells**? `CellProtectionEngine` blocks fill into locked cells?
1853. What happens with **validated cells**? `CellValidationEngine` rejects fill values that fail validation?
1854. Is drag-fill **undoable**? One undo undoes the entire fill operation?
1855. What's the **performance** of drag-filling 1,000 cells?

---

## 🔴 SECTION 148: THE CELL COMMENT SYSTEM

1856. `CellCommentEngine` — where are comments stored? In the store? In IndexedDB? Both?
1857. Can comments contain **rich text**? Bold, italic, links?
1858. Can comments **@mention** other users? Do mentions trigger notifications?
1859. Can comments be **threaded**? Reply to a comment?
1860. Can comments be **resolved**? Mark as addressed?
1861. `CellCommentPanel` — is this a sidebar panel showing comments for the current cell?
1862. Are comments **visible as indicators** in the cell? (Small triangle in corner.)
1863. Can comments be **searched**? Across all cells, all budgets?
1864. Are comments **included in exports**? (PDF footnotes? Excel cell notes?)
1865. What happens to comments when a **row or column is deleted**?

---

## 🔴 SECTION 149: THE DOCUMENT ENGINE — ATTACHMENTS

1866. `DocumentEngine` — what types of documents can be attached? PDFs? Images? Excel files?
1867. Where are documents stored? In **IndexedDB** as BLOBs? File system (Tauri)?
1868. Is there a **file size limit** per attachment? Total storage limit?
1869. Can documents be **previewed** in the app? PDF viewer? Image viewer?
1870. Can documents be **searched**? Full-text search inside PDFs?
1871. Are documents **versioned**? If a document is replaced, is the old version kept?
1872. Are documents **access-controlled**? Can specific users be restricted from seeing attachments?
1873. Are documents **included in backups**? They could be very large.
1874. Can documents be **linked to specific cells**? Or only to budgets/reports?
1875. Is there a **document management UI**? List, search, filter, preview?

---

## 🔴 SECTION 150: THE ENCRYPTION ENGINE — DETAILED

1876. `EncryptionEngine` — what data is encrypted? All data? Only sensitive fields?
1877. What **encryption algorithm**? AES-256-GCM? ChaCha20-Poly1305?
1878. What **key derivation function**? PBKDF2? Argon2id? scrypt?
1879. Where is the **encryption key** derived from? User password? Hardware key? Device-specific?
1880. Is the key **in memory** during app usage? How is it protected?
1881. Is **encryption transparent** to the engines? Do engines know they're working with encrypted data?
1882. Is encryption applied at the **store level** (entire store encrypted) or **field level** (individual fields)?
1883. What happens when the user **changes their password**? Is all data re-encrypted?
1884. Is there a **key escrow** mechanism? If the user forgets their password, can data be recovered?
1885. Is encryption **optional**? Can a user choose not to encrypt?
1886. What's the **performance impact** of encrypting/decrypting on every read/write?
1887. Is the **encryption implementation audited**? Any third-party security review?
1888. Are **encryption keys rotated**? How often? Is rotation automated?
1889. Is there **end-to-end encryption** for collaboration data? Or only at-rest encryption?
1890. Does encryption affect **IndexedDB storage size**? Encrypted data is typically larger.

---

## 🔴 SECTION 151: THE DATA GOVERNANCE ENGINE — DETAILED

1891. `DataGovernanceEngine` — what governance rules are defined? Data quality? Access control? Retention?
1892. How are governance rules **enforced**? Automatically? Manually? Both?
1893. Can governance rules be **customized per entity**? Different entities have different governance requirements.
1894. Is there a **governance dashboard**? Showing compliance status across all data domains?
1895. Can governance rules **block operations**? (E.g., "cannot import data without quality validation.")
1896. Is there a **governance audit trail**? Every governance action logged?
1897. Can governance rules be **exported** for external review?
1898. Is there a **data steward assignment** per data domain?
1899. Can governance rules **trigger notifications**? (E.g., "data quality below threshold.")
1900. Is there a **governance maturity model**? How mature is the governance implementation?

---

## 🔴 SECTION 152: THE DATA RETENTION ENGINE — DETAILED

1901. `DataRetentionEngine` — what retention policies are defined? By data type? By entity? By regulation?
1902. How are retention policies **enforced**? Automatic deletion? Manual review before deletion?
1903. What happens when retention expires? **Hard delete** (permanent) or **soft delete** (recoverable)?
1904. Can retention be **extended** for legal hold? Litigation hold?
1905. Is there a **retention dashboard**? Showing what data is due for deletion?
1906. Can retention policies **differ by jurisdiction**? EU retention vs US retention?
1907. Is the deletion **secure**? Can deleted data be recovered from IndexedDB/SQLite?
1908. Is there a **deletion audit trail**? Who deleted what, when, and under which policy?
1909. Can retention policies be **overridden** per data item?
1910. What's the **minimum retention** for financial data? SOX says 7 years. Does the engine enforce this?

---

## 🔴 SECTION 153: THE DATA CLASSIFICATION ENGINE

1911. `DataClassificationEngine` — what classification levels? Public, Internal, Confidential, Restricted?
1912. How is data **classified**? Automatically (based on content)? Manually (user assigns)?
1913. What happens **after classification**? Does confidential data get additional encryption? Access restrictions?
1914. Is classification **visible to users**? Color-coded labels? Badges?
1915. Can classification be **changed**? Who can upgrade/downgrade a classification?
1916. Is there a **classification dashboard**? Showing data distribution by classification level?
1917. Does classification affect **exports**? Confidential data excluded from certain exports?
1918. Does classification affect **search results**? Classified data excluded from general search?
1919. Is classification **used by the DataMaskingEngine**? Higher classification = more aggressive masking?
1920. Is there a **classification policy** document? Defining what each level means?

---

## 🔴 SECTION 154: THE DATA MASKING ENGINE — DETAILED

1921. `DataMaskingEngine` — what masking strategies? Full mask (****), partial mask (J*** Doe), hash, tokenize?
1922. Is masking **applied at the engine layer** (data never reaches UI unmasked) or **UI layer** (data masked for display)?
1923. If masking is at the UI layer, can a developer access unmasked data via browser devtools?
1924. Is masking **role-based**? CFO sees full data, analyst sees masked data?
1925. Is masking **reversible**? Can an authorized user unmask?
1926. Is masking applied to **specific fields only**? (Salary, SSN, bank account.) Or entire records?
1927. Is masking applied to **exports**? Masked data in PDF/Excel exports?
1928. Is masking applied to **charts**? Can charts show masked data meaningfully?
1929. Is masking **configurable** by administrators? Which fields are masked and how?
1930. Is there a **masking audit trail**? Who unmasked what, when?

---

## 🔴 SECTION 155: THE DATA LINEAGE ENGINE

1931. `DataLineageEngine` — what does it track? Source → Transformation → Destination?
1932. How far back does lineage go? Import file → GL entry → Budget → Report → PDF?
1933. Is lineage **stored per cell**? Or per data entity (budget, GL entry)?
1934. Can users **view lineage** for a specific value? "This number came from cell B5 in the imported Excel file."
1935. `DataLineageViewer` component — what does it show? A graph? A table? A tree?
1936. Is lineage **searchable**? "Show me all values derived from the Q3 import."
1937. Is lineage **included in exports**? Footnotes showing data provenance?
1938. Is lineage **used for impact analysis**? "If I change this account, which reports are affected?"
1939. Is lineage **versioned**? Can you see lineage at a historical point in time?
1940. Is lineage **performant**? Tracking lineage for 1M GL entries — storage and query performance?

---

## 🔴 SECTION 156: THE DATA CATALOG ENGINE

1941. `DataCatalogEngine` — what metadata is cataloged? Field name, type, description, owner, sensitivity, source?
1942. Is the catalog **auto-generated** from code (types) or **manually maintained**?
1943. Is the catalog **searchable**? Can users find "all fields related to revenue"?
1944. Is there a **catalog UI**? A data catalog page where users browse metadata?
1945. Can users **add annotations** to catalog entries? Business definitions, usage notes?
1946. Is the catalog **exported** for use in external data governance tools?
1947. Does the catalog include **data quality metrics**? Completeness, accuracy scores?
1948. Does the catalog include **usage statistics**? Which fields are most referenced in formulas?
1949. Does the catalog include **data ownership**? Who is responsible for each data domain?
1950. Is the catalog **integrated with the search system**? Search across field names AND descriptions?

---

## 🔴 SECTION 157: THE MASTER DATA ENGINE

1951. `MasterDataEngine` — what constitutes "master data"? Chart of accounts? Entity list? Cost centers? Employees?
1952. Is there a **golden record** for each master data type? The authoritative version?
1953. How is master data **synchronized** across entities? Same CoA for all entities? Different?
1954. Can master data be **imported** from external systems? (QuickBooks CoA imported as master data.)
1955. Can master data have **effective dates**? An account active from Jan 1, inactive from Jul 1.
1956. Is there a **master data change workflow**? Request → Review → Approve → Publish?
1957. Is master data **versioned**? Can you see the CoA at a historical point in time?
1958. Can master data be **hierarchical**? Accounts roll up into groups, groups into sections.
1959. Is there a **master data quality score**? Completeness, accuracy, timeliness?
1960. Can master data be **exported** for use in other systems?

---

## 🔴 SECTION 158: THE ETL PIPELINE ENGINE

1961. `ETLPipelineEngine` — what's the Extract, Transform, Load pipeline?
1962. What **data sources** are supported? Excel, CSV, API, database?
1963. What **transformations** are available? Mapping, filtering, aggregation, calculation, cleansing?
1964. Is the ETL pipeline **visual**? A drag-and-drop pipeline builder?
1965. Can ETL pipelines be **saved and reused**? As templates?
1966. Can ETL pipelines be **scheduled**? Run every night at midnight? (But there's no server.)
1967. Is ETL **incremental**? Only process new/changed data? Or full reload every time?
1968. What's the **ETL error handling**? Skip bad records? Halt on error? Log and continue?
1969. Is there an **ETL log**? Showing each step's execution status, rows processed, errors?
1970. Can ETL handle **very large datasets**? 10M rows? Streaming processing?

---

## 🔴 SECTION 159: THE MIGRATION ENGINE

1971. `MigrationEngine` — migrating from what? Excel? Adaptive Insights? Hyperion? Anaplan?
1972. What **migration steps** are involved? Export from source → Transform → Import into FinPlan Pro?
1973. Is there a **migration wizard** UI? Step-by-step guidance?
1974. Can migration handle **different data models**? Source system has different CoA structure.
1975. Is there **data mapping** between source and target? User-configurable?
1976. Is migration **reversible**? Can you undo a migration?
1977. Is there a **migration preview**? Show what will be imported before committing.
1978. Is there **migration validation**? Verify imported data is correct and complete.
1979. Is there a **migration report**? Showing what was imported, what failed, what was skipped.
1980. Can migrations be **incremental**? Import new data without duplicating existing data?

---

## 🔴 SECTION 160: THE STREAM IMPORT ENGINE

1981. `StreamImportEngine` — what's the streaming strategy? Line-by-line? Chunk-based?
1982. What's the **chunk size**? 1,000 rows? 10,000 rows?
1983. Does streaming work in a **Web Worker**? Or on the main thread?
1984. Is there **backpressure** handling? If processing is slow, does the stream pause?
1985. What's the **progress reporting** during streaming? Rows processed? Percentage?
1986. Can streaming be **paused and resumed**? If the user navigates away?
1987. What happens on **error during streaming**? At row 500,000 of 1,000,000. Are the first 499,999 preserved?
1988. Is streaming **memory-efficient**? Does it process and discard, or accumulate all in memory?
1989. Can streaming handle **different file formats**? Or is it Excel-specific?
1990. What's the **maximum file size** tested? 100MB? 1GB?

---

## 🔴 SECTION 161: THE SMART IMPORT MAPPER

1991. `SmartImportMapper` — how does it detect column mappings? Header text matching? Data pattern matching?
1992. Is it **AI-powered**? Using what model? Local inference or API call?
1993. What's the **accuracy rate**? 90%? 99%? What are common mis-mappings?
1994. Can the user **correct and retrain** the mapper? Does it learn from corrections?
1995. Does it handle **ambiguous headers**? "Amount" could be debit, credit, or net. How is disambiguated?
1996. Does it handle **multi-row headers**? Finance Excel files often have headers in rows 1-3.
1997. Does it handle **headers in different languages**? "Montant" (French for amount).
1998. Is the mapping **stored for reuse**? Next time the same file format is imported.
1999. Can the user **save a mapping template**? For recurring imports.
2000. Does it handle **merged cells in headers**? Excel finance files love merged cells.

---

## 🔴 SECTION 162: THE AUTO-SAVE ENGINE

2001. `AutoSaveEngine` — what's the save interval? 5 seconds? 30 seconds? 5 minutes?
2002. Is the interval **configurable** by the user?
2003. Does auto-save save **all stores** simultaneously? Or only changed stores?
2004. How does auto-save detect **what changed**? Dirty flags? Diffing?
2005. Does auto-save **debounce** rapid changes? (Don't save on every keystroke.)
2006. Is there a **visual indicator** of save status? "Saving..." → "All changes saved"?
2007. What happens if auto-save **fails**? Is the user notified? Are changes preserved in memory?
2008. Does auto-save **conflict with manual save**? If the user presses Ctrl+S while auto-save is running.
2009. Is auto-save **disabled during certain operations**? During import? During engine computation?
2010. Does auto-save create **save points** that can be reverted to? Or is it purely overwriting?

---

## 🔴 SECTION 163: THE CRASH RECOVERY ENGINE

2011. `CrashRecoveryEngine` — how does it detect a crash vs. a clean close?
2012. What data is stored for recovery? Full store snapshots? Incremental diffs?
2013. Where is recovery data stored? Separate IndexedDB database? Session storage?
2014. How often is recovery data **snapshot**? Every auto-save? Every state change?
2015. On recovery, does the user **choose** which recovery point to restore? Or is it automatic?
2016. Can the user **discard** recovery data and start fresh?
2017. How large can recovery data get? With 100MB of financial data, recovery could be expensive.
2018. Is recovery data **cleared on clean close**? So it doesn't accumulate.
2019. Does crash recovery handle **partial writes**? If IndexedDB write was interrupted.
2020. Has crash recovery been **tested**? Simulated crashes? Power loss scenarios?

---

## 🔴 SECTION 164: THE SESSION ENGINE

2021. `SessionEngine` — what constitutes a "session"? Login to logout? Browser open to close?
2022. Is session data stored in **session storage** (lost on tab close) or **IndexedDB** (persists)?
2023. What **session metadata** is tracked? Start time, last activity, entity, page, user actions?
2024. Is there a **session timeout**? How long? Configurable?
2025. What happens on **session timeout**? Redirect to login? Warning modal first?
2026. Can there be **multiple sessions** for the same user? (Multiple tabs.)
2027. Is session data **used for analytics**? (Time spent on each page, most used features.)
2028. Is session data **cleared on logout**? Including any temporary data?
2029. Does the session engine coordinate with the **authStore**? Token refresh, permissions.
2030. Is there a **session history**? Can the user see their past sessions?

---

## 🔴 SECTION 165: THE RECENT FILES ENGINE

2031. `RecentFilesEngine` — what constitutes a "recent file"? A `.finplan` file? A budget? A report?
2032. How many recent items are tracked? 10? 20? 50?
2033. Is the recent list **stored in IndexedDB**? Persists across sessions?
2034. Can the user **pin** a recent item? So it's always at the top.
2035. Can the user **remove** an item from the recent list?
2036. Does the recent list show **file path or budget name**?
2037. Is the recent list **searchable**?
2038. Is the recent list **sorted by last opened date**?
2039. Is the recent list available in the **sidebar**? Navbar? Command palette?
2040. What happens when a **recently opened file is deleted**? Is it removed from the list?

---

## 🔴 SECTION 166: THE UNDO/REDO ENGINE — DETAILED

2041. `UndoRedoEngine` — is it a **generic engine** used by multiple stores? Or store-specific?
2042. What's the **command pattern**? Each undoable action is a Command object with `execute()` and `undo()`?
2043. What's the **undo stack implementation**? Array? Linked list? Circular buffer?
2044. What's the **max stack size**? Memory-bounded? Entry-count-bounded?
2045. Are **compound actions** supported? (Multiple changes as one undo step.)
2046. Are **asynchronous actions** undoable? (Undo an import that took 10 seconds.)
2047. Is undo/redo **coordinated across stores**? Undo budget change → undo associated GL references?
2048. Is the undo stack **persisted**? Survives page refresh?
2049. Is there an **undo history UI**? Showing the list of undoable actions?
2050. Are undo/redo **exposed as keyboard shortcuts**? Ctrl+Z, Ctrl+Y, Ctrl+Shift+Z?

---

## 🔴 SECTION 167: THE VERSION CONTROL ENGINE

2051. `VersionControlEngine` — is this Git-like version control for financial data?
2052. What **versioning model**? Linear history? Branching? Merging?
2053. How are versions **stored**? Full copies? Deltas? Combination?
2054. Can users **compare** two versions side-by-side?
2055. Can users **merge** two versions? What about conflicts (same cell different values)?
2056. Can users **tag** a version? (e.g., "Submitted to Board Q3 2024")
2057. Can users **revert** to a previous version?
2058. Is there a **version limit**? How many versions before storage becomes a problem?
2059. Is version control **per budget**? Per forecast? Per scenario? Global?
2060. Is there a **version log**? Showing who created each version and when?

---

## 🔴 SECTION 168: THE PERIOD CLOSE ENGINE

2061. `PeriodCloseEngine` — what's the close process? Checklist-based? Automated?
2062. What's on the **close checklist**? Reconciliation complete? Adjustments posted? Reviews done?
2063. Can the close be **partial**? Close revenue but not expenses?
2064. What happens when a period is **closed**? Can data still be viewed? Can it be modified?
2065. Can a closed period be **reopened**? Who has permission?
2066. Is the close **per entity**? Or global across all entities?
2067. Is there a **close calendar**? Showing planned close dates for each period.
2068. Is there a **close dashboard**? Showing status across entities?
2069. Is the close **audited**? Close actions logged in the audit trail?
2070. Can the close **trigger notifications**? "Entity A closed Period 3."

---

## 🔴 SECTION 169: THE FISCAL CALENDAR ENGINE — DETAILED

2071. `FiscalCalendar` — supports Standard, 4-4-5, 4-5-4, 13-period. Are there other variants?
2072. For 4-4-5, the **53rd week** occurs periodically. How is it handled?
2073. For **leap years** in standard fiscal calendar, does February have 28 or 29 days?
2074. Can the fiscal calendar be **customized per entity**? Different entities, different calendars.
2075. Can the fiscal calendar **change mid-year**? (Extremely rare but possible in corporate restructuring.)
2076. Is the fiscal calendar **used consistently** across ALL engines? Or does each engine calculate periods independently?
2077. Is there a **fiscal period picker** component? Instead of a standard date picker.
2078. Can the fiscal calendar be **exported** for use in other systems?
2079. Is the fiscal calendar **included in the data model**? Every date-based entity has a fiscal period reference?
2080. Is the fiscal calendar **validated**? Ensure periods don't overlap and cover the full year.

---

## 🔴 SECTION 170: THE EXCEL KEYBOARD ENGINE

2081. `ExcelKeyboardEngine` — what Excel shortcuts are supported? F2 to edit? Enter to confirm? Tab to move?
2082. Does it support **Ctrl+Arrow** to jump to the edge of data?
2083. Does it support **Ctrl+Shift+Arrow** to select to the edge of data?
2084. Does it support **Ctrl+Home** to go to cell A1?
2085. Does it support **Ctrl+End** to go to the last used cell?
2086. Does it support **Ctrl+Page Up/Down** to switch between sheet tabs?
2087. Does it support **Alt+=** for auto-sum?
2088. Does it support **Ctrl+;** for current date? **Ctrl+Shift+;** for current time?
2089. Does it support **Ctrl+D** to fill down? **Ctrl+R** to fill right?
2090. Does it support **F4** to toggle absolute/relative reference in formulas?
2091. Does it support **Ctrl+`** to toggle formula view (show formulas instead of values)?
2092. Does it support **Ctrl+Shift+L** for filter toggle?
2093. `ExcelKeyboardShortcuts` — is this a separate file from `keyboardShortcuts.ts`? Why two files?
2094. Do Excel shortcuts **conflict with app-level shortcuts**? How are conflicts resolved?
2095. Can users **switch between Excel and standard keyboard modes**?

---

## 🔴 SECTION 171: THE GROUP OUTLINE ENGINE

2096. `GroupOutlineEngine` — row/column grouping like Excel's outline feature?
2097. Can groups be **nested**? Group within group (department within division)?
2098. Are groups **expandable/collapsible**? With +/- buttons?
2099. Are groups **persisted** with the budget? Or session-only?
2100. Can groups be **auto-generated** from the account hierarchy?
2101. Can groups be **manually created** by the user?
2102. Do groups affect **calculations**? Sum of the group = sum of visible + hidden rows?
2103. Can groups be **exported** to Excel with outline levels preserved?
2104. Is there a **maximum group depth**?
2105. Can groups be **printed** with collapse state preserved?

---

## 🔴 SECTION 172: THE CHART ANNOTATION ENGINE

2106. `ChartAnnotationEngine` — what annotation types? Text? Lines? Regions? Points?
2107. Are annotations **persisted**? Do they survive page refresh?
2108. Can annotations be **per-user**? Each user sees their own annotations?
2109. Can annotations be **shared**? Visible to all users?
2110. Can annotations be **locked**? Prevent modification by others?
2111. Are annotations **interactive**? Click to edit? Drag to move?
2112. Are annotations **exported** with the chart?
2113. Can annotations be **styled**? Color, font, size?
2114. Can annotations reference **specific data points**? Linked to a data value that updates.
2115. Is there a **list view** of all annotations? For review and management.

---

## 🔴 SECTION 173: THE DRILL-THROUGH ENGINE

2116. `DrillThroughEngine` — what drill-through paths are available? KPI → Detail → GL Entry?
2117. Are drill-through paths **configurable**? By the user? By the admin?
2118. Are drill-through paths **context-sensitive**? Different drill targets based on the cell's data type.
2119. Does drill-through **pass filters** to the target page? (e.g., drill from Q3 revenue → filtered GL showing Q3 revenue entries.)
2120. Does drill-through **open in a new tab**? Or replace the current page?
2121. Can drill-through go **across data types**? Budget cell → related GL entries → source document?
2122. Is there a **drill-through breadcrumb**? Showing the path back to the original view.
2123. Can drill-through be **configured per grid**? Different grids, different drill targets.
2124. Is drill-through **available in exported reports**? Interactive PDFs?
2125. Is drill-through **performant**? Does it require loading a full page or just a modal?

---

## 🔴 SECTION 174: THE GRID OFFLINE ENGINE

2126. `GridOfflineEngine` — what does "offline grid operations" mean?
2127. Does it **queue grid operations** when offline and replay when online?
2128. Does it handle **offline formula evaluation**? Formulas evaluated locally, no server needed.
2129. Does it handle **offline data persistence**? Grid changes saved to IndexedDB.
2130. Does it handle **offline conflict detection**? Marking cells that may have conflicts.
2131. Does it provide **offline indicators**? Per-cell? Per-grid? Per-page?
2132. Does it handle **offline exports**? Generate PDFs without server.
2133. Does it handle **offline collaboration**? Queue comments and tasks for later sync.
2134. Is the offline engine **aware of the network status**? Using `useOffline` hook?
2135. What's the **sync strategy** when coming back online? Full sync? Incremental? Conflict resolution?

---

## 🔴 SECTION 175: THE PLUGIN ENGINE — DETAILED

2136. `PluginEngine` — what's the **plugin lifecycle**? Install → Configure → Activate → Use → Deactivate → Uninstall?
2137. How are plugins **packaged**? NPM packages? Custom format? ZIP file?
2138. How are plugins **loaded**? Dynamic import? Eval? Sandboxed iframe?
2139. What **API surface** is exposed to plugins? Can they access stores? Engines? Components?
2140. Can plugins **register new routes**? New pages?
2141. Can plugins **register new stores**? New state management?
2142. Can plugins **register new hooks**?
2143. Can plugins **add middleware** to stores?
2144. Can plugins **intercept user actions**? (Before/after hooks.)
2145. What's the **plugin isolation** model? Can a plugin crash the app?
2146. Is there a **plugin permission model**? Plugin requests access to "budget data" but not "user data."
2147. Is there a **plugin update mechanism**? How do plugins get updated?
2148. Is there a **plugin compatibility matrix**? Plugin X requires FinPlan Pro v2.0+.
2149. Is there a **plugin debugging** tool? Console? Inspector?
2150. Is there a **plugin marketplace**? How are plugins discovered and installed?

---

## 🔴 SECTION 176: THE CONNECTOR ENGINE

2151. `ConnectorEngine` — manages external data connectors. What's the connector lifecycle?
2152. What **authentication methods** do connectors support? OAuth 2.0? API keys? Basic auth?
2153. Is there a **connector configuration UI**? For entering credentials, endpoints, mappings.
2154. Can connectors **sync bidirectionally**? Read from and write to external systems?
2155. Is there a **connector health check**? Detect when the external API is down.
2156. Is there a **connector rate limiter**? Respect external API rate limits.
2157. Is there a **connector retry logic**? Exponential backoff on failures.
2158. Is there a **connector log**? Showing sync history, errors, data volumes.
2159. Can connectors be **scheduled**? Automatic sync at specific times.
2160. Is there a **connector conflict resolution**? When external data conflicts with local data.

---

## 🔴 SECTION 177: THE BATCH OPERATION ENGINE

2161. `BatchOperationEngine` — what operations can be batched? Create, update, delete?
2162. Is there a **batch size limit**? What happens when the batch is too large?
2163. Are batch operations **atomic**? All succeed or all fail?
2164. Are batch operations **progressive**? Show progress as items are processed.
2165. Can batch operations be **cancelled** mid-way?
2166. Are batch operations **undoable**? One undo reverts the entire batch.
2167. Are batch operations **performant**? Processing 10,000 items without UI freeze.
2168. Do batch operations run in a **Web Worker**?
2169. Are batch operations **audited**? Each item's change logged?
2170. Is there a **batch operation history**? What was batched, when, by whom.

---

## 🔴 SECTION 178: THE INCREMENTAL CALC ENGINE

2171. `IncrementalCalcEngine` — how does it know which cells are **dirty**?
2172. Is there a **dependency graph** that tracks which cells depend on which?
2173. When cell A1 changes, does it only recalculate **A1's dependents**? Not the entire sheet?
2174. What's the **dirty propagation strategy**? Immediate? Batched? Deferred?
2175. How does it interact with `CalculationGraph`? Are they the same? Overlapping?
2176. What happens when the dependency graph **changes** (formula is edited)? Is the graph rebuilt?
2177. Is incremental calculation **correct**? Does it miss any dependencies?
2178. What's the **performance gain** vs full recalculation? For 10,000 cells, changing 1?
2179. Is incremental calculation **compatible with circular references**?
2180. Is the incremental calculation engine **tested with complex dependency chains**?

---

## 🔴 SECTION 179: THE ITERATIVE CALCULATION ENGINE

2181. `IterativeCalculationEngine` — what algorithm for circular reference resolution?
2182. What's the **maximum iteration count**? Configurable?
2183. What's the **convergence threshold**? 0.001? 0.0001? Configurable?
2184. Does it support **Gauss-Seidel iteration**? (Update cells in-place during iteration.)
2185. Does it support **Jacobi iteration**? (Update all cells from previous iteration's values.)
2186. What happens when **convergence fails**? After max iterations, what's shown?
2187. Is there a **CircularReferenceWarning** shown to the user? How prominent?
1188. Can the user **toggle iterative calculation** on/off? Like Excel's iterative calculation setting.
2189. Are circular references **detected proactively**? Before they cause issues?
2190. Is the circular reference **path shown** to the user? (A1 → B1 → C1 → A1.)

---

## 🔴 SECTION 180: THE SIGN CONVENTION ENGINE

2191. `SignConventionEngine` — what sign conventions are defined? Credit-positive for revenue? Debit-positive for assets?
2192. Is the sign convention **configurable per account type**? Assets positive, contra-assets negative.
2193. Does the engine **automatically flip signs** when data flows between systems? (GL → Budget → Report)
2194. Is the sign convention **documented**? Can users see the convention for each account?
2195. Does the engine handle **mixed sign conventions** from imported data? (Different ERP systems have different conventions.)
2196. Does the sign convention affect **aggregation**? Sum of positive and negative values.
2197. Does the sign convention affect **display**? Negative values shown in parentheses? Red?
2198. Does the sign convention affect **variance calculation**? Positive vs negative variance interpretation.
2199. Is the sign convention **tested**? Especially for multi-entity with different source systems.
2200. Is the sign convention engine **used by all engines** consistently? Or do some engines bypass it?

