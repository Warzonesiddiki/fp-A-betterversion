// Re-export Button from parent directory.
// TourOverlay.tsx imports './ui/Button' but the correct path is '../Button'.
// This stub makes the import resolve correctly.
export { Button } from '../Button';
