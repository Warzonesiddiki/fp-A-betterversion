# PERSISTENCE MAP (W0.8.1)

> Generated by `scripts/persistence-map-check.mjs`. **Do not hand-edit.**
> Source of truth: `src/domain/persistenceAuthority.ts`.
> Drift against `persist()` keys in `src/store` fails CI (`npm run persistence:map`).

**Measured 2026-08-20.** 41 persisted Zustand stores.

| Classification  | Count | Authority rule                                              |
| --------------- | ----- | ----------------------------------------------------------- |
| financial-truth | 30    | Server must be SoR. Today **30 local-draft**, **0 server**. |
| user-preference | 7     | Local is legitimate.                                        |
| derived-cache   | 3     | Rebuildable; never the system of record (ST2).              |
| session         | 1     | Tokens; not financial truth.                                |

**Honesty (R-21).** Clearing site data destroys every `local-draft` financial-truth store.
No backup, RTO, or audit claim is valid for those rows. The UI states this
(`DurabilityBanner`, W0.8.5). The W0.8.6 spike makes `gl-store` server-authoritative
after tenancy (W0.2).

## financial-truth

| persistKey           | module                           | authority       | server route         | notes                                                                                           |
| -------------------- | -------------------------------- | --------------- | -------------------- | ----------------------------------------------------------------------------------------------- |
| `audit-trail-store`  | `src/store/auditTrailStore.ts`   | **local-draft** | /api/audit           | Client-side audit chain. Official trail is server audit_log (F-AUDIT-001).                      |
| `budget-store`       | `src/store/budgetStore.ts`       | **local-draft** | /api/budgets         | Budget lines. Server route exists; the workspace store is not wired to it.                      |
| `capex-store`        | `src/store/capexStore.ts`        | **local-draft** | —                    | CapEx register and depreciation inputs. Local-draft until a server resource exists.             |
| `cell-lineage-store` | `src/store/cellLineageStore.ts`  | **local-draft** | —                    | Client lineage. Governed lineage graph is F-PLAT-005 (Phase 1).                                 |
| `construction-store` | `src/store/constructionStore.ts` | **local-draft** | —                    | Vertical pack inputs (projects, change orders). Must not seed demo quotes.                      |
| `cube-store`         | `src/store/cubeStore.ts`         | **local-draft** | —                    | OLAP cells. Derived stores are never the system of record (ST2).                                |
| `data-store`         | `src/store/dataStore.ts`         | **local-draft** | —                    | Imported datasets. Local-draft until inbox/outbox (F-INTEGRATE-000).                            |
| `debt-store`         | `src/store/debtStore.ts`         | **local-draft** | —                    | Debt facilities and schedules.                                                                  |
| `driver-store`       | `src/store/driverStore.ts`       | **local-draft** | —                    | Driver-based planning inputs.                                                                   |
| `education-store`    | `src/store/educationStore.ts`    | **local-draft** | —                    | Education-pack operational inputs (enrolment). Not a GL.                                        |
| `energy-store`       | `src/store/energyStore.ts`       | **local-draft** | —                    | Energy-pack generation inputs.                                                                  |
| `entity-store`       | `src/store/entityStore.ts`       | **local-draft** | /api/entities        | Legal-entity tree. Server route exists; store is not the authority.                             |
| `esg-store`          | `src/store/esgStore.ts`          | **local-draft** | —                    | ESG/carbon facts. Same control plane as money once governed.                                    |
| `forecast-store`     | `src/store/forecastStore.ts`     | **local-draft** | /api/forecasts       | Forecast versions. Server route exists; workspace store is unwired.                             |
| `fx-rate-store`      | `src/store/fxRateStore.ts`       | **local-draft** | —                    | FX rates stored with facts (D5). Local-draft until rate table is authoritative.                 |
| `gl-store`           | `src/store/glStore.ts`           | **local-draft** | /api/gl              | THE ledger. W0.8.6 spike target. Server /gl exists; client persist is still the copy users see. |
| `gl-upload-store`    | `src/store/glUploadStore.ts`     | **local-draft** | /api/gl/entries/bulk | GL import wizard mapping and staging.                                                           |
| `government-store`   | `src/store/governmentStore.ts`   | **local-draft** | —                    | Public-sector appropriations. Must not fall back to demo fixtures.                              |
| `healthcare-store`   | `src/store/healthcareStore.ts`   | **local-draft** | —                    | Healthcare-pack operational inputs (trials, quality).                                           |
| `insurance-store`    | `src/store/insuranceStore.ts`    | **local-draft** | —                    | Insurance-pack operational inputs.                                                              |
| `lease-store`        | `src/store/leaseStore.ts`        | **local-draft** | —                    | Lease abstracts and schedules (ASC 842 / IFRS 16).                                              |
| `logistics-store`    | `src/store/logisticsStore.ts`    | **local-draft** | —                    | Logistics-pack operational inputs (shipments).                                                  |
| `period-close-store` | `src/store/periodCloseStore.ts`  | **local-draft** | /api/periods         | Close-task UI state. Period lock itself must be DB-level (F-CLOSE-002).                         |
| `realestate-store`   | `src/store/realEstateStore.ts`   | **local-draft** | —                    | Real-estate pack (properties, REIT inputs).                                                     |
| `report-store`       | `src/store/reportStore.ts`       | **local-draft** | /api/reports         | Saved report definitions. Snapshots must be server-side (Phase 2).                              |
| `retail-store`       | `src/store/retailStore.ts`       | **local-draft** | —                    | Retail-pack operational inputs (promotions).                                                    |
| `scenario-store`     | `src/store/scenarioStore.ts`     | **local-draft** | /api/scenarios       | Scenario versions. Server route exists; store is unwired.                                       |
| `telecom-store`      | `src/store/telecomStore.ts`      | **local-draft** | —                    | Telecom-pack operational inputs (subscribers, ARPU).                                            |
| `workflow-store`     | `src/store/workflowStore.ts`     | **local-draft** | —                    | Approval workflow UI state. Declarative machines are F-WORKFLOW-008.                            |
| `workforce-store`    | `src/store/workforceStore.ts`    | **local-draft** | —                    | Headcount / compensation planning inputs. Masking is F-SEC-003.                                 |

## derived-cache

| persistKey              | module                             | authority       | server route          | notes                                                                       |
| ----------------------- | ---------------------------------- | --------------- | --------------------- | --------------------------------------------------------------------------- |
| `analytics-store`       | `src/store/analyticsStore.ts`      | **local-draft** | —                     | Derived analytics views. Rebuildable from GL/facts; not a system of record. |
| `gl-trialbalance-store` | `src/store/glTrialBalanceStore.ts` | **local-draft** | /api/gl/trial-balance | Trial-balance cache. Rebuildable from gl-store / server TB.                 |
| `variance-store`        | `src/store/varianceStore.ts`       | **local-draft** | —                     | Variance working set. Rebuildable from budget + actuals.                    |

## user-preference

| persistKey            | module                            | authority | server route | notes                                                 |
| --------------------- | --------------------------------- | --------- | ------------ | ----------------------------------------------------- |
| `collaboration-store` | `src/store/collaborationStore.ts` | **local** | —            | Presence/comments cache. Not financial truth.         |
| `dashboard-store`     | `src/store/dashboardStore.ts`     | **local** | —            | Dashboard layout/widgets. Preference, not ledger.     |
| `integration-store`   | `src/store/integrationStore.ts`   | **local** | —            | Connector configuration (no credentials). Not ledger. |
| `notification-store`  | `src/store/notificationStore.ts`  | **local** | —            | In-app notification inbox. Preference/UX, not ledger. |
| `settings-store`      | `src/store/settingsStore.ts`      | **local** | —            | User/tenant display settings. Local is legitimate.    |
| `tour-store`          | `src/store/tourStore.ts`          | **local** | —            | Onboarding tour progress.                             |
| `ui-store`            | `src/store/uiStore.ts`            | **local** | —            | Sidebar, density, toasts. Not financial truth.        |

## session

| persistKey   | module                   | authority | server route | notes                                                                 |
| ------------ | ------------------------ | --------- | ------------ | --------------------------------------------------------------------- |
| `auth-store` | `src/store/authStore.ts` | **local** | /api/auth    | Access/refresh tokens. Not financial truth. Never a durability claim. |

## Authority legend

| Value         | Meaning                                                           |
| ------------- | ----------------------------------------------------------------- |
| `server`      | Control Plane is the system of record. Client is a replica/cache. |
| `local-draft` | Client copy is a **draft**. Must be labelled. Not official.       |
| `local`       | Local is the legitimate authority (preferences, session).         |
