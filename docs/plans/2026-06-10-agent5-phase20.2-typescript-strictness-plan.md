# Phase 20.2 — TypeScript Strictness Plan

**Agent:** A5 | **Phase:** 20 Continuous Improvement & QA | **Subphase:** 2/5
**Created:** 2026-06-10 | **Status:** pending

## Objective

Incrementally remove `any` usage, audit generic parameters, and tighten TypeScript strictness.

## Current State

- ESLint: `@typescript-eslint/no-explicit-any` set to `warn`
- TSC errors: 17 errors (6 files)
- strict mode enabled in tsconfig
- `noUnusedLocals` and `noUnusedParameters` enabled

## Execution Steps

### Step 1: Any Audit

- [ ] Grep for all `any` usage: `rg ':\s*any\b' src/`
- [ ] Categorize: legit (external API) vs. fixable
- [ ] Fix obvious cases: replace with `unknown`, generics, or proper types
- [ ] Add `@ts-expect-error` for known external API mismatches

### Step 2: Fix Existing TSC Errors

- [ ] Fix 17 TSC errors in 6 files:
  - `scenarioStore.ts` (2): `isLocked` property
  - `constants.test.ts` (1): iterator type
  - `batch-calc.worker.ts` (5): `string | undefined` params
  - `consolidation.worker.ts` (1): `string | undefined` param
  - `formulaWorker.ts` (1): index type
  - `monte-carlo.worker.ts` (2): `number | undefined`
  - `worker-compute.test.ts` (3): `number | undefined`
  - `WorkerPool.ts` (2): `undefined` vs `null`

### Step 3: Generic Parameter Audit

- [ ] Audit generic constraints in store factories
- [ ] Audit generic parameters in engine classes
- [ ] Add branded types for financial IDs
- [ ] Add `Readonly` and `Partial` where appropriate

### Step 4: Strictness Improvements

- [ ] Enable `noUncheckedIndexedAccess` (tsconfig)
- [ ] Enable `exactOptionalPropertyTypes`
- [ ] Enable `noPropertyAccessFromIndexSignature`
- [ ] Audit violations of new strict rules

### Step 5: CI Enforcement

- [ ] Change `no-explicit-any` from `warn` to `error`
- [ ] Add TSC check to CI pipeline
- [ ] Add `tsc --noEmit` to pre-commit hook
- [ ] Document type patterns

## Success Criteria

- [ ] Zero TSC errors
- [ ] `no-explicit-any` set to `error`
- [ ] `noUncheckedIndexedAccess` enabled
- [ ] 80% reduction in `any` usage
