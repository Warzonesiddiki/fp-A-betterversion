# Phase 15.1 — Cash Forecast Plan

**Owner:** Agent 4 | **Status:** planned | **Priority:** high

## Scope

Cash forecasting with rolling forecast, DSO/DPO, liquidity planning.

## Existing

- `CashForecastChart.tsx` (18 lines) — WaterfallChart with static data
- `CashForecastChart.test.tsx` — 10 tests, 6 failing (WaterfallChart label rendering mismatch)
- `index.ts` — exports CashForecastChart
- No treasury store or engine

## Issues

1. 6 failing tests (WaterfallChart renders items as ": value" not "label: value")
2. Static hardcoded data
3. No rolling forecast, DSO/DPO, or liquidity components

## Plan

1. Create `treasuryStore.ts` with cash data, forecast periods
2. Update CashForecastChart to accept props and connect to store
3. Create `DSO_DPO_Panel.tsx`
4. Create `LiquidityPlanningPanel.tsx`
5. Create `TreasuryEngine.ts` (DSO/DPO calc, liquidity ratios)
