# Phase 2.2 — Scenario Engine

## Objective
Build complete scenario management — version creation, what-if branches, scenario comparison, and rollback.

## Current State
- `ScenarioEngine.ts` exists but needs feature expansion
- Basic create/load/save scenario operations
- No version diff or comparison logic
- No scenario branching (tree DAG)

## Work Units
| # | Task | Effort |
|---|------|--------|
| 1 | Design scenario data model (DAG of versions) | 2h |
| 2 | Implement `createBranch(parentId, label)` | 2h |
| 3 | Implement `compareScenarios(a, b)` → diff object | 3h |
| 4 | Implement `rollback(scenarioId, versionId)` | 1h |
| 5 | Add persist layer integration with budgetStore | 2h |
| 6 | Write engine tests (coverage > 80%) | 3h |

## Acceptance Criteria
- Scenario tree supports branching (not just linear versions)
- Comparison returns typed diff object with added/changed/removed keys
- Rollback preserves history (no destructive undo)
