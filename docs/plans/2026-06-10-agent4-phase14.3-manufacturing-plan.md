# Phase 14.3 — Manufacturing Plan

**Owner:** Agent 4 | **Status:** planned | **Priority:** high

## Scope

Production dashboard with cost variance, BOM analysis, throughput analysis.

## Existing

- `ProductionDashboard.tsx` (28 lines) — OEE gauge + Availability/Performance/Quality cards
- `ProductionDashboard.test.tsx` — 8 tests, 2 failing (duplicate OEE text, gauge API mismatch)
- `index.ts` — exports ProductionDashboard
- Types: `ProductionMetrics`, `BOMItem` in `sector-types.ts`

## Issues

1. 2 failing tests (duplicate OEE label + "Invalid data" text mismatch)
2. No store or engine for manufacturing
3. No BOM or throughput analysis
4. Type cast `(metrics as unknown as Record<string, number>)` is unsafe

## Plan

1. Fix the unsafe type cast with proper typed access
2. Create `manufacturingStore.ts`
3. Create `ManufacturingEngine.ts` (OEE calc, cost variance, throughput)
4. Create `BOMAnalyzer.tsx`
5. Create `CostVariancePanel.tsx`
