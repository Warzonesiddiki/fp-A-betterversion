# Phase 17.3 — Worker Offloading Plan

**Agent:** A5 | **Phase:** 17 Performance & Offline | **Subphase:** 3/5
**Created:** 2026-06-10 | **Status:** pending

## Objective
Audit and optimize Web Worker usage for Monte Carlo, consolidation, formula, and storage operations.

## Current State
| Worker | Files | Pool |
|--------|-------|------|
| Monte Carlo | `monte-carlo.worker.ts`, test | 2 workers, 120s timeout |
| Consolidation | `consolidation.worker.ts`, test | 1 worker, 60s timeout |
| Batch Calc | `batch-calc.worker.ts`, test | 2 workers, 30s timeout |
| Formula | `formulaWorker.ts`, test | (legacy, standalone) |
| Scenario | `scenarioWorker.ts`, test | (legacy, standalone) |
| Export | `exportWorker.ts`, test | (legacy, standalone) |
| Storage | `storage.worker.ts` | Pooled, 1 worker |
| WorkerPool | `worker-pool.ts`, `WorkerPool.ts`, tests | Dual files, needs dedup |

## Execution Steps

### Step 1: Worker Pool Consolidation
- [ ] Deduplicate `worker-pool.ts` vs `WorkerPool.ts` (two files with similar logic)
- [ ] Merge into single `WorkerPool` class
- [ ] Fix TSC errors in worker files (17 errors)

### Step 2: Legacy Worker Migration
- [ ] Audit formulaWorker.ts — migrate to worker-pool pattern
- [ ] Audit scenarioWorker.ts — migrate to worker-pool pattern
- [ ] Audit exportWorker.ts — migrate to worker-pool pattern

### Step 3: Worker Lifecycle Optimization
- [ ] Add worker warm-up on app idle
- [ ] Implement worker recycling threshold
- [ ] Add structured clone optimization for large payloads
- [ ] Review transferable objects usage

### Step 4: Error Handling & Timeouts
- [ ] Add worker crash recovery (current: single retry)
- [ ] Improve timeout granularity per task type
- [ ] Add progress reporting bandwidth control

### Step 5: Storage Worker Enhancement
- [ ] Audit storage.worker.ts for heavy operations
- [ ] Add IndexedDB batch operations
- [ ] Add compression/decompression in worker

## Success Criteria
- [ ] All workers use unified pool
- [ ] Zero TSC errors in workers/
- [ ] 50ms response for formula calculation
- [ ] Worker crash recovery proven
