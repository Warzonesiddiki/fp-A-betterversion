# Phase 15.2 — Debt Schedule Plan

**Owner:** Agent 4 | **Status:** planned | **Priority:** high

## Scope

Debt schedule with interest calculation, covenant tracking, refinancing analysis.

## Existing

- No dedicated components
- `finance/` directory has `RevRecSchedule`, `DepreciationProjection`, `LeaseSchedule`, `FXPositionGrid`, etc.

## Plan

1. Create `DebtSchedule.tsx` — amortization table, interest calc
2. Create `CovenantTracker.tsx` — covenant compliance dashboard
3. Create `RefinancingAnalyzer.tsx` — what-if refi scenarios
4. Create `debtStore.ts`
5. Create `DebtEngine.ts` (amortization, covenant ratios)
