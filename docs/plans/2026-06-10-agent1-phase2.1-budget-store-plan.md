# Phase 2.1 — Budget Store (Zustand) Optimization

## Objective
Audit and optimize `budgetStore.ts` — ensure proper Zustand/Immer/subscribeWithSelector/persist middleware chain, fix type issues, and add missing actions.

## Current State
- Store located at `src/store/budgetStore.ts`
- Uses Zustand + Immer + subscribeWithSelector + persist (masterStorage)
- State includes: budgets, scenarios, versions, activeBudgetId, ui state

## Work Units
| # | Task | Effort |
|---|------|--------|
| 1 | Audit middleware chain ordering (per AGENTS.md: subscribeWithSelector > persist > immer) | 1h |
| 2 | Add missing CRUD actions (clone budget, archive, version diff) | 3h |
| 3 | Add `getActiveBudget` computed selector with proper typing | 1h |
| 4 | Add unit tests for all actions (Vitest) | 3h |
| 5 | Verify persist serialization/deserialization of complex types (Map, Set, Date) | 1h |

## Acceptance Criteria
- Middleware chain matches AGENTS.md ordering
- All CRUD actions have tests
- No `any` types in store definitions
