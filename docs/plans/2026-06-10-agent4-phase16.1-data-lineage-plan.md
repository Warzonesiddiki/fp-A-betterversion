# Phase 16.1 — Data Lineage Plan

**Owner:** Agent 4 | **Status:** planned | **Priority:** high

## Scope

Data lineage viewer with cell provenance and calculation chain tracing.

## Existing

- `DataLineageViewer.tsx` (94 lines) — SVG-based graph with source/transform/report nodes
- `DataLineageViewer.test.tsx` — 14 tests, passes
- Component is in `src/components/analytics/`

## Issues

1. SVG layout is basic — nodes placed by index only, edges don't handle complex DAGs
2. No store for lineage data
3. No cell-level provenance tracking

## Plan

1. Create `lineageStore.ts` with node/edge management
2. Enhance DataLineageViewer with DAG layout (topological sort, layered rendering)
3. Create `CellProvenancePanel.tsx` — cell-level drill-through
4. Create `CalculationChainViewer.tsx`
