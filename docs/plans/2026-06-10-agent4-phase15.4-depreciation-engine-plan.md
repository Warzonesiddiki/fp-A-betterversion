# Phase 15.4 — Depreciation Engine Plan

**Owner:** Agent 4 | **Status:** planned | **Priority:** medium

## Scope
Depreciation projection with multiple methods (SL/DB/units of production) and asset lifecycle.

## Existing
- `DepreciationProjection.tsx` (13 lines) — ComboChart with empty data
- `DepreciationProjection.test.tsx` — 5 tests, passes
- Types: `AssetInput`, `DepreciationSchedule` in `sector-types.ts`

## Issues
1. ComboChart with empty data array
2. No engine logic despite types being defined
3. Only one method placeholder

## Plan
1. Create `depreciationStore.ts`
2. Create `DepreciationEngine.ts` (straight-line, double-declining, sum-of-years)
3. Connect DepreciationProjection to store with real data
4. Create `AssetLifecycleManager.tsx`
