# Phase 14.4 — Retail/Inventory Plan

**Owner:** Agent 4 | **Status:** planned | **Priority:** high

## Scope

Store dashboard with inventory turnover, margin analysis, promo effectiveness.

## Existing

- `StoreDashboard.tsx` (16 lines) — FinancialTable with empty rows and 5 column definitions
- `StoreDashboard.test.tsx` — 5 tests, passes
- `index.ts` — exports StoreDashboard
- Types: `RetailMetrics` in `sector-types.ts` — 10 fields

## Issues

1. Empty rows in FinancialTable
2. No store or engine for retail
3. No promo effectiveness or margin analysis

## Plan

1. Create `retailStore.ts` with store data, inventory, promo data
2. Connect StoreDashboard to store data
3. Create `InventoryTurnoverPanel.tsx`
4. Create `PromoEffectivenessPanel.tsx`
5. Create `RetailEngine.ts`
