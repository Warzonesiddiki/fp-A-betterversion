# Phase 14.1 — SaaS Metrics Plan

**Owner:** Agent 4 | **Status:** planned | **Priority:** high

## Scope
SaaS metrics dashboard with MRR breakdown, churn waterfall, cohort tables, unit economics.

## Existing
- `MRRBreakdown.tsx` (18 lines) — ComboChart with empty data
- `MRRBreakdown.test.tsx` — 4 tests, passes
- `ChurnWaterfall.tsx` (18 lines) — WaterfallChart with static data
- `ChurnWaterfall.test.tsx` — 5 tests, passes
- `SaaSCohortTable.tsx` (58 lines) — table with retention heatmap
- `SaaSCohortTable.test.tsx` — 10 tests, passes
- `SaaSMetricsEngine.ts` in engines/ — ARR, NRR, churn, LTV/CAC, magic number, quick ratio
- `index.ts` — exports all three

## Issues
1. MRRBreakdown uses empty data array
2. ChurnWaterfall uses hardcoded static data
3. No store for SaaS metrics
4. No unit economics component

## Plan
1. Create `saasStore.ts` with MRR, cohort, customer data
2. Connect MRRBreakdown to real store data
3. Connect ChurnWaterfall to store data
4. Create `UnitEconomicsPanel.tsx` (LTV/CAC, payback, gross margin)
5. Add comprehensive tests for store connection
