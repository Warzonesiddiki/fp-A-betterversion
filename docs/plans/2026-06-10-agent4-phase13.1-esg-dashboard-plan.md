# Phase 13.1 — ESG Dashboard Plan

**Owner:** Agent 4
**Status:** planned
**Priority:** high

## Scope
Implement/upgrade ESG Dashboard components for comprehensive ESG KPI tracking.

## Components
- `ESGDashboard.tsx` — high-level landing dashboard (uses GaugeChart, Card)
- `ESGMetricsDashboard.tsx` — detailed ESG metrics with Recharts bar chart, KPIValue cards, initiatives overview, diversity metrics

## Existing Files
- `src/components/esg/ESGDashboard.tsx` — 34 lines, static dashboard with gauge, emissions bars, diversity score
- `src/components/esg/ESGDashboard.test.tsx` — 8 tests, all passing
- `src/components/esg/ESGMetricsDashboard.tsx` — 300 lines, connects to useESGStore + ESGEngine
- `src/components/esg/ESGMetricsDashboard.test.tsx` — 1 test, passes
- `src/store/esgStore.ts` — Zustand store with persist/immer middleware
- `src/engines/ESGEngine.ts` — carbon calc, scope emissions, diversity score

## Issues Found
1. **Line 216 duplicate `role="alert"`** in ESGMetricsDashboard.tsx — `{' '} role="alert" role="alert"` rendered as text instead of JSX attribute
2. Shallow test coverage (ESGMetricsDashboard only 1 test)
3. Diversity metrics use empty workforce array `[]` (line 78)

## Implementation Plan
1. Fix JSX bug (duplicate role="alert")
2. Add comprehensive tests for ESGMetricsDashboard
3. Add ESG KPI framework type definitions
4. Connect ESG Dashboard to store data instead of hardcoded values
