<!-- DRAFT v0.1 — awaiting review — Hera 2026-06-13 — T-HE-005 -->

diff --git a/src/components/ui/BoxPlotChart.tsx b/src/components/ui/BoxPlotChart.tsx
index 7af8d703..7ff30431 100644
--- a/src/components/ui/BoxPlotChart.tsx
+++ b/src/components/ui/BoxPlotChart.tsx
@@ -89,7 +89,7 @@ export const BoxPlotChart: React.FC<BoxPlotChartProps> = React.memo(
                 <div className="relative w-full h-full flex items-center justify-center">
                   {/* Whiskers */}
                   <div
-                    className="absolute w-px bg-gray-400"
+                    className="absolute w-px bg-gray-400 dark:bg-gray-500"
                     style={{
                       bottom: `${scale(item.min)}%`,
                       height: `${scale(item.max) - scale(item.min)}%`,
diff --git a/src/components/ui/BulletChart.tsx b/src/components/ui/BulletChart.tsx
index b98aac66..8518227d 100644
--- a/src/components/ui/BulletChart.tsx
+++ b/src/components/ui/BulletChart.tsx
@@ -88,12 +88,12 @@ export const BulletChart: React.FC<BulletChartProps> = React.memo(
           ))}
           {/* Actual bar */}
           <div
-            className="absolute h-full bg-gray-800 rounded"
+            className="absolute h-full bg-gray-800 dark:bg-gray-200 rounded"
             style={{ width: `${(actual / max) * 100}%` }}
           />
           {/* Target marker */}
           <div
-            className="absolute h-full w-1 bg-black"
+            className="absolute h-full w-1 bg-black dark:bg-white"
             style={{ left: `${(target / max) * 100}%` }}
           />
         </div>
