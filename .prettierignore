node_modules
dist
package-lock.json
src-tauri
.tauri
AUDIT_LOGS
reports
# Generated artifact (scripts/generate-route-map.js owns it; K7). The drift
# check compares generator output verbatim, so prettier must not reformat it.
docs/product/ROUTE_MAP.md
# Machine-generated DSH plugin-market acquisition artifacts (bulk JSON verdicts).
.market-acquire/
.market-catalog.json
# Append-only multi-agent comms inboxes — messages are appended verbatim by
# agents mid-cycle; reformatting them after every append is unworkable and the
# files are operational logs, not published docs (F-0024: real checks only).
agents/team-comms/inbox-*.md
agents/coordination/tasks/
