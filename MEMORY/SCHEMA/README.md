---
id: MEMORY/SCHEMA/README.md
status: active
last_verified: 2026-08-18
verified_by: arena-agent/session-017
confidence: high
---

# SCHEMA — as it EXISTS

Extracted from real files only: `src-tauri/migrations/*.sql`, `server/src/db/*.ts`,
`server/src/routes/*.ts`, `src/types/*.ts`, `.env.example`.

`do_not_invent: true` — if a table, column, endpoint or env var is not listed here, verify on disk
before naming it. There is **no** Prisma/Drizzle schema in this repo.
